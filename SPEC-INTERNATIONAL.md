# Spec — International public-good extensions to encryptalotta

Status: draft 57 — draft 57 lands **UBL Seller VAT identifier validation** (§2.7 polish, EN 16931 BT-31 / PEPPOL BIS Billing 3.0). The supplier VAT number declared in `AccountingSupplierParty/Party/PartyTaxScheme/CompanyID` (paired with `TaxScheme/cbc:ID=VAT`) is the canonical buyer-side and tax-authority-side identifier for the seller; PEPPOL BIS Billing 3.0 requires the value to start with a valid ISO 3166-1 alpha-2 country code prefix followed by the member-state VAT format, and EN 16931 BR-CO-09 makes the Seller VAT ID mandatory for standard-rated invoices. UBL 2.1 declares `cbc:CompanyID` as plain identifier text, so a value missing the country prefix or carrying a malformed body is schema-valid but rejected by PEPPOL access points at the BT-31 schematron gate. The new `ublCheckSellerVat()` walks `AccountingSupplierParty/Party/PartyTaxScheme` blocks, picks the one whose nested `TaxScheme/cbc:ID = VAT`, reads `cbc:CompanyID`, and routes the value through the same `vatValidate()` the standalone EU VAT tool (§2.3) uses — same format + ISO-7064 / MOD-11 / weighted-checksum logic, no duplication. Emits one green `✓ Seller VAT identifier X valid per ISO 3166-1 alpha-2 + EU VAT format (EN 16931 BT-31 / PEPPOL BIS Billing 3.0)` row on success or one red `✗ Seller VAT identifier X invalid — PEPPOL BIS Billing 3.0 / EN 16931 BT-31 requires a valid ISO 3166-1 alpha-2 country prefix + EU VAT format` row on failure. Skips silently when no PartyTaxScheme/CompanyID with TaxScheme/ID=VAT is present — the existing supplier header already surfaces what's declared, and a real access point's schema-validation step would catch BR-CO-09 (mandatory Seller VAT ID for standard-rated invoices) upstream. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `DE136695976` injected into PartyTaxScheme → confirmation citing ISO 3166-1 alpha-2; same fixture with `123456789` (missing country prefix) → mismatch row citing PEPPOL BIS Billing 3.0 / BT-31). Audit OK. Full suite expected 304 → 306 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 56 lands **SEPA per-PmtInf `PmtInfId` character-set check** (§2.4 polish, EPC SEPA Rulebook), extending the EPC character-set rule from drafts 49 / 51 / 53 / 55 to the per-payment-information-block identifier — the fifth free-text identifier the EPC rulebooks pin to the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space). ISO 20022 declares `PmtInfId` as `Max35Text`, so any Unicode string up to 35 codepoints passes the schema; the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks both require the SEPA character set to hold across every free-text identifier in the file. The canonical real-world failure mode is a batch generator interpolating a company name (e.g. `Übermäßig GmbH`) into the block identifier — schema-valid Max35Text, rail-broken at the EBA STEP2 / STET / RT1 character-set gate. The new `sepaCheckPmtInfIdCharset()` walks every `<PmtInf>` in pain.001 / pain.008 wrappers, reads each block's `<PmtInfId>` (trimmed), tests against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ PmtInfId values use only EPC SEPA Rulebook allowed characters across all N PmtInf block(s) …` row when every value is clean, or one red `✗ PmtInfId X contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value. Blocks without a PmtInfId skip silently — the existing PmtInf header rendering surfaces what's declared. With drafts 49 / 51 / 53 / 55 / 56 the SEPA character-set gate covers MsgId (document-level), EndToEndId + InstrId (transaction-level), MndtId (pain.008 mandate reference), and PmtInfId (per-block payment-information identifier) — the full free-text identifier surface in pain.001 / pain.008. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `PmtInfId=PAY-001` → confirmation citing the 1-block count; same fixture with `PAY-Übermäßig` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full suite expected 304 → 306 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 55 lands **SEPA pain.008 `MndtId` character-set check** (§2.4 polish, EPC SDD Rulebook), extending the EPC SEPA character-set rule from drafts 49 / 51 / 53 to the per-transaction mandate identifier. The EPC SEPA Direct Debit Rulebook requires the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) to hold across every free-text identifier in pain.008 — including `<DrctDbtTx>/<MndtRltdInf>/<MndtId>`, the mandate reference the creditor and debtor agreed on. ISO 20022 declares MndtId as Max35Text, so accented characters (commonly seen when a debtor's name is folded into the mandate reference) pass the schema; clearing systems strip or reject the value at the character-set gate, breaking mandate-level reconciliation. The new `sepaCheckMndtIdCharset()` walks every `<DrctDbtTxInf>` in pain.008 wrappers (pain.001 skips — credit transfers don't carry mandates), reads each `<DrctDbtTx>/<MndtRltdInf>/<MndtId>` (trimmed), tests against the same `SEPA_CHARSET_RE` regex, and emits one green `✓ MndtId values use only EPC SDD Rulebook allowed characters across all N direct-debit transaction(s) …` row when every value is clean, or one red `✗ MndtId X contains characters outside the EPC SDD Rulebook allowed set …` row per offending value. With this draft the SEPA character-set checks cover MsgId (document-level), EndToEndId + InstrId (transaction-level credit-transfer + direct-debit), and MndtId (pain.008-specific). 2 new strings × 5 locales (en/fr/de reviewed; zh-CN/hi machine drafts). Two new Playwright cases. Audit OK; full suite expected 302 → 304 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 54 lands **UBL `TaxScheme/ID = VAT` enforcement** (§2.7 polish, PEPPOL BIS Billing 3.0 / UNCL 5153). UBL 2.1 allows `cac:TaxScheme/cbc:ID` to carry any UNCL 5153 tax-scheme code (GST, HST, VAT, etc.) but PEPPOL BIS Billing 3.0 narrows the value to "VAT" only — non-EU UBL templates repurposed for PEPPOL invoicing are the canonical failure mode this gate catches. The new `ublCheckTaxScheme()` walks every `<cac:TaxScheme>` reachable from the root (both header-side `TaxSubtotal/TaxCategory/TaxScheme` and item-level `ClassifiedTaxCategory/TaxScheme`), reads each `<cbc:ID>` (uppercased, deduped), and emits a single ✓ or ✗ row per offending code. Distinct from the VAT category-letter check (draft 41) and from the VAT-rate / totals checks (drafts 28 / 37): those validate values inside the tax breakdown; this check validates the tax-scheme designator itself. 2 new strings × 5 locales (en/fr/de reviewed against PEPPOL BIS Billing 3.0 / UNCL 5153 vocabulary; zh-CN/hi machine drafts). Two new Playwright cases. Audit OK; full suite expected 300 → 302 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 53 lands **SEPA transaction-level `InstrId` character-set check** (§2.4 polish, EPC SEPA Rulebook §AT-43 / ISO 20022 BasicLatin), the third character-set gate after the document-level `MsgId` gate from draft 49 and the transaction-level `EndToEndId` gate from draft 51. The EPC SEPA Credit Transfer and Direct Debit Rulebooks require the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) to hold across every free-text identifier; `InstrId` is the originator-side instruction key the rulebooks describe as a free-text identifier set by the initiating party for its own bookkeeping, commonly populated from internal sequence keys or batch references that may carry punctuation outside the allowed set. The new `sepaCheckInstrIdCharset()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` in pain.001 / pain.008 wrappers, reads each transaction's `<PmtId>/<InstrId>` (whitespace-trimmed), tests it against the same `SEPA_CHARSET_RE` regex from drafts 49 / 51, and emits a green `✓ InstrId values use only EPC SEPA Rulebook allowed characters across all N transaction(s) …` row when every value is clean or a red `✗ InstrId X contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value. Transactions without an InstrId skip silently — the existing per-transaction renderer already surfaces what's declared. With this draft the three character-set gates (MsgId / EndToEndId / InstrId) cover every free-text identifier the EPC rulebooks pin to the SEPA character set. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `InstrId=INSTR-001` → confirmation citing the 1-transaction count; same fixture with `INSTR-Übergabe` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK; full suite expected 300 → 302 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 52 lands **UBL PEPPOL `ProfileID` validation** (§2.7 polish), the second profile-level gate on the UBL side after the `CustomizationID` gate from draft 50. PEPPOL BIS Billing 3.0 defines a single process URN — `urn:fdc:peppol.eu:2017:poacc:billing:01:1.0` ("Billing 01: Invoicing") — and conforming documents declare it at the root via `<cbc:ProfileID>`. A UBL invoice carrying any other process URN is rejected by PEPPOL access points at the routing layer, because `ProfileID` selects the SML/SMP service-metadata record the receiver advertises; a mismatch sends the document to a routing dead-end. Parallel in shape to draft 50: CustomizationID names the document profile, ProfileID names the transaction process. The new `ublCheckProfileId()` reads the root `<cbc:ProfileID>` (trimmed), compares to the canonical PEPPOL string stored in an inline `UBL_PEPPOL_PROFILE_ID` constant, and emits one green `✓ ProfileID declares the PEPPOL BIS Billing 3.0 process (…)` row on exact match or a red `✗ ProfileID X does not match the PEPPOL BIS Billing 3.0 process (Y) — PEPPOL access points route documents by ProfileID, mismatches dead-end at the SML/SMP lookup` row otherwise. Missing element skips silently. With drafts 50 + 52 in place both PEPPOL profile-level gates are covered. 2 new strings × 5 locales (en/fr/de reviewed against PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical PEPPOL process URN → confirmation; `urn:fdc:peppol.eu:2017:poacc:billing:99:9.9` injected → mismatch row citing both actual and expected). Audit OK; full suite expected 298 → 300 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 51 lands **SEPA transaction-level `EndToEndId` character-set check** (§2.4 polish, EPC SEPA Rulebook §AT-43 / ISO 20022 BasicLatin) extending the document-level `MsgId` gate from draft 49 to the per-transaction reconciliation identifier. The EPC SEPA Credit Transfer and Direct Debit Rulebooks both require the SEPA character set — `A-Z a-z 0-9 / - ? : ( ) . , ' +` and space — to hold across every free-text identifier; MsgId is only one entry point, and transaction-level `EndToEndId` values are commonly populated from customer / invoice references that originated in a CRM or ERP and may carry characters the originator never sanitised. The new `sepaCheckE2eIdCharset()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>`, reads each transaction's `<PmtId>/<EndToEndId>` (trimmed), and emits one green `✓ EndToEndId values use only EPC SEPA Rulebook allowed characters across all N transaction(s) …` row when every value is clean, or one red `✗ EndToEndId X contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (keyed by the bad identifier so the maintainer can locate the source record). Transactions without an EndToEndId skip silently — the existing per-transaction renderer already surfaces what's declared. Pairs naturally with the uniqueness gate from draft 43 (uniqueness covers content; this check covers form). 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `E2E-001` → confirmation citing the 1-transaction count; same fixture with `E2E-Müller` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK; full suite expected 298 → 300 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 50 lands **UBL PEPPOL `CustomizationID` validation** (§2.7 polish). PEPPOL BIS Billing 3.0 invoices and credit notes MUST declare `<cbc:CustomizationID>urn:cen.eu:en16931:2017#compliant#urn:fdc:peppol.eu:2017:poacc:billing:3.0</cbc:CustomizationID>` at the document root — the CEN prefix asserts EN 16931 compliance and the PEPPOL suffix narrows the document to the BIS Billing 3.0 subset. A schema-valid UBL 2.1 invoice carrying any other URN (or a stripped-down EN 16931 URN without the PEPPOL suffix) is still UBL but is rejected by PEPPOL access points at the profile gate; the typical real-world cause is a Factur-X / ZUGFeRD or generic UBL template repurposed for PEPPOL without updating the URN. The new `ublCheckCustomizationId()` reads the root `<cbc:CustomizationID>`, compares it to the canonical PEPPOL string, and emits one green `✓ CustomizationID declares PEPPOL BIS Billing 3.0 conformance …` row on exact match or a red `✗ CustomizationID X does not match PEPPOL BIS Billing 3.0 (expected) — PEPPOL access points reject documents at the profile gate` row otherwise. Missing element skips silently (the existing `CustomizationID` rendering already surfaces absence). Distinct from the UNCL 1001 document-type-code check (draft 47, validates per-document code values) and the UNCL 5305 VAT category-code check (draft 41, validates per-line category letters): those check codes inside the document, this check validates the profile the document claims conformance to. 2 new strings × 5 locales (en/fr/de reviewed against PEPPOL BIS Billing 3.0 / EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with the canonical PEPPOL URN → confirmation; same fixture with the URN reduced to `urn:cen.eu:en16931:2017` → mismatch row citing both the actual and expected values and the PEPPOL profile-gate failure mode). Audit OK; full suite expected 296 → 298 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 49 lands **SEPA `MsgId` EPC character-set check** (§2.4 polish, EPC SEPA Rulebook §AT-43 / ISO 20022 BasicLatin). ISO 20022 declares `GrpHdr/MsgId` as `Max35Text`, so any Unicode string up to 35 codepoints passes the schema; the EPC SEPA Credit Transfer and Direct Debit Rulebooks both restrict free-text fields and identifiers to a narrow subset of ISO 20022 BasicLatin: `A-Z a-z 0-9 / - ? : ( ) . , ' +` and space. A `MsgId` carrying accented letters, em-dashes, or emoji pasted from a CRM template is schema-valid XML but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the bad characters before forwarding (which destroys the reconciliation reference) or hard-reject the file at the character-set gate. The new `sepaCheckMsgIdCharset()` reads `GrpHdr/MsgId` on pain.001 / pain.008 wrappers, tests it against the regex `^[A-Za-z0-9/\-?:().,'+ ]+$`, and emits one green `✓ GrpHdr/MsgId X uses only EPC SEPA Rulebook allowed characters …` row on match or a red `✗ GrpHdr/MsgId X contains characters outside the EPC SEPA Rulebook allowed set …` row otherwise. Header-only by design — `EndToEndId` uniqueness and `InstrId` presence have their own gates (drafts 43, 19) and the character-set failure mode is dominated by the document-level identifier in practice. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `MsgId=MSG-2024-001` → confirmation citing the allowed set; same fixture with `Ää` injected → mismatch row citing the EPC SEPA character-set requirement). Audit OK; full suite expected 296 → 298 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 48 lands **UBL line-ID uniqueness check** (§2.7 polish, EN 16931 BR-21 / PEPPOL BIS Billing 3.0). EN 16931 BR-21 names `cbc:ID` as the InvoiceLine identifier; PEPPOL BIS Billing 3.0 access points additionally reject duplicates across two different lines because downstream processors key per-line allowances, charges, and dispute references off the ID, so a collision causes silent reconciliation failures. ISO 12047 / UBL 2.1 declare `cbc:ID` as plain `Identifier` text, so duplicates are schema-valid but rail-broken; the typical real-world cause is a batch generator iterating over line items without advancing the sequence counter. The new `ublCheckLineIdUnique()` walks every `<cac:InvoiceLine>` (or `<cac:CreditNoteLine>` for credit notes — selected via `detected.type`), extracts each line's `<cbc:ID>` (whitespace-trimmed), tallies occurrences in a `Map`, and emits a single green `✓ Line IDs unique across the document (N line(s)) per EN 16931 BR-21 / PEPPOL BIS Billing 3.0` row when all values are distinct, or one red `✗ Line ID duplicated within document: X appears N times — EN 16931 BR-21 / PEPPOL BIS Billing 3.0 requires unique line identifiers` row per duplicated identifier. Skips silently when fewer than two lines carry an ID — single-line invoices trivially satisfy uniqueness, and the inspector doesn't add noise for the degenerate case. Parallel in shape to the SEPA `EndToEndId` uniqueness gate from draft 43; both gates fire on the canonical batch-generator bug where the sequence counter doesn't advance. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (two InvoiceLines carrying distinct `cbc:ID=1` / `cbc:ID=2` → confirmation citing BR-21; both lines reusing `cbc:ID=1` → duplicate row citing the count and BR-21 requirement). Audit OK; full suite expected 294 → 296 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 47 lands **UBL `InvoiceTypeCode` / `CreditNoteTypeCode` validation** (§2.7 polish, UNCL 1001 / EN 16931 / PEPPOL BIS Billing 3.0). PEPPOL BIS Billing 3.0 restricts the UNCL 1001 document type code to a small allowed set per root: `<cbc:InvoiceTypeCode>` on `<Invoice>` documents may carry 380 (Commercial invoice), 381, 384, 389, 875, 876, 877; `<cbc:CreditNoteTypeCode>` on `<CreditNote>` documents may carry 381 (Credit note), 396 (Factored credit note), 532 (Forwarders credit note). Any other value is schema-valid plain integer text but PEPPOL access points reject the document at the type-code check. The new `ublCheckDocumentTypeCode()` reads the document-type element from the root (selected by `detected.type`), and emits one green `✓ Document type code N is allowed by PEPPOL BIS Billing 3.0 / EN 16931 (UNCL 1001)` row on match or a red `✗ Document type code N is not in the PEPPOL BIS Billing 3.0 allowed set (…)` row citing the full allowed set via `intlList()` so the locale joiner applies. Missing element skips silently (the existing header rendering surfaces what's declared, and BR-02 absence is caught elsewhere). 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / UNCL 1001 vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (InvoiceTypeCode=380 injected → confirmation citing UNCL 1001; InvoiceTypeCode=999 → mismatch row citing the allowed set). Audit OK; full suite expected 292 → 294 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 46 lands **SEPA pain.008 `SeqTp` validation** (§2.4 polish, EPC SDD Rulebook). ISO 20022 declares `PmtTpInf/SeqTp` as `Max4Text`, so any 4-character value passes the schema; the EPC SEPA Direct Debit Rulebook restricts it to four codes — `FRST` (first collection of a recurring mandate), `RCUR` (recurring), `OOFF` (one-off), `FNAL` (final collection). A pain.008 declaring `RECR` (a common typo for RCUR) or any other value is schema-valid XML but rail-broken — clearing systems reject it at the rail-level sequence-type gate. The new `sepaCheckSeqTp()` walks every `<PmtInf>` in pain.008 wrappers, reads `PmtTpInf/SeqTp` (case-folded and whitespace-stripped for paste-safety), and emits one ✓ `SeqTp valid per EPC SDD Rulebook: N PmtInf block(s) declare one of FRST / RCUR / OOFF / FNAL` confirmation row across all matching blocks, or one ✗ `SeqTp invalid: PmtInf #i declares X but EPC SDD Rulebook allows only FRST / RCUR / OOFF / FNAL` row per offending block keyed by the 1-based PmtInf index. pain.001-only — SeqTp is a direct-debit construct — so credit-transfer files skip silently. Blocks that omit `PmtTpInf` or `SeqTp` also skip silently, parallel to the PmtMtd / ChrgBr / SvcLvl checks from drafts 40 / 42 / 44. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SDD vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed pain.008 with `SeqTp=FRST` → confirmation; same fixture with `SeqTp=RECR` → mismatch row citing the EPC SDD allowed set). Audit OK; full suite expected 290 → 292 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 45 lands **UBL `PayableAmount` consistency check** (§2.7 polish, EN 16931 BR-CO-16). PEPPOL BIS Billing 3.0 invoices declare `LegalMonetaryTotal/PayableAmount` as the figure the buyer is actually asked to pay; EN 16931 business rule BR-CO-16 pins it to `TaxInclusiveAmount − PrepaidAmount + PayableRoundingAmount` (the two subtrahends default to 0 when absent). A mismatch is schema-valid arithmetic but rail-broken — PEPPOL access points reject the invoice at the BR-CO-16 check, and the typical real-world cause is a prepaid amount applied in the buyer's accounting but not subtracted from `PayableAmount`, so the invoice asks for the wrong figure. The new `ublCheckPayable()` reads the four amounts from `LegalMonetaryTotal`, treats missing `PrepaidAmount` / `PayableRoundingAmount` as 0 cents, computes the expected value in integer cents (±1¢ tolerance per the EN 16931 rounding policy), and emits a single ✓ `PayableAmount X matches TaxInclusive − Prepaid + PayableRounding (EN 16931 BR-CO-16)` row when the invariant holds or a ✗ row citing both the computed and declared values otherwise. The third file-wide accounting gate on the UBL side, complementing the line-sum and tax-inclusive invariants from draft 28. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (PayableAmount=119.00 on TaxInclusive=119.00 → confirmation; PayableAmount=120.00 on TaxInclusive=119.00 → mismatch row citing both values and BR-CO-16). Audit OK; full suite expected 288 → 290 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 44 lands **SEPA `SvcLvl=SEPA` enforcement** (§2.4 polish, EPC SEPA Rulebook). ISO 20022 allows `PmtTpInf/SvcLvl/Cd` to carry any 4-character service-level code (NURG, URGP, PRPT, …); the EPC SEPA Credit Transfer and Direct Debit Rulebooks both pin it to `SEPA` for the standard SCT/SDD service level. A pain.001 or pain.008 declaring NURG / URGP is schema-valid XML but rail-broken — clearing systems reject it at the rail-level service-level gate, and the typical real-world cause is a template repurposed across SEPA and non-SEPA flows without updating the field. The new `sepaCheckSvcLvl()` walks every `<PmtInf>`, reads `PmtTpInf/SvcLvl/Cd` (case-folded and whitespace-stripped for paste-safety), and emits a single ✓ `SvcLvl matches EPC SEPA Rulebook: N PmtInf block(s) declare SEPA (standard SCT/SDD service level)` confirmation row across all matching blocks, or one ✗ `SvcLvl mismatch: PmtInf #i declares X but EPC SEPA Rulebook requires SEPA` row per offending block keyed by the 1-based PmtInf index. SvcLvl is optional per the schema, so blocks that omit `PmtTpInf` or `SvcLvl/Cd` skip silently — parallel to the PmtMtd / ChrgBr checks from drafts 40 / 42. Together with PmtMtd (TRF/DD) and ChrgBr (SLEV), this completes the three EPC-rulebook scalar PmtInf-level gates. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed pain.001 with `SvcLvl/Cd=SEPA` injected → confirmation; same fixture with `Cd=NURG` → mismatch row citing the SEPA requirement). Audit OK; full suite expected 286 → 288 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 43 lands **SEPA `EndToEndId` uniqueness check** (§2.4 polish, ISO 20022 / EPC SEPA Rulebook). Both the EPC SEPA Credit Transfer Rulebook (pain.001) and SEPA Direct Debit Rulebook (pain.008) require the `EndToEndId` carried in `PmtId/EndToEndId` to be unique per Originator — it is the reconciliation key the beneficiary and the originator both use to track the payment end-to-end through the clearing system. ISO 20022 lets `EndToEndId` be any `Max35Text`, so duplicates are schema-valid but rail-broken; the typical real-world cause is a batch-generation script that forgot to advance a sequence counter, which surfaces as a hard reject (or worse, a silent reconciliation collision) at EBA STEP2 / STET / RT1 once the file leaves the originator. The new `sepaCheckE2eIdUnique()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` across all `PmtInf` blocks, collects each transaction's `EndToEndId`, and emits a single ✓ `EndToEndId values are unique across the file (N transaction(s)) per EPC SEPA Rulebook` confirmation row when all values are distinct, or one ✗ `EndToEndId duplicated within file: X appears N times — EPC SEPA Rulebook requires uniqueness per Originator (ISO 20022 / EPC SCT/SDD)` row per duplicated identifier. Skips silently when fewer than two transactions carry an `EndToEndId` — single-transaction files trivially satisfy uniqueness, and the inspector doesn't add noise. Pairs naturally with the `NbOfTxs` + `CtrlSum` invariants from draft 35 (the third file-wide accounting gate) and complements the per-PmtInf scalar checks from drafts 40 (PmtMtd) / 42 (ChrgBr). 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed pain.001 with two distinct `EndToEndId` values → confirmation; same fixture with the second transaction reusing `E2E-001` → duplicate row citing the count and the EPC SEPA uniqueness requirement). Audit OK; full suite expected 284 → 286 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 42 lands **SEPA ChrgBr (charge bearer) enforcement** (§2.4 polish, EPC SEPA Rulebook). ISO 20022 enumerates `ChrgBr` as one of `DEBT / CRED / SHAR / SLEV` at the `<PmtInf>` level; the EPC SEPA Credit Transfer and Direct Debit Rulebooks both pin it to a single value — `SLEV` ("charges shared, allocated by local law"). A SEPA pain.001 or pain.008 declaring `DEBT`, `CRED`, or `SHAR` is schema-valid XML but rail-broken — SEPA clearing systems reject it because the charge-bearer contradicts the rulebook's settlement model, and the typical real-world cause is a template repurposed across SEPA and non-SEPA wire-instruction flows without updating the field. The new `sepaCheckChrgBr()` walks every `<PmtInf>`, reads its `<ChrgBr>` element (case-folded and whitespace-stripped for paste-safety), and emits a single ✓ `ChrgBr matches EPC SEPA Rulebook: N PmtInf block(s) declare SLEV (charges shared, allocated by local law)` confirmation row across all matching blocks, or one ✗ `ChrgBr mismatch: PmtInf #i declares X but EPC SEPA Rulebook requires SLEV` row per offending block keyed by the 1-based PmtInf index. `ChrgBr` is optional per the schema, so blocks that omit it skip silently — parallel to the PmtMtd check from draft 40. Pairs naturally with draft 40 (PmtMtd) as the second of the two scalar PmtInf-level EPC-rulebook gates. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed pain.001 with `ChrgBr=SLEV` injected → confirmation; same fixture with `ChrgBr=DEBT` → mismatch row citing EPC SEPA's SLEV requirement). Audit OK; full suite expected 282 → 284 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 41 lands **UBL VAT category code validation** (§2.7 polish, EN 16931 / UNCL 5305). PEPPOL BIS Billing 3.0 invoices declare a VAT category letter in every `<cac:TaxCategory>/<cbc:ID>` element; EN 16931 names a separate business-rule family (BR-S, BR-Z, BR-E, BR-AE, BR-K, BR-G, BR-O, BR-L, BR-M) for each allowed letter, and any value outside that set is schema-valid plain text but rail-broken — PEPPOL access points reject the invoice at the category-code check. The new `ublCheckVatCategories()` walks every `<cac:TaxCategory>` reachable from the root (header-side `TaxSubtotal/TaxCategory` and item-level `ClassifiedTaxCategory` alike), reads each `<cbc:ID>`, dedupes case-folded, and surfaces a single ✓ `VAT category codes valid (UNCL 5305 / EN 16931): S` confirmation row across all matching codes via `intlList()` (locale joiner applies — `S und Z` in de, `S et Z` in fr), or one ✗ `Unknown VAT category code XX — not in UNCL 5305 / EN 16931 allowed set (S, Z, E, AE, K, G, O, L, M)` row per offending value. Codes are uppercased on extraction so a `<cbc:ID>s</cbc:ID>` typo doesn't false-trigger the warning. Catches the canonical template-mistake of typing the wrong category letter (e.g. `T` for `Z` zero-rated, or `X` for `E` exempt) — failures the totals-consistency check (draft 28) and VAT-rate check (draft 37) wouldn't see because amounts can still balance against themselves with the wrong category. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine drafts pending native review per the project pattern). Two new Playwright cases (well-formed `S` → confirmation row + UNCL 5305 reference; `XX` → warning row citing the allowed set). Audit OK; full suite expected 280 → 282 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 40 lands **SEPA PmtMtd consistency check** (§2.4 polish). ISO 20022 enumerates `PmtMtd` as one of `CHK / TRF / TRA / DD`; the EPC SEPA Rulebooks pin two of these to specific message types — pain.001 (customer credit-transfer initiation) MUST declare `PmtMtd=TRF`, pain.008 (customer direct-debit initiation) MUST declare `PmtMtd=DD`. A pain.001 carrying `DD` (or vice versa) is schema-valid XML but rail-broken — banks reject it because the payment method contradicts the message schema's purpose. The new `sepaCheckPmtMtd()` walks every `<PmtInf>` block, reads its `<PmtMtd>` element, and surfaces a single ✓ `PmtMtd matches document type: N PmtInf block(s) declare TRF as expected for pain.001` confirmation row when every present declaration matches, or one ✗ `PmtMtd mismatch: PmtInf #i declares X but pain.001 requires TRF (ISO 20022 / EPC SEPA rulebook)` row per offending block. `PmtMtd` is optional per the schema, so blocks that omit it skip silently rather than emitting a noisy warning. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed pain.001 with TRF → confirmation; same fixture with TRF rewritten to DD → mismatch row citing pain.001's TRF requirement). Audit OK; full suite expected 278 → 280 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 39 lands **UBL DueDate vs IssueDate temporal sanity check** (§2.7 polish). PEPPOL BIS Billing 3.0 invoices declare `IssueDate` (EN 16931 BT-2) and `DueDate` (BT-9) at the document root; payment cannot fall due before the invoice is issued, and PEPPOL access points routinely reject the backdated case as a data-entry error. The new `ublCheckDates()` reads both ISO 8601 dates from the root, parses them via `Date.parse()`, and emits a green `✓ DueDate D is on or after IssueDate I` confirmation row when both parse cleanly and `DueDate ≥ IssueDate`, or a red `✗ DueDate D is earlier than IssueDate I — payment cannot fall due before the invoice is issued` row otherwise. Skips silently when either date is missing or unparseable — the existing raw `Issue date` / `Due date` rows above already surface the underlying values. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine drafts pending native review per the project pattern). Two new Playwright cases (well-formed Invoice with IssueDate 2024-04-01 / DueDate 2024-05-01 → confirmation; swapped to DueDate 2024-03-15 → mismatch row citing both dates). Audit OK; full suite expected 276 → 278 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 38 lands **SEPA IBAN ↔ BIC country-code consistency check** (§2.4 polish). ISO 13616 IBANs encode the issuing country in chars 0-1; ISO 9362 BICs encode the same in chars 4-5 (the BIC country sub-code). Within a single SEPA scope — `<DbtrAcct>/<IBAN>` paired with `<DbtrAgt>/<BIC|BICFI>` at PmtInf level, and `<CdtrAcct>/<IBAN>` paired with `<CdtrAgt>/<BIC|BICFI>` at transaction level — the two country codes MUST agree. A German IBAN routed via a French agent is almost always a paste-mistake and is rejected at clearing (EBA STEP2, STET, RT1). The new `sepaCheckIbanBicCountryParity()` walks every PmtInf and every CdtTrfTxInf / DrctDbtTxInf, pairs the sibling Acct + Agt elements, compares the country codes, and surfaces a single ✓ confirmation row across all matching pairs or a ✗ warning row per mismatch with both the IBAN and BIC values quoted. Earlier: draft 37 lands **UBL VAT-rate consistency check** (§2.7 polish, EN 16931 BR-CO-17 and the parallel category-letter rules BR-S-08 / BR-Z-08 / BR-E-08 / BR-AE-08 / BR-K-08 / BR-G-08 / BR-O-08 / BR-IC-08). Every `<cac:TaxSubtotal>` in a PEPPOL BIS Billing 3.0 invoice declares three values that the EN 16931 business rules require to agree: `TaxableAmount`, `Percent` (from the nested `TaxCategory`), and `TaxAmount`. The new `ublCheckVatRates()` walks every subtotal across every `TaxTotal`, computes `Math.round((TaxableAmount × Percent) / 100)` in integer cents (matching EN 16931's "round per subtotal" policy), and compares against the declared `TaxAmount` within ±1¢. On match: one green `✓ TaxSubtotal #N: TaxAmount matches TaxableAmount × X% = Y` confirmation row per subtotal. On mismatch: one red `✗ TaxSubtotal #N VAT-rate mismatch: declared TaxAmount A but TaxableAmount × X% = B` row, citing EN 16931 BR-CO-17. Catches the canonical test-invoice error: typing the wrong monetary amount for a given rate (e.g. 19.50 EUR tax on a 100.00 EUR base at 19%, where the correct value is 19.00). Subtotals missing any of the three fields skip silently — the per-rate breakdown above already surfaces the raw values, and the totals check from draft 28 catches header-level arithmetic. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed 19% subtotal → confirmation + BR-CO-17 reference; 19.50 declared on a 100×19% base → mismatch row citing both values). Audit OK; full suite 274 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 36 lands **UBL Payee/Payer financial-account IBAN MOD-97 cross-check** (§2.7 polish, parallel to the SEPA IBAN cross-check shipped in draft 16). PEPPOL BIS Billing 3.0 / EN 16931 invoices carry the supplier's bank coordinates in `<cac:PaymentMeans>/<cac:PayeeFinancialAccount>/<cbc:ID>` (or `<cac:PayerFinancialAccount>` for self-billing); when the value is shaped like an ISO 13616 IBAN (`CC + 2 digits + 11-30 alnum`, length 15-34) the inspector now runs the same `ibanValidate()` MOD-97 walk that powers the standalone IBAN tool + the SEPA cross-check. On full success: one confirmation row `✓ Payee/Payer financial account IBANs valid (ISO 13616 MOD-97) (N)`; on failure: one warning row per IBAN that fails the check. Values outside the IBAN regex are silently skipped — bank-specific account numbers (BBANs, US ABA routing numbers, etc.) are not the wrong kind of identifier, just a different one. Closes a gap where a maintainer pasting a PEPPOL invoice with a typo'd IBAN would see the inspector parse and render the invoice fine, while a real-world payments processor would reject it at MOD-97 time. 2 new strings × 5 locales (en/fr/de reviewed against ISO 13616 vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (well-formed `DE89370400440532013000` → confirmation row; final-digit flip → warning row). Audit OK; full suite 272 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 35 lands **SEPA `NbOfTxs` + `CtrlSum` group-header invariants** (§2.4 polish, parallel to the UBL totals check from draft 28 and the SEPA BIC / currency cross-checks from drafts 27 / 29). pain.001 and pain.008 files carry two header-level totals in `GrpHdr`: `NbOfTxs` (the declared transaction count) and `CtrlSum` (the declared transaction-amount total). EPC SEPA rulebooks make these hard bank-side reject reasons — a mismatch is exactly the kind of mistake the inspector should surface. The inspector now walks every `<CdtTrfTxInf>` + `<DrctDbtTxInf>` across all `PmtInf` blocks: (a) compares the count against `NbOfTxs` and surfaces a green `✓ NbOfTxs matches transaction count (N)` row or a red `✗ NbOfTxs mismatch: header declares X but file contains Y transactions` row; (b) sums every transaction-level `<InstdAmt>` / `<Amt>` in integer cents (±1¢ tolerance per the ISO 20022 IG rounding policy) and surfaces the parallel CtrlSum confirmation or mismatch row. Both gates are independent and only fire when the respective header element is present (CtrlSum is optional per the schema). 4 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC vocabulary; zh-CN/hi machine drafts). Three new Playwright cases (happy path, NbOfTxs overcounted, CtrlSum undersummed). Audit OK; full suite 270 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 34 lands **LOTL / TSL `NextUpdate` freshness check** (§2.6 polish, ETSI TS 119 612 §5.3.13). The eIDAS LOTL viewer already rendered the raw `NextUpdate/dateTime` from `SchemeInformation` as a key-value row; the inspector now additionally parses that timestamp and adds a colored freshness row beneath it. If the horizon is in the future, a green `✓ Trust list is current — next update due in N days` row surfaces with the ETSI reference; if it has already passed, a red `✗ Trust list is stale — NextUpdate was N days ago; refetch from ec.europa.eu before relying on this list` row surfaces instead. Real-world signal: a maintainer working from a cached LOTL or per-member-state TSL XML needs to know the operator's commitment window has lapsed, because accepting trust decisions from a stale list can mean trusting a CA that has since been withdrawn or downgraded at the EU level. Day delta is rendered via the existing `intlNumber()` so the French / German locales pick up locale thousands separators on month-scale numbers. No row if `NextUpdate` is absent or unparseable (the raw row still shows). 2 new strings (`lotl.warn.stale`, `lotl.ok.current`) + 1 row label (`lotl.row.freshness`) × 5 locales (en/fr/de reviewed against ETSI TS 119 612 vocabulary; zh-CN/hi machine drafts). Two new Playwright cases (NextUpdate 30 days in future → current row; 90 days in past → stale row). Audit OK; full suite 267 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 33 lands **JWT `typ` + `crit` header rows** (RFC 7515 §4.1.9 + §4.1.11 / RFC 8725 §3.11). The inspector now renders two more header-side badges immediately after the existing `kid` row. (a) `Token type (typ)` — informational muted badge. RFC 8725 §3.11 names explicit token typing as the standard defense against cross-purpose confusion (e.g. an OAuth access token issued with `typ: "at+jwt"` per RFC 9068 being accepted as an OIDC `id_token+jwt`). Surfacing `typ` lets a maintainer confirm the declared media type matches the slot the token is being used in. (b) `Critical headers (crit)` — danger red badge. RFC 7515 §4.1.11 specifies that when `crit` is present, the verifier MUST understand and process every header parameter named in the array, or reject the token. The inspector can't know what extensions the maintainer's verifier supports, so it surfaces the names via `intlList()` (locale-aware joiner — `exp und b64` in de, `exp et b64` in fr) and the RFC 7515 reference. 2 new strings × 5 locales (en/fr/de reviewed against RFC 7515/8725 vocabulary; zh-CN/hi machine drafts). One new Playwright case covering both rows with `typ: "at+jwt"` (RFC 9068 access-token type) and `crit: ["exp", "b64"]`. Audit OK; full suite 265 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 32 lands **JWT `jti` + `kid` display rows** (RFC 7519 §4.1.7 / RFC 7515 §4.1.4). The inspector now renders two more informational badges: `Key ID (kid)` after the alg=none warning slot (sourced from the header — JWKS selector, lets a maintainer cross-reference the active signing key without eyeballing the raw header JSON) and `JWT ID (jti)` after the `sub` row (sourced from the payload — replay-defense identifier, useful for correlating a token with issuer-side audit logs). Both honor string or numeric values per RFC 7519's leave-it-to-the-application typing rule. 2 new strings × 5 locales (en/fr/de reviewed against RFC 7515/7519 vocabulary; zh-CN/hi machine drafts). One new Playwright case covering both rows. Audit OK; full suite 264 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 31 lands **UBL ISO 4217 currency-code cross-check** (§2.7 polish, parallel to the SEPA cross-check from draft 29). The PEPPOL UBL inspector now walks every element carrying a `currencyID="..."` attribute (the UBL counterpart of ISO 20022's `Ccy="..."` — present on every `*Amount` element: `LineExtensionAmount`, `TaxAmount`, `PriceAmount`, `TaxInclusiveAmount`, `PayableAmount`, …) and verifies each code against the inline `ISO_4217_ACTIVE` set already used by the SEPA inspector. Confirmation row uses `intlList()` so the locale joiner applies (`EUR und USD` in de, `EUR et USD` in fr); warning row per offending code. Catches typos (`ERU` for `EUR`) that the totals-consistency check wouldn't see, since amounts can still balance against themselves with the wrong code. 2 new strings × 5 locales (en/fr/de reviewed; zh-CN/hi machine drafts). Two new Playwright cases. Audit OK; full suite 263 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 30 lands two JWT-inspector polishes. (a) **`alg: "none"` warning row** (RFC 8725 §3.1 / JWT BCP) — any token whose header declares the unsigned `none` algorithm now surfaces an unconditional red badge immediately after the header/payload/signature blocks. Case-insensitive (`None` / `NONE` show up in the wild). This is the textbook algorithm-confusion footgun; the inspector should never let a maintainer paste a `none`-alg token without seeing the warning. (b) **`sub` (subject) claim row** (RFC 7519 §4.1.2) — the subject identifier the JWT carries claims about now renders as its own informational badge after the existing `iat` row, so a maintainer reading a token doesn't have to eyeball the raw JSON to find the user it identifies. Strings (2 new × 5 locales — en/fr/de reviewed against RFC 8725 + 7519 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (alg=none surfaces RFC 8725 mention; RFC 7519 §A.1 fixture's sub=1234567890 surfaces in the new row). Audit OK; full suite 261 passed. No new vendored deps, no CSP change, tool count unchanged. Earlier: draft 29 lands **SEPA ISO 4217 currency-code cross-check**. Every `Ccy="..."` attribute (on `<Amt>` / `<InstdAmt>` / `<NtryAmt>` / `<Bal>/<Amt>` / `<EqvtAmt>` …) is collected and validated against an inline ISO 4217:2015 (Amendment 174) active-currencies set. On full success: one confirmation row listing each unique code via `intlList()` (so the locale's "and" / "und" / "et" joiner applies); on failure: one warning row per typo / historical code. Catches the classic `ERU` for `EUR` finger-flip and legacy codes (`ITL`, `DEM`, `ROL`) that should have been migrated long ago. Earlier: draft 28 lands **UBL totals consistency check** (§2.7 polish, EN 16931 invariants). The PEPPOL UBL inspector now verifies two invariants and surfaces ✓ / ✗ rows after the existing parse: (1) `Σ InvoiceLine/LineExtensionAmount === LegalMonetaryTotal/LineExtensionAmount`, (2) `LegalMonetaryTotal/TaxExclusiveAmount + Σ TaxTotal/TaxAmount === LegalMonetaryTotal/TaxInclusiveAmount`. Compared in integer cents with ±1¢ tolerance per the EN 16931 published rounding policy. Earlier: draft 27 lands **SEPA BIC format cross-check** (parallel to the existing IBAN MOD-97 walk). Every `<BIC>` and `<BICFI>` element in the document is run through the same `bicValidate()` that powers the standalone BIC tool — on full success a single `BIC format valid (ISO 9362) (N)` confirmation row; on failure one warning row per bad BIC. Catches typos / pasted-in-wrong-column BICs that the IBAN check alone wouldn't surface. Earlier: draft 26 lands **main-JWT `iss` / `aud` verifier cross-checks** (RFC 7519 §4.1.1 + §4.1.3). The collapsible verifier-checks panel under the JWT input is restructured into two labeled groups ("JWT (RFC 7519)" and "SD-JWT KB-JWT"); two new inputs (`#jwt-iss`, `#jwt-aud`) feed independent ✓ / ✗ rows after the time-claim badges. RFC 7519 array-form `aud` is honored (match if the expected value is a member). Existing KB-JWT inputs continue to live in the same `<details>` element so the panel-open helper in tests keeps working. Earlier: draft 25 lands two cross-cutting polishes. (a) **JWT time-claim row coverage** — the inspector now renders independent badges for `exp`, `nbf` (RFC 7519 §4.1.5; red when the token is not yet valid), and `iat` (informational, neutral colour). Each gate is reported separately so a token that is `exp`-valid but `nbf`-future is flagged on the right line. (b) **Phase 1 §1.5 #4 close-out** — the audit script now enforces the plural-key parity invariant: any group whose keys end in a CLDR rule suffix (`.zero|.one|.two|.few|.many|.other`) MUST define `.other` in en (the universal Intl.PluralRules fallback). The convention is documented inline in `scripts/export-strings-csv.js`. Earlier: draft 24 lands SD-JWT KB-JWT verifier-side `aud` / `nonce` cross-checks. The JWT inspector now exposes a collapsible "verifier checks" panel with two optional inputs (expected audience, expected nonce); when supplied they are compared against the KB-JWT payload's `aud` (string or array per RFC 7519) and `nonce` claims, with explicit ✓ / ✗ rows surfacing the received value on mismatch and a "missing claim" row when the KB-JWT omits the field. Closes the §2.8 draft 23 deferral. Earlier: draft 23 lands SD-JWT KB-JWT signature verification + `sd_hash` binding check using the `cnf.jwk` declared in the issuer-signed payload (ES256 / ES384 / EdDSA / RS256 via Web Crypto). Earlier: draft 22 polished §2.4 SEPA (per-PmtInf `ReqdExctnDt` requested execution date + per-transaction `MndtId` mandate ID for pain.008 direct debits) and §2.7 UBL (one-click "Copy as CSV" for the line items table). Earlier: draft 21 landed the SD-JWT cleartext-payload reconstruction view (the §7 verifier-side disclosure-resolution algorithm) and consolidated the two `bytesToBase64()` definitions that were colliding at hoist-time. Status: draft 20 (Phase 1 substantially complete; Phase 7 §2.4 SEPA ISO 20022 inspector + §2.6 eIDAS LOTL / TSL viewer + §2.7 first slice (PEPPOL UBL Invoice / CreditNote inspector) landed, tool count 42 → 45; draft 19 polished §2.4 with explicit schema-version detection (banner now reads `pain.001.001.03 (customer credit-transfer initiation)` instead of just `pain.001`) and surfaced the per-transaction `<InstrId>` field alongside `<EndToEndId>`; draft 20 lands the §2.8 SD-JWT slice — the existing JWT inspector now detects the `~`-delimited Selective Disclosure JWT format (draft-ietf-oauth-selective-disclosure-jwt), decodes each disclosure to its `[salt, name, value]` triple (or `[salt, value]` for array elements), computes the SHA-256 digest over the wire-bytes and cross-checks it against every `_sd` array reachable from the payload (top-level or nested), and surfaces an optional trailing key-binding JWT. Decode-only; no signature verification (the inner JWT verify path is unchanged). Tool count unchanged. §1.4 logical-property CSS refactor + §1.5 full Intl coverage + §1.6 command palette landed. Every physical-direction CSS property in `index.html` is logical; `Intl.NumberFormat` + `Intl.RelativeTimeFormat` + `Intl.PluralRules` + `Intl.ListFormat` are all wired (PBKDF2 timing, CIDR / PGP key dates / relative timestamp / regex plurals / BIC multi-tag prose). The ⌘K / Ctrl+K command palette ships with 42 English synonym blocks + localized `palette.{title,placeholder,empty}` strings across all five locales; locale-specific synonyms deferred until §1.3 native reviewers are sourced — polyglot fallback ensures English synonyms always match. See §1.4, §1.5, §1.6 progress notes; Phase 4 complete except BLAKE3; Phase 5 distribution scripts + PWA precache service worker shipped; Phase 6 regulator presets shipped in PGP key generator + PBKDF2 tool + annual freshness audit covering both blocks; SECURITY.md + THREAT-MODEL.md published; GDPR-by-design /privacy.html published; regional hreflang aliases)
Audience: maintainers and contributors
Scope: how to extend encryptalotta.com so it serves an international audience — with a deliberate emphasis on Europe and other regions that lean heavily on open-source — without compromising the site's existing character (single-page, fully client-side, no analytics, no CDN, no tracking, no build step beyond a couple of vendored scripts).

This document is a menu, not a commitment. Workstreams are independent; each section is written so it can be picked up on its own.

---

## 0. Guiding principles

These constraints come first; everything below must respect them.

1. **Client-side only.** Nothing in this spec introduces a server, a build pipeline that requires a server, or any runtime network call. Every new tool runs in the browser. Anything that cannot run in the browser is out of scope, full stop.
2. **No third-party requests.** No CDN, no fonts, no analytics, no telemetry. Vendored dependencies only. New deps must be small, MIT/BSD/Apache-2-licensed, and copy-pasteable as a single file.
3. **No fingerprinting.** Locale comes from `navigator.language` and `localStorage` only. Country is never inferred (no IP geolocation, no `Intl.DateTimeFormat().resolvedOptions().timeZone`-based country guessing). The same site is served to everyone; users opt in to a locale.
4. **No PII validators.** See [§7 Tensions](#7-tensions-and-non-goals). Commercial identifiers (IBAN, VAT, BIC, GS1) are fine. National identity numbers are not.
5. **No compliance theater.** No cookie-banner builders, no DPA templates, no "GDPR auditor" tools. The site is a technical toolbox, not a legal product.
6. **Translation quality > translation breadth.** A new locale ships only after a native reviewer has signed off on the strings that appear on screen. Machine-quality drafts are fine as scaffolding; they are not fine as a shipped locale unless explicitly labeled "machine-translated, awaiting review" in the UI.
7. **Reversibility.** Every workstream below should be revertable by deleting files. No global rewrites, no architectural commitments that lock the project in.

---

## 1. Internationalization (i18n) workstream

### 1.1 Current state (as of this writing)

- Five locales shipped: `en` (source of truth), `fr`, `zh-CN`, `de`, `hi`.
- Strings live inline in `index.html` as a single `STRINGS` object, round-tripped through `i18n/strings.csv` via `scripts/export-strings-csv.js` and `scripts/import-strings-csv.js`.
- Pre-rendered SEO variants generated by `scripts/build-i18n-variants.js` at `/fr/`, `/zh/`, `/de/`, `/hi/`.
- Auto-detection from `navigator.language` on first visit; user choice persisted in `localStorage`.
- `<link rel="alternate" hreflang>` declared for all five locales plus `x-default`.

### 1.2 Proposed locale roadmap

Locales are organized into tiers by impact-per-effort. **No locale ships until its translations have been reviewed by a native speaker who is also technically literate** (the crypto vocabulary matters).

#### Tier 1 — high impact, high familiarity (work starts here)

| Code   | Language          | Script | Direction | Plural forms | Notes |
|--------|-------------------|--------|-----------|--------------|-------|
| `es`   | Spanish (neutral) | Latin  | LTR       | 2            | Aim for a neutral/international register (avoid Iberian-only or LatAm-only idioms). Defer regional splits (`es-ES`, `es-MX`, `es-AR`) until there is clear demand. |
| `pt-BR`| Portuguese (Brazil) | Latin | LTR     | 2            | Distinct enough from `pt-PT` in tech vocabulary that one neutral `pt` is *not* a good idea. Ship `pt-BR` first (larger tech community). |
| `pt-PT`| Portuguese (Portugal) | Latin | LTR   | 2            | Add after `pt-BR`; diff against `pt-BR` is small but matters (e.g., "ficheiro" vs "arquivo"). |
| `it`   | Italian           | Latin  | LTR       | 2            | |
| `pl`   | Polish            | Latin  | LTR       | 3 (one/few/many) | Complex pluralization — use `Intl.PluralRules`. |
| `uk`   | Ukrainian         | Cyrillic | LTR     | 3            | Treat separately from `ru`. Many speakers actively avoid Russian-language tools. |
| `ar`   | Arabic (MSA)      | Arabic | **RTL**   | 6 (zero/one/two/few/many/other) | Use Modern Standard Arabic. Unlocks the RTL infrastructure for `he`, `fa`, `ur`. |

#### Tier 2 — high impact, after Tier 1 has shaken out the RTL and plural infrastructure

| Code   | Language     | Script | Direction | Plural forms | Notes |
|--------|--------------|--------|-----------|--------------|-------|
| `ru`   | Russian      | Cyrillic | LTR     | 3            | |
| `tr`   | Turkish      | Latin  | LTR       | 2            | Privacy-conscious audience. |
| `nl`   | Dutch        | Latin  | LTR       | 2            | |
| `ja`   | Japanese     | Mixed (Kanji/Kana) | LTR | 1     | No pluralization. Watch line-break behavior — set `word-break: keep-all` on CJK content. |
| `ko`   | Korean       | Hangul | LTR       | 1            | |
| `id`   | Indonesian   | Latin  | LTR       | 1            | No pluralization in the European sense. |
| `vi`   | Vietnamese   | Latin  | LTR       | 1            | Diacritics-heavy — confirm font coverage. |

#### Tier 3 — smaller audiences but high alignment with project's mission

| Code   | Language     | Script | Direction | Notes |
|--------|--------------|--------|-----------|-------|
| `sv`   | Swedish      | Latin  | LTR       | |
| `cs`   | Czech        | Latin  | LTR       | 3 plural forms |
| `he`   | Hebrew       | Hebrew | **RTL**   | Trivial once `ar` lands. |
| `fa`   | Persian/Farsi| Arabic | **RTL**   | Trivial once `ar` lands. |
| `ur`   | Urdu         | Arabic | **RTL**   | Trivial once `ar` lands; expands Hindi audience. |
| `bn`   | Bengali      | Bengali| LTR       | |
| `ta`   | Tamil        | Tamil  | LTR       | |
| `sw`   | Swahili      | Latin  | LTR       | |

#### Tier 4 — defer

Regional English variants (`en-GB`, `en-IN`) are useful for `hreflang` mapping but do not need separate string files unless date/number conventions diverge meaningfully. Use `hreflang` aliasing to point them at `en`.

`zh-TW` (Traditional Chinese) is a real audience but the diff against `zh-CN` is non-trivial (vocabulary, not just characters). Defer until a Traditional reviewer is available.

### 1.3 Translation methodology — "multiple passes"

The bar this project should hold: **a native speaker who works in tech can read the UI without wincing.**

Procedure for each new locale:

1. **Pass 1 — machine draft.** Run an LLM (or DeepL for European locales, which is genuinely strong on EU languages) against the `en` column of `strings.csv`. Mark every cell `machine` in the per-locale `_status` column.
2. **Pass 2 — glossary lock.** Before reviewing prose strings, lock the translations of the core technical glossary (see [§1.7](#17-core-glossary-translation-targets)) for that locale. Every occurrence of "encrypt", "decrypt", "key", "passphrase", etc. must use the same target term across the file. The CSV makes this a global find-and-replace; do it in one pass.
3. **Pass 3 — native review.** A native speaker who reads code goes through the CSV row by row. They flip `_status` from `machine` to `reviewed` once they're satisfied. They are explicitly allowed to mark cells `awkward` or `needs-context` and leave them for the maintainer to revisit.
4. **Pass 4 — in-context review.** Build the variant (`scripts/build-i18n-variants.js`), open `/<locale>/index.html`, click through every tool. Strings that read fine in a spreadsheet sometimes read wrong in a button. Note overflows, truncations, line-wrap problems, mismatched case (English title case doesn't transfer to most languages).
5. **Pass 5 — RTL pass (RTL locales only).** Same as Pass 4 but in the RTL build, with eyes on icon directionality (arrows, chevrons, progress indicators), tabular alignment, and any place CSS uses `left`/`right` instead of `inline-start`/`inline-end`.
6. **Pass 6 — date/number/plural smoke test.** Generate a UUID v7, decrypt a file, decode a cron expression. Confirm dates render in the locale's conventional order, decimal separators are correct, and pluralized counts agree.
7. **Sign-off.** All cells `reviewed` (or explicitly `n/a` for strings that don't apply, e.g., English-only acronyms). Add the locale to `VARIANTS` in `build-i18n-variants.js`, to the `hreflang` block in `index.html`, and to the language picker.

Anything short of this ships with a visible banner: *"This locale is machine-translated and awaiting native review — corrections welcome at [link]."*

### 1.4 RTL support (one-time architectural change) — **🟡 CSS logical-property refactor shipped (draft 13)**

**Status notes (draft 13):** Step 1 of the §1.4 plan is done. Every physical-direction CSS property in `index.html` was replaced with its logical equivalent:

- `padding-left` → `padding-inline-start` (1 site: `.home-section h3`).
- `margin-left` → `margin-inline-start` (1 stylesheet site: `.warning-box ul`; 9 inline `style="margin-left: 8px;"` button-spacers across the regex/diff/CSV/format/encode/timestamp/URL/TOTP/PEM swap buttons).
- `right: 22px` → `inset-inline-end: 22px` (1 site: `.tool-card::after` arrow indicator).
- `border-left` → `border-inline-start` (4 sites: `.alert-{warning,danger,success,info}`).
- `text-align: left` → `text-align: start` (2 sites: `.tool-card`, `.key-info-table th/td`).

A grep for `(margin|padding|border)-(left|right)|text-align:\s*(left|right)` over `index.html` now returns zero hits. The one remaining JS-set property (`wrapper.style.textAlign = 'center'` in the QR multi-share renderer) is direction-neutral and intentional.

One new Playwright case (`rtl: home tool cards mirror under dir=rtl`) flips `document.documentElement.dir = 'rtl'` at runtime, then asserts `getComputedStyle('.tool-card', '::after').insetInlineEnd === '22px'` to prove the logical property resolves correctly in RTL mode. Cleaner than a full visual diff, and the harness has no headless RTL fonts to fight with. Full suite: 215 passed.

What remains for §1.4 before an RTL locale can ship:

- Step 2 — runtime `<html dir="rtl">` flip in the `setLang()` path when an RTL locale is the active selection (one-line conditional; deferred until the first RTL locale is added to `SUPPORTED_LANGS`).
- Step 3 — icon audit: chevrons / arrows / progress indicators get `transform: scaleX(-1)` under `[dir="rtl"]` where they are direction-bearing. (The single `→` indicator on `.tool-card::after` will need this when the first RTL locale ships.)
- Step 4 — wrap `<pre>` / `<code>` / `<output>` code blocks with `direction: ltr; unicode-bidi: embed` so PGP/Base64/hex output stays LTR even inside an RTL page.
- Step 5 — `dir="ltr"` on code-like form fields (regex patterns, cron expressions, IBANs, hex).
- Step 6 — `word-break: keep-all` under `:lang(zh), :lang(ja), :lang(ko)` for CJK line-wrap behavior.

Steps 2–6 are deferred to the slice that ships the first RTL locale (Arabic, per the §1.2 roadmap), since none of them are observable until then.

Currently the CSS uses `left`/`right`/`margin-left`/`padding-right`/`text-align: left` in places. RTL support is a one-time refactor that then makes every future RTL locale free.

Concrete changes:

1. Replace physical properties with logical properties throughout the stylesheet:
   - `left` → `inset-inline-start`
   - `right` → `inset-inline-end`
   - `margin-left` → `margin-inline-start`
   - `padding-right` → `padding-inline-end`
   - `text-align: left` → `text-align: start`
   - `border-left` → `border-inline-start`
   - etc.
2. Set `<html dir="rtl">` for RTL locales, both at runtime (when the user switches) and in `build-i18n-variants.js` (so crawlers see it).
3. Audit icons: arrows ("back", "next", chevrons in collapsible sections, copy-to-clipboard) need a CSS `transform: scaleX(-1)` under `[dir="rtl"]` *unless* they are direction-agnostic (search, settings, lock).
4. Code and crypto output (Base64, hex, PGP blocks, JSON) stays LTR even inside RTL pages. Wrap with `<bdo dir="ltr">` or apply `direction: ltr; unicode-bidi: embed` to `<pre>` and `<code>` and `<output>` containers.
5. Form fields that accept code-like input (regex patterns, cron expressions, IBANs, hex) get `dir="ltr"` even in RTL contexts.
6. Verify line wrapping in CJK locales separately: set `word-break: keep-all` on CJK content (`:lang(zh), :lang(ja), :lang(ko)`) to prevent breaks mid-word.

Acceptance: the existing site rendered in `dir="rtl"` looks intentional, not mirrored-by-accident. Tested with at least Arabic and Hebrew before either locale ships.

### 1.5 Locale-aware formatting via `Intl` — **🟡 three slices shipped (drafts 11, 12, 14)**

**Status notes (draft 14):** First `Intl.ListFormat` call-site. The BIC validator's `tags` row (which surfaces "test BIC", "passive participant", "primary branch" as per-locale prose) now renders via `intlList(r.tags)` instead of `r.tags.join(', ')`. For a multi-tag BIC like `AAAADEB0XXX` (location ends in 0 → test; branch is XXX → primary) the German output becomes `Test-BIC (Ort endet auf 0) und Hauptniederlassung (XXX)` instead of `Test-BIC (Ort endet auf 0), Hauptniederlassung (XXX)`. One new Playwright case asserts the `und` connector in the de variant.

The other `.join(', ')` sites in `index.html` were considered and intentionally left alone: TLS SAN lists, CIDR / cron internal fields, and the X.509 DN parts are technical lists where a "and" / "und" final-joiner would feel wrong (they are tokens, not prose). The passphrase-strength dialog joins issue phrases that are still hardcoded English; wiring `intlList` there is a follow-up bundled with translating the issue strings themselves.

Full Playwright suite: 216 passed.

---

**Status notes (drafts 11, 12 — superseded heading kept here for diffability):**

**Status notes (draft 12):** Plural + list helpers landed.

- `intlPlural(baseKey, n, vars?)` — picks `<baseKey>.<rule>` via `Intl.PluralRules` against the active document lang, falling back through `<baseKey>.other` and then the bare `baseKey`. `n` is auto-injected as `{n}` already formatted via `intlNumber()`, so callers don't pass it twice. Engine-side fallback (`n === 1 ? 'one' : 'other'`) keeps the helper safe if Intl.PluralRules rejects the tag.
- `intlList(items, opts?)` — wraps `Intl.ListFormat` with `{type: 'conjunction', style: 'long'}` defaults. No call-sites converted in this slice — landed as scaffolding for future Shamir / multi-recipient phrasing. Falls back to a plain `, `-join on older engines.

First plural call-site: the regex tool's match-count status row. The legacy `regex.match.count: '{n} match(es)'` key (with its awkward parenthesized form) was removed across all five locales and replaced with `regex.match.count.one` / `regex.match.count.other` pairs:

- en: `1 match` / `{n} matches`
- fr: `1 correspondance` / `{n} correspondances`
- de: `{n} Treffer` (invariant — German uses the same form for both)
- zh-CN: `{n} 个匹配` (no plural distinction)
- hi: `{n} मिलान` (no plural distinction)

The call-site now reads `intlPlural('regex.match.count', matches.length)` — one argument, no manual `{n}` injection, no parenthesized fallback. Strings table grew from 1097 → 1098 keys × 5 locales (one net addition because the old single key was removed and two replacements added).

Two new Playwright cases cover the slice:

- English regex: `cat` → `1 match`; `cat cat cat` → `3 matches`.
- French regex: `chat` → `1 correspondance`; `chat chat chat` → `3 correspondances`.

Full Playwright suite: 214 passed.

---

**Status notes (draft 11):** Two small helpers landed alongside `t()` / `tFormat()`:

**Status notes (draft 11):** Two small helpers landed alongside `t()` / `tFormat()`:

- `intlNumber(n, opts?)` — wraps `Intl.NumberFormat` against `document.documentElement.lang`. Falls back to `String(n)` if Intl throws on an unknown tag.
- `intlRelativeFromNow(date)` — cascades through second/minute/hour/day/month/year buckets, then renders via `Intl.RelativeTimeFormat` with `{numeric: 'auto'}` so locales that have "yesterday"/"hier"/"gestern" surface them. Falls back to the legacy `timestamp.rel.{past,future}` template strings when `Intl.RelativeTimeFormat` is unavailable.

Four call-sites converted in this slice:

1. PBKDF2 derivation time (`elapsed.toFixed(0) + ' ms'` → `intlNumber(...) + ' ms'`) — French/German thousands separators now apply once derivation crosses 1 000 ms.
2. CIDR `Total addresses` / `Usable hosts` (`r.total.toLocaleString()` → `intlNumber(r.total)`) — the prior `toLocaleString()` used the browser locale; the new path uses the active document lang, so a runtime language switch immediately re-formats.
3. PGP key info `Created` / `Expires` rows now pass the explicit document lang to `Date.prototype.toLocaleString(intlLang())`.
4. Unix-timestamp `tsRelative()` now delegates to `intlRelativeFromNow()`, so a French user sees `il y a 5 minutes` and a German user sees `vor 5 Minuten` without per-locale template strings.

The legacy `timestamp.rel.{past,future}` + `timestamp.unit.*` strings are kept as the fallback path; they are no longer the primary render but still cover Safari versions that pre-date `Intl.RelativeTimeFormat` (2018). Removing them is a follow-up once the fallback is no longer needed.

Three new Playwright cases cover the slice (`tests/specs/01-tools.spec.js`):

- PBKDF2 in French — derivation completes, document lang flips to `fr`.
- CIDR `10.0.0.0/8` in German — total renders as `16.777.216`.
- Timestamp tool in French — relative phrasing matches `/il y a\s+5\s+minutes/i`.

Full Playwright suite: 212 passed.

Remaining §1.5 work (deferred to a future slice): `Intl.PluralRules` integration for `{n} shares` / `{n} matches`-style strings (needs the CSV schema extension below), `Intl.ListFormat` for Shamir / multi-recipient phrasing, and `Intl.Collator` for any palette sort that lands with §1.6.

The browser's `Intl` namespace handles this without dependencies. Touch points:

- **`Intl.NumberFormat`** — Decimal commas in French/German/Italian/Spanish/Polish/etc. Affects: PBKDF2 derivation time display, file sizes, "5 minutes ago" thresholds, color converter percentages, CIDR host counts, hash collision probabilities.
- **`Intl.DateTimeFormat`** — Unix timestamp converter (already shows UTC + local + relative; add locale-formatted absolute). Cron decoder next-fire times. TLS / PGP key validity windows.
- **`Intl.RelativeTimeFormat`** — Replace the hand-rolled "5 minutes ago" with `RelativeTimeFormat`. Handles Polish/Russian/Arabic plurals correctly.
- **`Intl.PluralRules`** — Strings like `"{n} shares"`, `"{n} matches"`, `"{n} bytes"`. Use a small helper:

  ```js
  function plural(locale, n, forms) {
    const rule = new Intl.PluralRules(locale).select(n);
    return (forms[rule] || forms.other).replace('{n}', n);
  }
  // forms = { one: '{n} share', few: '{n} shares', many: '{n} shares', other: '{n} shares' }
  ```

  Strings file gets one entry per plural category, not one entry per language hard-coded. The CSV schema needs a small extension: a `key.plural.one`, `key.plural.few`, `key.plural.many`, `key.plural.other` convention. Backwards compatible — existing entries are treated as `other`.

- **`Intl.ListFormat`** — "share A, share B, and share C" / "share A, share B y share C". Surfaces in Shamir, QR multi-part output, multi-recipient encryption.
- **`Intl.Collator`** — If the command palette ever sorts strings alphabetically per locale (German ß, Swedish å, Czech č all sort differently), use `Collator`. Not urgent.

### 1.6 Command-palette synonyms — **🟡 palette + en synonyms shipped (draft 15); locale-specific synonyms deferred**

**Status notes (draft 15):** The command palette landed end-to-end:

- **UI** — a modal dialog (`#palette` / `#palette-overlay`) with overlay, accessible title (`role="dialog"`, `aria-modal="true"`, `aria-labelledby`), search input, and a `role="listbox"` results list. Hidden by default; CSS uses logical properties (`inset-inline`, `margin-inline: auto`) so RTL just works once an RTL locale ships. ~14 lines of CSS, ~13 lines of HTML.
- **Trigger** — `⌘K` on macOS, `Ctrl+K` elsewhere. The document-level `keydown` listener skips when `e.isComposing` so multibyte IME composition (Chinese / Japanese / Korean) isn't stolen.
- **Filter** — for each entry in `TOOL_ORDER`, the haystack is `tool-id + localized-label + English-synonyms`, all run through `toLocaleLowerCase(lang)` for locale-correct case folding (Turkish dotted i, German ß, etc.). Multiple whitespace-separated query terms are AND'd against the haystack.
- **Keyboard** — `ArrowDown` / `ArrowUp` cycle the active result; `Enter` navigates via `location.hash`; `Esc` closes; click on the overlay closes; mouse hover updates the highlight via CSS.
- **Synonyms** — `TOOL_SYNONYMS` is a flat JS object keyed by tool id, 42 entries, English only. Whitespace-separated tokens so the source reads like search keywords, not prose. The spec is emphatic that locale-specific synonyms (`chiffrer` / `cifrar` / `verschlüsseln` / `암호화` / etc.) are the single most error-prone localization gap and should ship en-only first — and that polyglot users typing English should always work in every locale. Both invariants are preserved.
- **Strings** — three new UI strings (`palette.title`, `palette.placeholder`, `palette.empty`) added to all five locales (en/fr/de/zh-CN/hi). Locked against the §1.7 glossary where the terms apply (German `Befehlspalette` / `Werkzeuge`; French `palette de commandes` / `outils`). STRINGS now 1101 keys × 5 locales (up from 1098).

Seven new Playwright cases cover the slice:

1. `Ctrl+K` opens; `Esc` closes; input is focused.
2. Synonym `"swift"` finds the BIC tool; `Enter` navigates.
3. Synonym `"epoch"` finds the timestamp tool.
4. Multi-term `"ed signature"` finds Ed25519.
5. `ArrowDown` then `Enter` selects the second match.
6. `"zzzzznotarealtool"` shows the empty state.
7. French locale + English query (`"swift"`) still matches via the en-synonym fallback (proves the polyglot invariant).

Full Playwright suite: 223 passed.

**Deferred to a follow-up:**

- **Per-locale synonyms.** The spec lists illustrative French / German / Spanish / Polish / Arabic / Japanese / Russian / Portuguese examples per tool. Implementing these is gated on §1.3 native review per locale — the failure mode (`crypter` as a French synonym for `encrypt` when `chiffrer` is the actual term used by French cryptographers; `cifrar` vs. `encriptar` for Spanish) is exactly the textbook MT failure the spec warns against. Will be added incrementally as native reviewers come online per locale.
- **Synonym schema in the CSV.** Locale-specific synonyms, when they arrive, will plug into the existing STRINGS table with a `nav.tool.<id>.synonyms` key. The audit script's en-only carve-out (currently scoped to `tool.<id>.{title,metaDescription,about.{what,how,why}}`) will be extended to allow synonyms to drop to en-only when a locale hasn't been reviewed yet — same pattern as the existing SEO prose carve-out.

The `⌘K` command palette currently matches against English tool names. A French user typing "chiffrer" finds nothing. Fix:

1. Extend the `STRINGS` schema with a per-tool `synonyms` field: a comma-separated list of terms in the current locale that should match that tool.
2. Localize the synonyms list. **This is the single highest-friction localization gap on most tools.** It is also the one most prone to machine-translation failure (machine translators don't know that `chiffrer` is "encrypt" in a crypto context — they often produce `crypter` which is a false friend).
3. Search matches against `label + synonyms + key`, all lowercased, all locale-folded (`String.prototype.toLocaleLowerCase(locale)`).
4. Always keep the English keys in the synonyms set even for non-English locales, so polyglot users can still find tools by typing English.

Concrete examples per locale (illustrative, not exhaustive — native reviewers will expand these):

- French: `encrypt` → also match `chiffrer`, `crypter` (incorrect but common), `coder`
- German: `encrypt` → also match `verschlüsseln`, `chiffrieren`
- Spanish: `encrypt` → also match `cifrar`, `encriptar`
- Polish: `encrypt` → also match `zaszyfrować`, `szyfrowanie`
- Arabic: `encrypt` → also match `تشفير`, `شفّر`
- Japanese: `encrypt` → also match `暗号化`, `あんごうか`
- Russian: `encrypt` → also match `зашифровать`, `шифрование`
- Portuguese: `encrypt` → also match `cifrar`, `criptografar`

### 1.7 Core glossary translation targets

This is the lock-down vocabulary for Pass 2. Each row is a term that appears repeatedly across the UI. Translations marked **(✓)** I am confident in; **(?)** I am only moderately confident in and a native reviewer should be the final arbiter; **(—)** I'm explicitly leaving for native review. Confidence ratings are based on standard cryptographic / software terminology in each language as I know it, not personal preference.

| English | Spanish (es) | Italian (it) | Portuguese (pt-BR) | Polish (pl) | Russian (ru) | Ukrainian (uk) | Turkish (tr) | Dutch (nl) | Arabic (ar) | Japanese (ja) | Korean (ko) | Indonesian (id) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Encrypt | Cifrar (✓) | Cifrare (✓) | Criptografar (✓) | Zaszyfrować (✓) | Зашифровать (✓) | Зашифрувати (✓) | Şifrele (✓) | Versleutelen (✓) | تشفير (✓) | 暗号化 (✓) | 암호화 (✓) | Enkripsi (✓) |
| Decrypt | Descifrar (✓) | Decifrare (✓) | Descriptografar (✓) | Odszyfrować (✓) | Расшифровать (✓) | Розшифрувати (✓) | Şifre çöz (✓) | Ontsleutelen (✓) | فك التشفير (✓) | 復号 (✓) | 복호화 (✓) | Dekripsi (✓) |
| Sign | Firmar (✓) | Firmare (✓) | Assinar (✓) | Podpisać (✓) | Подписать (✓) | Підписати (✓) | İmzala (✓) | Ondertekenen (✓) | توقيع (✓) | 署名 (✓) | 서명 (✓) | Tandatangani (✓) |
| Verify | Verificar (✓) | Verificare (✓) | Verificar (✓) | Zweryfikować (✓) | Проверить (✓) | Перевірити (✓) | Doğrula (✓) | Verifiëren (✓) | تحقّق (✓) | 検証 (✓) | 검증 (✓) | Verifikasi (✓) |
| Key | Clave (✓) | Chiave (✓) | Chave (✓) | Klucz (✓) | Ключ (✓) | Ключ (✓) | Anahtar (✓) | Sleutel (✓) | مفتاح (✓) | 鍵 (✓) | 키 (✓) | Kunci (✓) |
| Public key | Clave pública (✓) | Chiave pubblica (✓) | Chave pública (✓) | Klucz publiczny (✓) | Открытый ключ (✓) | Відкритий ключ (✓) | Genel anahtar (✓) | Publieke sleutel (✓) | المفتاح العام (✓) | 公開鍵 (✓) | 공개 키 (✓) | Kunci publik (✓) |
| Private key | Clave privada (✓) | Chiave privata (✓) | Chave privada (✓) | Klucz prywatny (✓) | Закрытый ключ (✓) | Закритий ключ (✓) | Özel anahtar (✓) | Privésleutel (✓) | المفتاح الخاص (✓) | 秘密鍵 (✓) | 비밀 키 (✓) | Kunci privat (✓) |
| Passphrase | Frase de contraseña (?) | Frase di accesso (?) | Frase secreta (?) | Hasło / fraza (?) | Парольная фраза (✓) | Парольна фраза (✓) | Parola (?) | Wachtwoordzin (?) | عبارة المرور (✓) | パスフレーズ (✓) | 패스프레이즈 (✓) | Frasa sandi (?) |
| Password | Contraseña (✓) | Password (✓) [loanword commonly used] | Senha (✓) | Hasło (✓) | Пароль (✓) | Пароль (✓) | Parola (✓) | Wachtwoord (✓) | كلمة المرور (✓) | パスワード (✓) | 비밀번호 (✓) | Kata sandi (✓) |
| Generate | Generar (✓) | Genera (✓) | Gerar (✓) | Wygeneruj (✓) | Сгенерировать (✓) | Згенерувати (✓) | Oluştur (✓) | Genereren (✓) | إنشاء (✓) | 生成 (✓) | 생성 (✓) | Buat (✓) |
| File | Archivo (✓) | File (✓) | Arquivo (✓) [pt-PT: Ficheiro] | Plik (✓) | Файл (✓) | Файл (✓) | Dosya (✓) | Bestand (✓) | ملف (✓) | ファイル (✓) | 파일 (✓) | Berkas (✓) |
| Text | Texto (✓) | Testo (✓) | Texto (✓) | Tekst (✓) | Текст (✓) | Текст (✓) | Metin (✓) | Tekst (✓) | نص (✓) | テキスト (✓) | 텍스트 (✓) | Teks (✓) |
| Hash | Hash (✓) [universal loanword] | Hash (✓) | Hash (✓) | Skrót / Hash (?) | Хеш (✓) | Хеш (✓) | Özet (?) [or "Hash"] | Hash (✓) | تجزئة (?) [or "هاش"] | ハッシュ (✓) | 해시 (✓) | Hash (✓) |
| Signature | Firma (✓) | Firma (✓) | Assinatura (✓) | Podpis (✓) | Подпись (✓) | Підпис (✓) | İmza (✓) | Handtekening (✓) | توقيع (✓) | 署名 (✓) | 서명 (✓) | Tanda tangan (✓) |
| Certificate | Certificado (✓) | Certificato (✓) | Certificado (✓) | Certyfikat (✓) | Сертификат (✓) | Сертифікат (✓) | Sertifika (✓) | Certificaat (✓) | شهادة (✓) | 証明書 (✓) | 인증서 (✓) | Sertifikat (✓) |
| Fingerprint | Huella digital (✓) | Impronta (✓) | Impressão digital (✓) | Odcisk (klucza) (✓) | Отпечаток (✓) | Відбиток (✓) | Parmak izi (✓) | Vingerafdruk (✓) | بصمة (✓) | フィンガープリント (✓) | 지문 (✓) | Sidik jari (✓) |
| Copy | Copiar (✓) | Copia (✓) | Copiar (✓) | Kopiuj (✓) | Копировать (✓) | Копіювати (✓) | Kopyala (✓) | Kopiëren (✓) | نسخ (✓) | コピー (✓) | 복사 (✓) | Salin (✓) |
| Download | Descargar (✓) | Scarica (✓) | Baixar (✓) [pt-PT: Transferir] | Pobierz (✓) | Скачать (✓) | Завантажити (✓) | İndir (✓) | Downloaden (✓) | تنزيل (✓) | ダウンロード (✓) | 다운로드 (✓) | Unduh (✓) |
| Search | Buscar (✓) | Cerca (✓) | Buscar (✓) [pt-PT: Pesquisar] | Szukaj (✓) | Поиск (✓) | Пошук (✓) | Ara (✓) | Zoeken (✓) | بحث (✓) | 検索 (✓) | 검색 (✓) | Cari (✓) |
| Settings | Ajustes (✓) [es-ES] / Configuración (✓) [neutral] | Impostazioni (✓) | Configurações (✓) | Ustawienia (✓) | Настройки (✓) | Налаштування (✓) | Ayarlar (✓) | Instellingen (✓) | الإعدادات (✓) | 設定 (✓) | 설정 (✓) | Pengaturan (✓) |
| Language | Idioma (✓) | Lingua (✓) | Idioma (✓) | Język (✓) | Язык (✓) | Мова (✓) | Dil (✓) | Taal (✓) | اللغة (✓) | 言語 (✓) | 언어 (✓) | Bahasa (✓) |
| Tool | Herramienta (✓) | Strumento (✓) | Ferramenta (✓) | Narzędzie (✓) | Инструмент (✓) | Інструмент (✓) | Araç (✓) | Hulpmiddel / Tool (?) | أداة (✓) | ツール (✓) | 도구 (✓) | Alat (✓) |
| About | Acerca de (✓) | Informazioni (✓) | Sobre (✓) | O programie (✓) | О программе (✓) | Про програму (✓) | Hakkında (✓) | Over (✓) | حول (✓) | 概要 (✓) | 정보 (✓) | Tentang (✓) |
| Privacy | Privacidad (✓) | Privacy (✓) | Privacidade (✓) | Prywatność (✓) | Конфиденциальность (✓) | Конфіденційність (✓) | Gizlilik (✓) | Privacy (✓) | الخصوصية (✓) | プライバシー (✓) | 개인정보 보호 (✓) | Privasi (✓) |
| Open source | Código abierto (✓) | Open source (✓) | Código aberto (✓) | Otwarte oprogramowanie (✓) | Открытый исходный код (✓) | Відкритий код (✓) | Açık kaynak (✓) | Open source (✓) | مفتوح المصدر (✓) | オープンソース (✓) | 오픈 소스 (✓) | Sumber terbuka (✓) |
| Offline | Sin conexión (✓) | Offline (✓) | Offline / Sem conexão (✓) | Offline / bez połączenia (✓) | Офлайн / без сети (✓) | Офлайн (✓) | Çevrimdışı (✓) | Offline (✓) | بدون اتصال (✓) | オフライン (✓) | 오프라인 (✓) | Luring / Offline (✓) |

**Translation pitfalls observed during these passes** (notes for the maintainer):

- **Spanish "encriptar" vs "cifrar".** Both are widely used; cryptographers prefer "cifrar". Lock one project-wide. I recommend `cifrar` (the academic / industry-standard term in Spain and most LatAm cryptography literature) but include `encriptar` in the palette synonyms.
- **French "chiffrer" vs "crypter".** `chiffrer` is correct; `crypter` is a frequent calque from English and is technically wrong (in French cryptographic literature "crypter" means *to encode in an unknown cipher*). Use `chiffrer` in UI strings, include `crypter` as a palette synonym only.
- **Portuguese "criptografar" vs "cifrar".** Both used; in pt-BR `criptografar` is more common; in pt-PT `cifrar` is more common. Pick per-locale.
- **German "verschlüsseln" vs "chiffrieren".** Use `verschlüsseln` (modern); `chiffrieren` is archaic.
- **Russian/Ukrainian "ключ".** Same word as "key" in any sense (door key, musical key). Always pair with the modifier (открытый/закрытый, відкритий/закритий) in UI labels, never alone.
- **Polish verb aspect.** Polish has perfective/imperfective verb pairs. Button labels should use the perfective (`Zaszyfrować` — "to encrypt [completely]"), not imperfective (`Szyfrować` — "to be encrypting"). This is a frequent machine-translation error.
- **Arabic gendered verbs.** Imperative button labels in Arabic carry implicit gender. Use the masculine singular form by convention (this is the standard for software UI in Arabic).
- **Japanese "暗号化" vs "暗号化する".** Use the noun form (`暗号化`) for menu labels, and the verb form (`暗号化する`) only inside sentences. Buttons are typically the noun.
- **Chinese "密钥" vs "秘钥".** Both appear in the wild; `密钥` is correct and standard. `秘钥` is a common typo / non-standard variant. Audit `zh-CN` strings for this — it's easy to slip through.
- **Indonesian "berkas" vs "file".** `berkas` is the official Indonesian word but `file` is far more common in tech contexts. Pick `file` for consistency with how Indonesian developers actually speak about software. (Reverse argument: `berkas` is more accessible to non-technical users. Native reviewer to decide.)
- **Turkish vowel harmony.** Suffixes attached to dynamic content (e.g., `{filename}'i indir`) need to obey vowel harmony, which can't be done with simple string interpolation. Prefer rewording (`İndir: {filename}`) over interpolated suffixes.

These notes are explicitly the kind of thing that takes multiple passes — Pass 1 (machine) will get every one of them wrong; Pass 3 (native review) is where they get caught.

### 1.8 Per-locale CSS / typography checklist

- **Latin-script locales:** existing fonts sufficient (system stack).
- **Cyrillic (`ru`, `uk`):** confirm system fonts on macOS, Windows, and Linux cover Cyrillic glyphs at all weights. They do.
- **Arabic (`ar`):** RTL pass + line-height bump (Arabic glyphs sit lower). Avoid italics — Arabic doesn't italicize.
- **CJK (`zh`, `ja`, `ko`):** `word-break: keep-all`, slight line-height bump. Avoid all-caps styling.
- **Indic (`hi`, `bn`, `ta`):** confirm conjunct rendering. macOS and Android handle this well; older Windows fonts can break Bengali ligatures. No fix in CSS — note it in the README.
- **Vietnamese (`vi`):** diacritics stack tall; line-height bump.

---

## 2. New tools — Europe-resonant additions

Each tool below follows the existing project pattern: pure browser code, zero network, vendored deps only, ASCII-armored or hex output where applicable, constant-time comparisons where they matter.

### 2.1 IBAN validator / generator — **✅ shipped (draft 2)**

**Why:** Every European has typed an IBAN wrong. The tool gives instant feedback on a string that is otherwise opaque.

**Status notes (draft 2):** Landed as the `iban` tool with full English, French, and German UI strings (banking terms locked against the §1.7 glossary). zh-CN and hi have machine-quality drafts pending native review. Country table covers ~80 IBAN-issuing jurisdictions; BBAN sub-fields surfaced where the registry defines them. Generator uses `crypto.getRandomValues` for the BBAN and is labeled "For test data only." Functional tests cover the canonical ECBS example (`DE89 3704 0044 0532 0130 00`), a flipped-digit negative case, and a generator → validator round-trip.

**Spec:**
- Input: a string. Tolerate spaces, dashes, lowercase.
- Validate:
  1. Strip non-alphanumerics, uppercase.
  2. First two chars are an ISO 3166-1 alpha-2 country code with a known IBAN length (table of ~80 countries, ship inline).
  3. Total length matches that country's expected length.
  4. MOD-97 checksum: rearrange `CCKK BBAN` → `BBAN CCKK`, map letters A-Z → 10-35, compute the resulting integer modulo 97, expect 1.
- Output:
  - Pass/fail with reason.
  - Country, bank code, account number, branch code parsed from the BBAN per the country's structure (these subfields vary; ship a per-country regex/structure table).
  - Pretty-printed form (groups of 4).
- Generator side: produce a *valid* IBAN for a given country and BBAN (fill in the two checksum digits using MOD-97). Useful for test data. Label it clearly: **"For test data. Do not use for real transactions."**

**Dependencies:** none. Hand-written, ~120 lines.

**Security notes:** IBANs are commercial identifiers, not PII in any meaningful sense, but the tool should still process input only locally. Pasted IBANs should never appear in the URL, in `localStorage`, or in `document.title`.

### 2.2 BIC / SWIFT code checker — **✅ shipped (draft 2)**

**Why:** Companion to IBAN. ISO 9362 format check.

**Status notes (draft 2):** Landed as the `bic` tool. Validates 8- and 11-character forms, decodes bank / country / location / branch, cross-checks the country segment against the IBAN country table, and flags conventional markers (`0`-terminated → test BIC; `1`-terminated → passive participant; branch `XXX` → primary). en/fr/de translations reviewed against standard banking terminology; zh-CN/hi are machine-quality drafts.

**Spec:**
- Format: `BBBB CC LL [XXX]` — 4 bank, 2 country (ISO 3166-1 alpha-2), 2 location, optional 3 branch.
- Validate length (8 or 11), uppercase, alphanumeric, valid country code, location code rules (second character `0` = test BIC, etc.).
- No checksum (BIC has no checksum), but expose the parsed components and flag test BICs (location code ends in `0`).

**Dependencies:** none.

### 2.3 EU VAT number validator (offline) — **✅ shipped (draft 3)**

**Why:** Catches 90% of typos without ever touching VIES.

**Status notes (draft 3):** Landed as the `vat` tool. Format check for all 27 EU member states + GB, NO, CH, XI (Northern Ireland). Checksum implemented for AT, BE, DE, DK, EE, EL, ES (CIF only), FI, FR, HR, HU, IT, LU, MT, PL, PT, SE, SI, SK, CH, NO (≈ 70% of supported set). Countries without a published or stable algorithm (BG, CY, CZ, GB legacy, IE, LT, LV, NL post-2020, RO, XI) report "format only" without claiming more. ES support is intentionally limited to the CIF subset; individual NIF (`12345678Z`-style) and NIE (`X1234567Z`-style) inputs are explicitly rejected with a message pointing at the spec, satisfying the §7.1 tension constraint. en/fr/de translations locked against standard tax terminology (German `USt-IdNr.`, French `TVA`); zh-CN/hi are machine-quality drafts. Functional tests cover real public VAT numbers from DE/FR/IT (Ferrero `IT00159560366`), the NL "format-only" path, the ES NIF rejection, and a DE flipped-digit negative case.

**Spec:**
- Per-country format and checksum table for all 27 EU member states + GB, NO, CH (close enough to ship together).
- Each country has a different algorithm:
  - DE: 9 digits, weighted checksum.
  - FR: 2-char prefix + 9 digits, MOD-97 derived from the SIREN.
  - IT: 11 digits, Luhn-style checksum.
  - ES: 1 letter + 7 digits + 1 letter/digit, several sub-formats (NIF, NIE, CIF).
  - NL: 9 digits + B + 2 digits, no checksum.
  - PL: 10 digits, weighted checksum.
  - etc.
- Output: pass/fail per the format and (where one exists) per the checksum, plus a clearly-labeled "this is a syntactic check — to verify the VAT number is actively registered, use VIES at the EU Commission's website" disclaimer with a link out.

**Dependencies:** none. ~300 lines of country-specific code.

**Tension flag:** ES NIF/NIE overlap with personal tax IDs. **Validate VAT formats only**, never NIF/NIE for individuals. The CIF (corporate) format is fine. This is one of the rare cases where the line between "commercial identifier" and "PII" is fuzzy — when in doubt, omit the country.

### 2.4 SEPA XML inspector (pain.001 / camt.053 / pain.008) — **🟡 pain.001 + camt.053 shipped (draft 16); pain.008 inherits the pain.001 renderer; EPC SEPA character-set checks shipped across MsgId / EndToEndId / InstrId / MndtId / PmtInfId / Ustrd / party Nm / AdrLine / TwnNm / PstCd (drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 / 63 / 65 / 67)**

**Status notes (draft 16):** Landed as the `sepa` tool. The parser is namespace-blind — it walks ISO 20022 XML by `localName` only, so the same code handles `pain.001.001.03`, `pain.001.001.09`, `camt.053.001.02`, `camt.053.001.08`, and any other minor revision without per-version branching.

- **pain.001 + pain.008**: header row (MsgId, CreDtTm, InitgPty, NbOfTxs, CtrlSum) + per-`PmtInf` debtor account/IBAN/BIC, then per-transaction (`CdtTrfTxInf` / `DrctDbtTxInf`) end-to-end ID, amount + currency (from `Amt/InstdAmt`'s `Ccy` attribute), debtor + creditor names with their IBANs in parens, remittance info.
- **camt.053**: statement ID, account IBAN, FrToDt window, every `<Bal>` (opening `OPBD` and closing `CLBD` labeled explicitly via the existing strings table; other balance-type codes fall through with their raw 4-letter code), then per-`Ntry` booking date, amount, credit/debit indicator.
- **IBAN cross-check**: every `<IBAN>` element anywhere in the document is run through the existing `ibanValidate()` (the same MOD-97 implementation that powers the IBAN tool). When all checksums pass, a single confirmation row is rendered (`IBAN MOD-97 valid (N)`); on a failure, each bad IBAN gets its own warning row.
- **Unrecognized roots**: a `<Document>` whose first child isn't one of the three known wrappers still renders the header (with the unknown local name as the document type) and then emits a `sepa.warn.unknownDoc` warning rather than refusing the file outright.

UI: a single textarea + Parse button, mirrors the existing IBAN / BIC / VAT layout. No vendored deps — `DOMParser` only. ~200 lines of parser + render JS, ~12 new strings × 5 locales (en + fr + de reviewed against standard banking terminology; zh-CN + hi are machine-quality drafts per the project pattern). 5 SEO prose keys (`tool.sepa.{title,metaDescription,about.{what,how,why}}`) en-only per the existing Phase 7 carve-out.

Four new Playwright cases:

1. pain.001 — parses headers, transactions, both IBANs, and renders `100.00 EUR` + `Invoice 2024-001`.
2. camt.053 — parses statement ID, opening / closing balance, entry amounts.
3. Flipping the last digit of the debtor IBAN — surfaces a MOD-97 warning row with the bad IBAN inline.
4. Non-ISO-20022 XML (`<foo><bar/></foo>`) — reports `No <Document> root element`.

Full Playwright suite: 227 passed. Tool count 42 → 43; JSON-LD `featureList_count`, JSON-LD description, meta description, og:description, twitter:description, and README all updated.

**What's not in this slice:** XAdES-signed SEPA files (the embedded `<ds:Signature>` inside pain.001 production files is parsed *around* by the namespace-blind walker, not verified — that's §2.5 territory); SEPA card schemes (`acmt.*`, `caaa.*`).

**Draft 22 polish:** per-PmtInf `<ReqdExctnDt>` (requested execution date, both schema shapes — bare element value for pain.001.001.03 and the wrapped `<Dt>` child form for .09+) is now surfaced as a header row above the transactions for each `<PmtInf>` group. Per-transaction `<MndtId>` is surfaced for pain.008 direct debits (sourced from `DrctDbtTx/MndtRltdInf/MndtId`); it only appears on transactions that carry one, so pain.001 credit transfers are unaffected. Two new strings (`sepa.row.reqdExctnDt`, `sepa.tx.mndtId`) × 5 locales. Two new Playwright cases (ReqdExctnDt date row, pain.008 MndtId row).

**Draft 19 polish:** schema-version detection landed — `sepaDetectDocType()` now reads the `Document`/wrapper `namespaceURI`, extracts the trailing `<msg>.<variant>.<version>` triple (e.g. `pain.001.001.03`), and surfaces it inline in the document-type row so users can distinguish pain.001.001.03 from .09 at a glance. The fallback when the namespace URI is absent or non-matching is the friendly mnemonic alone, so unknown / handwritten inputs degrade cleanly. Per-transaction `<InstrId>` is now surfaced above `<EndToEndId>` (mirroring the order banks emit them); one new UI string `sepa.tx.instrId` × 5 locales (en/fr/de reviewed against the same banking-terminology lock as the rest of the SEPA strings; zh-CN/hi machine-quality drafts pending native review, consistent with the project pattern). Two new Playwright cases (`pain.001.001.03` banner; InstrId surfaced alongside EndToEndId). No new vendored deps, no CSP change, no tool-count change.

**Why:** Finance/accounting developers across the EU need to debug payment files. No good browser-based tool exists.

**Spec:**
- Accept SEPA ISO 20022 XML: `pain.001.001.{03,09}` (credit transfer initiation), `pain.008.001.{02,08}` (direct debit initiation), `camt.053.001.{02,08}` (bank-to-customer statement).
- Parse with `DOMParser` (no third-party XML library — same pattern as the existing JSON/YAML/XML formatter).
- Render:
  - Header: message ID, creation datetime, initiating party, number of transactions.
  - Per-transaction table: end-to-end ID, amount + currency, debtor/creditor name + IBAN/BIC, remittance info, scheduled date.
  - Validation: schema-level sanity (required elements present), IBAN MOD-97 on each party (reuse §2.1), currency code is ISO 4217.
- Output: a pretty HTML table; one-click copy as CSV.

**Dependencies:** none (DOMParser is built in).

### 2.5 eIDAS digital signature inspector — PAdES / CAdES / XAdES

**Why:** EU has invested heavily in eIDAS qualified signatures. Free inspector tools are scarce, server-based, or paywalled.

**Spec:**
- **PAdES** (PDF signatures): parse the PDF, extract the `/Sig` dictionary, extract the embedded CMS `Contents` blob, hand off to the CAdES parser.
- **CAdES** (CMS / RFC 5652 + extensions): ASN.1 / DER parser (extend the existing X.509 parser — it already does ASN.1). Surface: signer info, signing certificate (chain if embedded), signing time, signature algorithm, content hash, attribute certificates, timestamp token (RFC 3161) if present, OCSP/CRL revocation info if embedded.
- **XAdES** (XML signatures): `DOMParser`-based. Surface the same fields. XAdES levels (B, T, LT, LTA) are detectable from the embedded properties.
- Output: a structured view of every property; raw ASN.1 / XML toggle for the curious.
- **No revocation checking against the network.** Tool reports what's in the signature; it does not contact OCSP responders or CRL endpoints.

**Dependencies:** none (extends the existing ASN.1 parser, reuses `DOMParser`).

**This is the largest tool in this spec. Plan ~1,500 lines and a multi-week effort.**

### 2.6 EU LOTL trust list viewer — **✅ shipped (draft 17)**

**Status notes (draft 17):** Landed as the `lotl` tool. The parser walks ETSI TS 119 612 XML by `localName` only, so the same code handles every minor revision (v2.1.1, v2.2.1, ...) without per-version branching.

- **LOTL mode** (`TSLType` ends in `EUlistofthelists`): renders the scheme operator + name + territory + issue/next-update dates, then one section per `<OtherTSLPointer>` with the country code (mined from `OtherInformation/SchemeTerritory`) and the `<TSLLocation>` URL.
- **TSL mode** (`TSLType` ends in `schemes/...`): renders the scheme metadata, then one section per `<TrustServiceProvider>` showing the TSP name, followed by each `<TSPService>` with type identifier (e.g. `Svctype/CA/QC`), status (e.g. `Svcstatus/granted`), and status starting time. Embedded `<X509Certificate>` blocks are acknowledged as present with their base64 byte count, but not parsed inline — the TLS Certificate Parser tool handles that if the user wants details.
- **Multi-language `<Name>` blocks**: ETSI lists carry `xml:lang`-tagged `<Name>` children inside `<SchemeName>`, `<SchemeOperatorName>`, `<TSPName>`, etc. The viewer prefers `xml:lang="en"` and falls back to the first child if no English variant is present.
- **No fetching**: the §0.2 / §2.6 "no network" rule is preserved — every parse run appends a footer note pointing the user to `ec.europa.eu` for the official LOTL file. No automatic resolution of `<TSLLocation>` URLs.

UI mirrors the SEPA tool layout (textarea + Parse button). ~190 lines of parser + render JS, ~25 new UI strings × 5 locales (en + fr + de reviewed against standard PKI/eIDAS terminology; zh-CN + hi machine-quality drafts pending native review per the project pattern). 5 SEO prose keys en-only via the existing Phase 7 carve-out.

Three new Playwright cases:

1. LOTL mode — both German and French pointer URLs surface, both country codes appear.
2. TSL mode — Bundesnetzagentur (scheme operator), D-TRUST GmbH (TSP), CA/QC service type, "granted" status, and the embedded X.509 acknowledgement row.
3. Non-trust-list XML (a bare `<Document/>`) gets rejected with a `TrustServiceStatusList` mention in the error.

Full Playwright suite: 230 passed. Tool count 43 → 44; JSON-LD featureList + featureList_count, meta / og / twitter descriptions, and README updated.

**Why:** EU publishes a "List of Trusted Lists" — a master XML pointing to each member state's trust list of qualified certificate authorities. Useful to anyone debugging eIDAS-signed documents. Almost no free tool surfaces this nicely.

**Spec:**
- The user pastes a Trust Service Status List (TSL) XML file or a LOTL XML.
- Parse with `DOMParser`.
- Render: per-country sections, per-service entries (CA name, service type, status, service supply points, qualifiers).
- For each embedded certificate, link to the existing X.509 parser (same site).
- **No fetching.** User downloads the LOTL from the EU Commission's published URL themselves and pastes it. This is a non-negotiable consequence of the "no network" rule. Document the source URL in the tool's help text.

**Dependencies:** none.

### 2.7 Factur-X / ZUGFeRD / PEPPOL UBL invoice inspector — **🟡 PEPPOL UBL shipped (draft 18); PEPPOL profile + Seller/Buyer VAT identifier gates added (drafts 50 / 52 / 54 / 57 / 58); InvoicePeriod sanity (draft 60); PaymentMeansCode UNCL 4461 gate (draft 62); DocumentCurrencyCode consistency BR-CO-04 (draft 64); TaxCurrencyCode ISO 4217 gate BT-6 (draft 66); Factur-X / ZUGFeRD / CII deferred**

**Status notes (draft 18):** Landed as the `ubl` tool. Namespace-blind `DOMParser` walk by `localName` handles every OASIS UBL 2.1 minor revision and the PEPPOL BIS Billing 3.0 customization without per-version branching. Both root document types are detected: `<Invoice>` and `<CreditNote>` (the credit-note renderer reuses the same line-item path but pulls `CreditNoteLine` / `CreditedQuantity` instead of `InvoiceLine` / `InvoicedQuantity`). Surfaced fields: `CustomizationID`, `ProfileID`, invoice number, issue and due dates, document currency, supplier / customer name (`PartyName/Name` preferred, falls back to `PartyLegalEntity/RegistrationName` as it is in most real PEPPOL invoices), the four `LegalMonetaryTotal` amounts (line-extension, tax-exclusive, tax-inclusive, payable), per-line item (quantity with `unitCode`, unit price, item name, line amount), and per-rate `TaxSubtotal` breakdown (category ID + scheme + percent + taxable amount + tax amount). All currency-tagged amounts render with their `currencyID` (e.g. "119.00 EUR"). No vendored deps — the parser reuses the existing `sepaFirst` / `sepaTextOf` / `sepaAllLocal` helpers as generic namespace-blind XML walkers. ~160 lines of parser + render JS, ~32 new UI strings × 5 locales (en + fr + de reviewed against standard e-invoicing terminology; zh-CN + hi machine-quality drafts pending native review per the project pattern), 5 SEO prose keys en-only via the existing Phase 7 carve-out. Tool count 44 → 45; JSON-LD `featureList` + `featureList_count`, meta / og / twitter descriptions, and README all updated. Three new Playwright cases (Invoice happy path with line + tax breakdown, CreditNote happy path, non-UBL XML rejected).

**Draft 29 polish:** the inspector now also cross-checks every `Ccy="..."` attribute against the ISO 4217:2015 (Amendment 174) active-currencies set (inline, ≈ 180 codes). Confirmation row uses `intlList()` so the locale-correct joiner ("and" / "und" / "et") is applied; warning row per offending code. Historic codes are deliberately rejected so the cleanup task is visible.

**Draft 27 polish:** the inspector now cross-checks every `<BIC>` and `<BICFI>` element against ISO 9362 (`bicValidate()` from the standalone BIC tool), parallel to the existing IBAN MOD-97 walk. One confirmation row on full success, one warning row per bad BIC on failure. Catches typos that the IBAN check wouldn't surface (e.g. a digit-bearing first segment, lowercase characters, an 11-char BIC missing its branch). 4 new strings × 5 locales; 2 new Playwright cases.

**Draft 28 polish:** the inspector now surfaces an EN 16931 totals consistency check after parsing — ✓ rows when both invariants hold, ✗ rows with computed vs declared values on mismatch. Two invariants: Σ line LineExtensionAmount = declared LineExtensionAmount; TaxExclusive + Σ TaxAmount = TaxInclusive. Integer-cents arithmetic with ±1¢ rounding tolerance.

**Draft 22 polish:** the line-items table now ships with a "Copy as CSV" button. The export rows are emitted alongside the on-screen rendering (same parse, same `<InvoiceLine>` / `<CreditNoteLine>` walk) and the column shape is fixed: `#,Name,Quantity,UnitCode,Price,PriceCurrency,LineAmount,LineCurrency`. Quoting follows RFC 4180 — fields with `,`, `"`, or newline are double-quoted with embedded `"` doubled up; empty cells pass through unquoted. The button label gives the user a `✓ Copied` confirmation for 1.5 s after the click. Four new strings (`ubl.lines.copyCsv` + `copied` + `copyFail` + the existing `lines.heading` styling tweak) × 5 locales. One new Playwright case (asserts the CSV column header and the first line item row byte-for-byte against the test fixture).

**Factur-X / ZUGFeRD deferred.** Hybrid PDF/A-3 inputs need the PDF attachment extractor that is also a §2.5 dependency (PAdES `/EmbeddedFiles` walk). When that lands for PAdES, the same extractor will feed CII (UN/CEFACT Cross Industry Invoice) XML into a sister renderer here — same six row groupings, different element names. Profile / conformance level highlighting (Minimum / Basic WL / Basic / EN 16931 / Extended for ZUGFeRD; equivalents for Factur-X) lands in that same slice. CSV export of line items deferred until there's at least one user request — UBL is already searchable with browser find.

**Why:** German and French e-invoicing rules mandate one of these formats by 2026–2027. Many devs will be debugging invoice XML for years.

**Spec:**
- **PEPPOL UBL** (`Invoice`/`CreditNote` XML, OASIS UBL 2.1): parse, render header (supplier, customer, totals, currency, due date), line items table, tax breakdown.
- **Factur-X / ZUGFeRD** (hybrid PDF/A-3 with embedded CII XML): extract the embedded XML attachment from the PDF, then parse as CII (UN/CEFACT Cross Industry Invoice). Same renderer otherwise.
- Highlight which profile/conformance level the document claims (Minimum / Basic WL / Basic / EN 16931 / Extended for ZUGFeRD; equivalent for Factur-X).
- Output: structured view + one-click CSV export of line items.

**Dependencies:** none (DOMParser + extending PDF parsing already in §2.5).

### 2.8 EUDI Wallet credential decoder (SD-JWT and ISO mdoc) — **🟡 SD-JWT shipped (draft 20); ISO mdoc deferred**

**Status notes (draft 20):** SD-JWT support landed as an extension of the existing JWT inspector (no new top-level tool, no tool-count change — per the §2.8 spec's "already partly handled by the existing JWT inspector" framing). The JWT input field now detects the SD-JWT `~`-separator and:

- Splits the input into a leading JWT, zero or more disclosures, and an optional trailing key-binding JWT (KB-JWT detected by `dot.dot.dot` shape of the final segment).
- Decodes each disclosure as base64url → UTF-8 → JSON. Recognizes the two disclosure shapes from draft-ietf-oauth-selective-disclosure-jwt §5.2: `[salt, name, value]` for object claims and `[salt, value]` for array elements.
- Computes the SHA-256 digest over the wire-bytes of the disclosure (the literal base64url ASCII bytes, per §5.1.1) — not over the decoded JSON. The `_sd_alg` payload claim is honored (defaults to `sha-256` per §5.1).
- Walks the entire payload tree to collect every `_sd` digest array (top-level and nested under `vc`, `address`, etc.), then per-disclosure shows `✓ referenced in payload _sd` when the computed digest is present and `✗ unreferenced` when it is orphaned.
- Surfaces a present KB-JWT as a separate decoded JSON block (header + payload pretty-printed). Signature verification of the KB-JWT is out of scope for this slice — the existing JWT verify path covers the leading SD-JWT issuer signature, and the KB-JWT verification flow (audience, nonce, holder key resolution) is large enough to warrant its own slice if/when there's demand.

10 new UI strings (`jwt.sdjwt.{heading,colName,colValue,colSalt,colDigest,matched,orphan,arrayElem,kbJwt,parseErr}`) × 5 locales — en/fr/de reviewed against the SD-JWT draft's own vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern. Three new Playwright cases (digest-matched disclosures, orphaned disclosure flagged, KB-JWT surfaced). Full suite: 235 → 238 passed. No new vendored deps, no CSP change.

**Status notes (draft 21):** Two follow-ups landed.

- **Cleartext-payload reconstruction (SD-JWT draft §7).** After the disclosure table, the inspector now renders a `Disclosed payload (reconstructed)` block: every `_sd` digest that has a matching disclosure is replaced by the disclosed claim name + value; array `{"...": digest}` placeholders are replaced by the disclosed array element; `_sd` and `_sd_alg` are stripped. Digests *without* a matching disclosure are dropped — that is what a verifier sees when the holder withholds a claim, so the rendered output is exactly the JSON the verifier would consume. One new UI string (`jwt.sdjwt.reconstructed`) × 5 locales (en/fr/de reviewed; zh-CN/hi machine-quality drafts pending native review). One new Playwright case (reconstructs two disclosed claims, confirms `_sd` / `_sd_alg` are stripped). Full suite: 238 → 239 passed.
- **`bytesToBase64()` consolidation.** The file used to ship two declarations — the second one (no `urlSafe` parameter) hoisted over the first, silently disabling base64url output for any caller below the duplicate. The redundant second declaration was deleted; sdJwtRender now calls the canonical helper instead of carrying its own inline base64url loop. PBKDF2 base64 output (the only caller of the second form) is unaffected because the first signature treats `urlSafe === undefined` as falsy, returning the same plain base64 the second form produced. Audit + a11y + full Playwright suite all green.

**Status notes (draft 23):** KB-JWT signature verification + `sd_hash` binding check landed.

- When the input is an SD-JWT with a trailing key-binding JWT and the issuer-signed payload declares a `cnf.jwk`, the inspector now imports that JWK via Web Crypto (`sdJwtImportJwk()` maps EC P-256 → ES256, EC P-384 → ES384, OKP Ed25519 → EdDSA, RSA → RS256/RS384/RS512), verifies the KB-JWT signature over `<kb-header>.<kb-payload>`, and renders a `KB-JWT signature ✓ verified with cnf.jwk (ES256)` / `✗ signature does not verify` row. Missing `cnf.jwk` is surfaced as an explicit "no cnf.jwk … cannot verify" row rather than a silent skip.
- The KB-JWT payload's `sd_hash` claim is recomputed at decode time: `base64url(SHA-256(<jwt>~<disc1>~…~<discN>~))` — the literal presentation bytes up to and including the final tilde but excluding the KB-JWT itself, per SD-JWT draft §4.3. Mismatch (substituted disclosures or a swapped KB-JWT) renders an explicit "does not match" row.
- Seven new UI strings (`jwt.sdjwt.{kbSig,kbSigOk,kbSigFail,kbNoCnf,sdHash,sdHashOk,sdHashFail}`) × 5 locales (en/fr/de reviewed against the SD-JWT draft's vocabulary; zh-CN/hi machine-quality drafts pending native review). Two new Playwright cases (ES256 KB-JWT with on-the-fly node-generated P-256 keypair: happy-path verify + tampered-signature rejection). Full suite: 242 → 244 passed.

**Status notes (draft 24):** KB-JWT verifier-side `aud` / `nonce` cross-checks landed. A collapsible `<details>` panel under the JWT input now hosts two optional inputs (`#jwt-kb-aud`, `#jwt-kb-nonce`). When either is filled, sdJwtRender appends an explicit row comparing the value against the KB-JWT payload's `aud` (string or array per RFC 7519) / `nonce` claim — ✓ on match, ✗ with the received value on mismatch, and an explicit "no aud/nonce claim in KB-JWT" row when the claim is absent so a verifier never silently skips a check. Inputs are intentionally not persisted (no localStorage, no URL params) — they are per-decode replay-defense values, not user preferences. 13 new UI strings (`jwt.sdjwt.kbExpect{Summary,AudLabel,AudPh,NonceLabel,NoncePh}` + `jwt.sdjwt.kb{Aud,AudOk,AudFail,AudMissing,Nonce,NonceOk,NonceFail,NonceMissing}`) × 5 locales (en/fr/de reviewed against the SD-JWT draft + RFC 7519 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Three new Playwright cases (aud match, aud mismatch surfaces received value, nonce mismatch surfaces received value). Full suite: 244 → 247 passed. No new vendored deps, no CSP change, tool count unchanged.

**Deferred to a follow-up slice:**
- **ISO mdoc decoder.** Needs a small CBOR decoder (~300 lines, hand-rolled per ISO 18013-5's deterministic CBOR subset) plus COSE_Sign1 unwrapping for `MobileSecurityObject`. The CBOR decoder is also the natural prerequisite for any future COSE / WebAuthn attestation work, so it earns the vendor / hand-roll trade-off in its own slice.

**Why:** EU Digital Identity Wallet pilots are running now and the regulation forces member states to roll out wallets by 2026. The credential formats — SD-JWT (selective-disclosure JWT) and ISO 18013-5 mdoc (CBOR + COSE-signed) — are new and tooling is sparse.

**Spec:**
- **SD-JWT (`vc+sd-jwt`)**: parse the issuer-signed JWT (already partly handled by the existing JWT inspector), decode the disclosures (Base64URL-encoded JSON arrays), reconstruct the cleartext claim set, surface which claims are disclosed vs blinded.
- **ISO mdoc**: CBOR + COSE_Sign1. Needs a small CBOR decoder (~300 lines, hand-rolled; specifically the deterministic CBOR subset used by ISO 18013-5). Decode the `MobileSecurityObject`, surface namespaces, doctype, digests, validity, device key.
- **No verification against issuer trust anchors.** Tool decodes; does not verify the chain.

**Dependencies:** small hand-rolled CBOR decoder. Worth vendoring as `cbor.min.js` alongside the other deps.

**Risk:** the specs are still moving. Implement against the published ARF v1.x (Architecture and Reference Framework) and label the tool as "early — formats may change."

### 2.9 Argon2id KDF

**Why:** Modern PBKDF2 replacement. BSI, ANSSI, OWASP, and the Password Hashing Competition winners all recommend Argon2id.

**Spec:**
- Inputs: password (text), salt (text or hex), memory cost (KiB), time cost (iterations), parallelism (lanes), output length (bytes), variant (Argon2id default; offer Argon2i and Argon2d).
- Output: hex + PHC-formatted string `$argon2id$v=19$m=...,t=...,p=...$<b64-salt>$<b64-hash>`.
- Report wall-clock derivation time (same UX as the existing PBKDF2 tool).

**Dependencies:** This is the trade-off. Options:
1. **Vendored pure-JS Argon2** (e.g., `argon2-js`, ~30KB, ~10x slower than WASM). Acceptable for parameters typical of interactive logins (memory ~64MB, t=3). Painful for larger parameters.
2. **Vendored WASM Argon2** (`argon2-browser` core ~50KB Wasm). Fast. Requires the page to load WASM, which is a CSP change.

**Recommendation:** ship pure-JS first, label it "slower than native", and revisit WASM only if real users complain.

### 2.10 Ed25519 / X25519 standalone tools — **✅ shipped (draft 4)**

**Why:** Web Crypto now supports Ed25519 and X25519 natively (Safari 17, Chrome 113, Firefox 130). Useful for SSH, age, Signal, Noise, and many EU-origin protocols.

**Status notes (draft 4):** Shipped as two sister tools, `ed25519` and `x25519`. Both use native Web Crypto exclusively — no vendored JS, no WASM. Both detect lack of browser support and show an explicit banner with the minimum browser versions, rather than failing silently. Private keys are surfaced as the 32-byte RFC 8032 §5.1.5 seed (Ed25519) / RFC 7748 scalar (X25519); on import, the tool wraps the seed in the standard PKCS#8 / RFC 8410 envelope that Web Crypto requires. X25519 includes optional HKDF-SHA-256/SHA-512 post-processing with a user-supplied "info" context, matching Noise / X3DH conventions. Functional tests exercise RFC 8032 §7.1 vector 1 (empty message) and vector 2 (single byte `0x72`), a tampered-signature rejection, RFC 7748 §6.1 Alice/Bob shared secret derivation, and a round-trip generate→sign→verify; a11y verified in both themes.

**Spec:**
- **Ed25519:** generate keypair, sign (text or file), verify. Keys in raw 32-byte hex, OpenSSH format, and PEM (PKCS#8 / SPKI).
- **X25519:** generate keypair, compute shared secret given a peer public key. Keys in raw, PEM. Optionally pipe the shared secret through HKDF to produce a symmetric key (offer SHA-256 / SHA-512).

**Dependencies:** none — `window.crypto.subtle` handles both. Gracefully degrade on Safari < 17.

### 2.11 age file encryption

**Why:** Filippo Valsorda's age is a simpler, modern PGP successor — widely adopted in EU privacy circles (used by NixOS deployments, dotfile encryption, password managers like passage).

**Spec:**
- Encrypt to one or more X25519 recipients or scrypt passphrase recipients.
- Decrypt with X25519 identity or passphrase.
- Generate age keypairs (`age-keygen` equivalent).
- Output: armored age format (`-----BEGIN AGE ENCRYPTED FILE-----...`).

**Dependencies:** `age-encryption.js` (or hand-roll against the age v1 spec — it's small, ~600 lines). Vendor as `age.min.js`.

### 2.12 BLAKE2b / BLAKE3 hashes — **BLAKE2b ✅ shipped (draft 5); BLAKE3 deferred**

**Why:** Used by WireGuard, IPFS, Zcash, Filecoin, Borg backup, and other EU/global open-source projects. Modern, fast, and not in Web Crypto (which only does SHA-1/SHA-2).

**Status notes (draft 5):** BLAKE2b-512 shipped as an additional algorithm in the existing Hash & Checksum tool — no new tool view, no new top-level entry, just an extra row alongside SHA-1/SHA-256/SHA-384/SHA-512 in the output. Hand-rolled per RFC 7693 §3, 64-bit words emulated as 32-bit pairs (no BigInt to keep the inner loop fast). Verified against RFC 7693 §A.5 (the empty-input vector) and the canonical `"abc"` vector. ~150 lines of crypto code; no new vendored dependency. BLAKE3 deferred — its parallel tree-mode reference implementation is substantially larger and would benefit from a vendored single-file build rather than a hand-rolled port.

**Spec:**
- Add BLAKE2b (variable output 1–64 bytes, optional key for MAC) and BLAKE3 (variable output, fast on large inputs) to the existing hash & checksum tool.
- Same UI: text or file input, hex output, constant-time comparison.

**Dependencies:** vendor `blake2b.js` and `blake3.js` (both available as small single-file MIT-licensed implementations under 20KB combined).

### 2.13 GS1 / EAN / GTIN barcode checksum — **✅ shipped (draft 2)**

**Why:** Boring but universally useful. Validates EAN-8, EAN-13, UPC-A, UPC-E, ITF-14, GTIN-14, SSCC. Mod-10 weighted checksum.

**Status notes (draft 2):** Landed as the `gs1` tool. Detects format by length (8/12/13/14/18), computes mod-10 weighted checksum, looks up the GS1 prefix in a ~130-row inline registry (covers every assigned range as of GS1 General Specifications v23). Functional test uses the real EAN-13 `4006381333931` plus a flipped-digit negative case.

**Spec:**
- Input: a barcode number.
- Output: which format it matches (by length), pass/fail on checksum, and the GS1 prefix decoded (country / registration authority — table ships inline).

**Dependencies:** none. ~80 lines.

---

## 3. Regulator presets — **✅ shipped in PGP key generator (draft 5) + PBKDF2 tool (draft 10); Argon2 deferred with §2.9**

A dropdown in the PGP key generator (and in the PBKDF2 / Argon2 tools) that selects pre-vetted parameters per regulator.

**Status notes (draft 5):** Shipped for the PGP key generator. Presets surface BSI TR-02102-1, ANSSI RGS B1, NIST SP 800-57, CNSA 2.0, and Privacy Guides recommended; each auto-fills algorithm + keysize and links to the regulator's source document in a footnote. The PBKDF2/Argon2 extension is still pending (Argon2 itself is in §2.9 and not yet shipped).

**Status notes (draft 7):** Each preset now carries an `asOf: 'YYYY-MM-DD'` field tracking the most recent re-verification against the upstream document. The preset's help footnote surfaces this date inline (e.g. "parameters verified 2026-05-13") so the user can see at a glance how fresh the recommendation is. `scripts/audit-release.js` parses the literal and warns (does not fail) when any preset's stamp is more than 12 months old — a release can still ship, but the staleness is visible. Verified prefix translated for en/fr/de/zh-CN/hi.

**Status notes (draft 10):** Regulator-preset dropdown extended to the PBKDF2 tool. Four presets ship: BSI TR-02102-1 (1,000,000 iterations, SHA-256), ANSSI RGS B1 (600,000, SHA-256), NIST SP 800-132 / OWASP minimum (600,000, SHA-256 — the OWASP Password Storage Cheat Sheet floor is the most widely-cited interoperable minimum for SP 800-132 deployments and is what the preset cites), and Privacy Guides recommended (1,000,000, SHA-256). CNSA 2.0 is intentionally absent — KDF parameters fall outside CNSA scope, and the NSA explicitly defers to SP 800-132. Manually editing the iteration count or the PRF resets the dropdown back to "Custom" so the UI never claims a regulator's recommendation once the user has diverged. Each PBKDF2 preset carries the same `asOf: 'YYYY-MM-DD'` stamp + source link + help footnote as the PGP presets. `scripts/audit-release.js` was generalized to scan every `*REGULATOR_PRESETS*` literal (not just one), so the freshness check now covers both blocks (9 entries total) without per-block changes. Six new strings (`pbkdf2.label.regulatorPreset`, `pbkdf2.opt.preset.{custom,bsi,anssi,nistOwasp,privacyGuides}`, plus four `pbkdf2.preset.note.*` keys) translated for en/fr/de/zh-CN/hi (zh-CN/hi machine-quality drafts pending native review, consistent with the rest of those locales). Three new functional tests (BSI sets iterations + PRF; ANSSI 600k floor; manual edit resets to custom). Full Playwright suite: 209 passed.

| Preset | PGP key | PBKDF2 iters (SHA-256) | Argon2id (m, t, p) | Source URL |
|--------|---------|------------------------|---------------------|------------|
| **BSI TR-02102-1** | RSA 3072+ or Curve25519/Curve448 | ≥ 1,000,000 | m=64MiB, t=2, p=1+ | bsi.bund.de/SharedDocs/Downloads/EN/BSI/Publications/TechGuidelines/TG02102/BSI-TR-02102-1.html |
| **ANSSI RGS B1** | RSA 3072+ or Curve25519 | ≥ 600,000 | m=64MiB, t=3, p=1 | ssi.gouv.fr (RGS B1 annex) |
| **NIST SP 800-57** | RSA 3072+ or P-256/P-384 | ≥ 600,000 (OWASP 2023) | m=19MiB, t=2, p=1 (OWASP minimum) | csrc.nist.gov/pubs/sp/800/57/pt1/r5 |
| **CNSA 2.0** | RSA 3072+ or P-384 | n/a (KDFs not in CNSA scope; defer to SP 800-132) | n/a | nsa.gov/Press-Room/Press-Releases/Press-Release-View/Article/3148990 |
| **Privacy Guides** | Curve25519 (`ed25519`/`cv25519`) | ≥ 1,000,000 | m=64MiB, t=3, p=4 | privacyguides.org/en/basics/passwords-overview/ |

Each preset:
- Shows the choices it imposes in the UI before the user confirms.
- Links out to the regulator's actual document (not a summary) so the choice is verifiable.
- Is dated (`as of YYYY-MM-DD`) in the help text. Regulator recommendations drift; the preset must be auditable.
- Is implemented as a JSON blob, not hardcoded logic, so updating a preset is a one-line change.

**Update cadence:** Audit the four sources once per year (set a calendar reminder in `scripts/audit-release.js` so a release surfaces stale presets).

---

## 4. Distribution, sovereignty, and trust

These changes do not touch the codebase; they touch how the project is published and discovered.

### 4.1 Mirror on Codeberg

Codeberg is an EU-based (Germany), non-profit Forgejo instance. Many EU public-sector and activist users prefer it for jurisdictional reasons.

- Push a mirror to `codeberg.org/<user>/encryptalotta`.
- Add a `git remote` so pushes go to both GitHub and Codeberg.
- Mention both URLs in README and footer.

### 4.2 Reproducible release manifest — **✅ generator shipped (draft 6); signing deferred**

- Each tagged release publishes a `RELEASES.md` line with the SHA-256 of every shipped file (or a single SHA-256 of a deterministic tarball).
- Sign the manifest with a project minisign or sigstore key. Publish the public key in `SECURITY.md` and in the README.
- The build is essentially "copy files + run `build-i18n-variants.js` + run `build-sitemap.js`"; reproducibility is trivial.

**Status notes (draft 6):** `scripts/build-release-manifest.js` shipped. Generates an append-only `RELEASES.md` with the SHA-256 of every shipped file (index.html, the four locale variants, the four vendored .js libraries, all icons, sitemap.xml, robots.txt, _headers, site.webmanifest, plus optional artifacts encryptalotta-portable.html and sbom.json). Each entry records the git commit, tag (if any), working-tree cleanliness flag, and a manifest-fingerprint SHA-256 of all per-file hashes. RELEASES.md itself is *not* committed in this round — maintainers will regenerate at tagged-release time when the working tree is clean and the fingerprint is meaningful. Signing the manifest with minisign/sigstore is still deferred.

### 4.3 Software Bill of Materials (SBOM) — **✅ shipped (draft 6)**

- Publish a CycloneDX 1.5 JSON SBOM listing the vendored dependencies (`openpgp.min.js`, `qrcode.js`, `js-yaml.min.js`, `secrets.min.js`, and any new vendored deps from §2).
- The EU Cyber Resilience Act and NIS2 are pushing public-sector procurement toward requiring SBOMs. Having one available — even for a free static site — is a credibility signal.

**Status notes (draft 6):** `scripts/build-sbom.js` shipped. Output is `sbom.json` at the repo root, CycloneDX 1.5, listing all four vendored libraries with name + version + license + PURL + SHA-256/384/512 hashes + VCS URL + on-disk file path. The serial number is derived from the on-disk dep hashes, so re-running on unchanged inputs produces byte-identical output. The audit script (`scripts/audit-release.js`) cross-checks the SBOM's SHA-384 against the README manifest and the on-disk bytes — if a maintainer updates a vendored library without regenerating the SBOM, audit fails before the commit lands.

### 4.4 Tor onion mirror

- Stand up a v3 onion service mirroring the static files (cheap; the site is static).
- Add the onion address to `SECURITY.md` and the footer.
- This is high signal for the journalist/activist audience the site implicitly serves.

### 4.5 PWA installability and offline-precaching — **✅ shipped (draft 9)**

- Confirm the manifest at `site.webmanifest` declares `display: standalone` and the install icons.
- Add a minimal service worker that precaches the shell. Strict CSP — service worker scope must match.
- This makes the site genuinely "downloadable" on Android and desktop: users in censored or low-bandwidth regions can install once and use forever.

**Status notes (draft 9):** `sw.js` shipped at the repo root. ~60 lines, no dependencies. Precaches a 20-entry shell on install (the four locale roots + `/index.html` + `/privacy.html` + the four vendored libraries + the three favicons + `favicon.ico` + `site.webmanifest`) using `cache.addAll` with `cache: 'reload'` so the SW never installs a stale snapshot mid-flight. Cache-first on `fetch` for same-origin GETs only; cross-origin requests pass straight through (defense in depth — even though the site has no cross-origin scripts). Cache name is versioned (`encryptalotta-shell-v1`); bump at release time to invalidate. `activate` claims clients and drops old caches. Registration in `index.html` is HTTPS-gated and silent on failure (no UI prompt). The document CSP is unchanged — `connect-src 'none'` still applies to the page itself; a path-scoped `_headers` override grants `/sw.js` `connect-src 'self'` so install-time precache fetches resolve. The grant is narrow on purpose: the SW has no logic that would dial out elsewhere. Audit's CSP check still inspects the default (document) rule and continues to enforce `connect-src 'none'` on the page surface.

### 4.6 Single-file portable build — **✅ shipped (draft 6)**

- A script that produces `encryptalotta.html` — the entire site as a single HTML file with all JS/CSS/icons inlined as `data:` URIs.
- Audience: journalists in transit, USB-stick distribution, hostile-network environments.
- Output: hosted as a downloadable on the site itself.

**Status notes (draft 6):** `scripts/build-portable.js` shipped. Output is `encryptalotta-portable.html` at the repo root, ~1.3 MB self-contained. Inlines the four vendored .js libraries and the three favicons (apple-touch + 16x16 + 32x32 as base64 data URIs). Strips OpenGraph / Twitter card / canonical / hreflang metadata and the PWA manifest link (none of those are meaningful from `file://`). Banner timestamp is derived from the newest input mtime so successive builds with unchanged inputs produce byte-identical output (reproducibility-friendly). Verified end-to-end via Playwright smoke test loading from `file://` and exercising hash (BLAKE2b + SHA-256), IBAN MOD-97 validation, and the QR view (which proves the vendored qrcode.js inlined correctly). Audit script verifies no `<script src=>` references remain.

### 4.7 Submission to public-good registries

Targets, in order of fit:

- [Digital Public Goods Alliance registry](https://digitalpublicgoods.net/registry/) — explicit catalog of OSS serving SDGs.
- [Privacy Guides](https://privacyguides.org) tools list.
- [PRISM Break](https://prism-break.org).
- [EFF Surveillance Self-Defense](https://ssd.eff.org) — submit as a tool.
- `Lissy93/awesome-privacy` GitHub list.
- `awesome-selfhosted-non-self-hosted` and `awesome-static-website` lists.
- [Public Sector Tech Watch](https://joinup.ec.europa.eu/collection/public-sector-tech-watch) — EU Commission's OSS catalog.
- Tactical Tech and Access Now resource directories.

---

## 5. Framing and discoverability

### 5.1 GDPR-by-design page — **✅ shipped English-only (draft 8); localizations deferred**

A `/privacy` page (and translations) that states, in plain language: *no data ever leaves your browser; no cookies; no analytics; no CDN; no fonts loaded from third parties; nothing is stored server-side because there is no server.* This is trivially true for this site and is exactly what EU procurement and compliance teams literally search for. Same content per locale.

**Status notes (draft 8):** Shipped as a standalone static page at `/privacy.html` (English). The page is intentionally minimal: ~200 lines of self-contained HTML + inline CSS, no JavaScript, light/dark via `prefers-color-scheme`, no external assets. Content is structured around what the site does not do, what is stored on the device, what the static host sees, GDPR/CCPA framing, and recommendations for high-risk users — with links out to SECURITY.md / THREAT-MODEL.md / the SBOM / the GitHub repo. Footer link added to the SPA with the i18n key `footer.privacy` translated for all five locales (en/fr/de/zh-CN/hi); the linked page itself is English-only for v1, with localized variants deferred per the §0.6 "translation quality > breadth" rule. Sitemap updated with a single `/privacy.html` entry at priority 0.5 / yearly changefreq.

### 5.2 Security and threat-model documents — **✅ shipped (draft 7)**

- `SECURITY.md` — disclosure policy, contact (PGP-encrypted preferred), supported versions, known-not-applicable threat classes.
- `THREAT-MODEL.md` — what the tool does and does not protect against. Important caveat against overclaiming: *the site cannot protect you from a compromised browser, a compromised device, a malicious browser extension, or a network adversary that can MITM the site's load (mitigated by HTTPS but not eliminated).*

**Status notes (draft 7):** Both files shipped at the repo root. SECURITY.md covers the disclosure email (PGP-preferred), realistic single-maintainer response timelines, what counts as in-scope vs out-of-scope, and the release-verification chain (RELEASES.md, sbom.json, portable build, audit script). THREAT-MODEL.md is structured around four sections: what the site is, what it protects against, what it does not (with explicit mitigations for high-risk users), and recommendations by user type. Both documents avoid the security-marketing register — they are conservative on purpose so users don't decide what to type into the tool based on overclaimed guarantees.

### 5.3 Regional `hreflang` aliases — **✅ shipped for English (draft 8); Spanish/Portuguese/Chinese deferred until those locales ship**

For locales that have meaningful regional splits in date/number conventions, add `hreflang` entries pointing at the same content:

- `en-GB`, `en-IE`, `en-IN`, `en-AU`, `en-NZ`, `en-ZA` → `/`
- `pt-BR` → `/pt-br/`, `pt-PT` → `/pt-pt/`
- `es-ES` → `/es/`, `es-MX` → `/es/`, `es-AR` → `/es/` (until separate variants ship)
- `zh-CN` → `/zh/`, `zh-TW` → `/zh-tw/` (when it ships)

**Status notes (draft 8):** Six regional English aliases (`en-GB`, `en-IE`, `en-AU`, `en-NZ`, `en-ZA`, `en-IN`) added to `<link rel="alternate" hreflang>` in `index.html` and to each `<url>` block in `sitemap.xml` via `scripts/build-sitemap.js`. All six point to the canonical English root. Variants inherit the same hreflang block via `build-i18n-variants.js`. Spanish and Portuguese regional aliases are deferred until the `es` / `pt-BR` / `pt-PT` locales themselves ship (Tier 1 roadmap, §1.2); `zh-TW` deferred per the same logic.

### 5.4 Localized OpenGraph and Twitter cards

`scripts/build-i18n-variants.js` already does this for the five existing locales. Confirm new locales inherit the same treatment, with translated `og:title`, `og:description`, `og:locale`, and `twitter:title`/`twitter:description`.

---

## 6. Phased rollout

Each phase is independent. Stop after any phase; nothing below requires the next.

### Phase 1 — i18n infrastructure (no new tools yet)

1. ✅ Logical-property CSS refactor for RTL readiness (§1.4) — shipped draft 13. RTL runtime flip + icon mirroring + LTR code-block embedding deferred until the first RTL locale is queued.
2. ✅ `Intl.PluralRules` / `Intl.RelativeTimeFormat` / `Intl.ListFormat` integration (§1.5) — three slices shipped (drafts 11, 12, 14). `Intl.NumberFormat` + `Intl.RelativeTimeFormat` drive PBKDF2 timing, CIDR address counts, PGP key dates, and the Unix-timestamp relative phrasing. `Intl.PluralRules` powers the regex match-count row. `Intl.ListFormat` powers the BIC multi-tag row. Remaining `.join(', ')` sites are technical lists where the locale's "and" / "und" / "et" final-joiner would feel wrong; the passphrase-strength prose case is deferred until its issue strings are translated.
3. 🟡 Command-palette synonym field added to the `STRINGS` schema (§1.6) — palette + 42 English synonym blocks + 3 localized UI strings × 5 locales shipped draft 15. Locale-specific synonyms (`chiffrer` / `cifrar` / etc.) deferred until §1.3 native reviewers are sourced.
4. ✅ CSV schema extended for plural categories (§1.5) — convention formalized in draft 25. Plural-suffixed keys follow the six CLDR rule names (`.zero|.one|.two|.few|.many|.other`); audit-release.js enforces that every group defines `.other` in en (the universal Intl.PluralRules fallback). Non-en locales may omit rules their grammar does not use. Convention documented inline at the top of `scripts/export-strings-csv.js`.

### Phase 2 — Tier 1 locales

1. Spanish (`es`), Italian (`it`), Polish (`pl`), Portuguese (`pt-BR`).
2. Each goes through all 7 translation passes (§1.3) before shipping.
3. Locale picker UI confirms readability of all option labels in their own scripts.

### Phase 3 — First RTL locale

1. Arabic (`ar`). RTL pass (Pass 5 in §1.3) is mandatory.
2. Validates the §1.4 architectural refactor end-to-end.
3. Hebrew, Persian, Urdu become near-free follow-ups.

### Phase 4 — Quick-win tools

1. ✅ IBAN validator/generator (§2.1) — landed draft 2.
2. ✅ BIC/SWIFT checker (§2.2) — landed draft 2.
3. ✅ EU VAT validator (§2.3) — landed draft 3.
4. ✅ GS1 barcode (§2.13) — landed draft 2.
5. ✅ BLAKE2b (§2.12) — landed draft 5; BLAKE3 deferred (see §2.12 status notes).
6. ✅ Ed25519 / X25519 standalone (§2.10) — landed draft 4.

All ~1-day implementations. Roughly doubles the site's tool count for a small fraction of the effort.

**Progress (draft 2):** IBAN + BIC + GS1 shipped together. Tool count went from 36 → 39. New functional tests in `tests/specs/01-tools.spec.js` (8 cases) and a11y coverage in `tests/specs/05-a11y.spec.js` (3 views × 2 themes = 6 cases). JSON-LD `featureList` updated to 39 entries; `audit-release.js` passes; full Playwright suite passes (170 tests).

**Progress (draft 3):** EU VAT validator shipped. Tool count 39 → 40. 6 new functional tests (real public VATs for DE/FR/IT + NL format-only + ES NIF rejection + DE bad-checksum). 2 new a11y tests (light + dark). JSON-LD `featureList` updated to 40 entries; STRINGS now 1014 keys × 5 locales; audit passes; full new-tool test suite (22 tests including all of IBAN/BIC/GS1/VAT) passes.

**Progress (draft 4):** Ed25519 + X25519 shipped as two sister tools. Tool count 40 → 42. 6 new functional tests using RFC 8032 §7.1 (vectors 1 & 2), RFC 7748 §6.1, plus a tamper-rejection and HKDF derivation. 4 new a11y tests. JSON-LD `featureList` updated to 42 entries; STRINGS now 1071 keys × 5 locales; gzipped page weight 0.33 MB (still well under the 2 MB budget). Audit passes.

**Progress (draft 5):** BLAKE2b-512 added to the existing Hash & Checksum tool (no new view, just an extra algorithm row). Tool count remains 42 because BLAKE2b is integrated into an existing tool. Phase 4 quick-wins now complete except BLAKE3 (deferred). Phase 6 regulator presets shipped in the PGP key generator: dropdown for BSI TR-02102-1, ANSSI RGS B1, NIST SP 800-57, CNSA 2.0, and Privacy Guides recommended; each preset auto-fills algorithm + keysize and surfaces a footnote linking to the source document. Manually editing algorithm/keysize resets the preset to "Custom" so the UI never lies about which regulator's choice is currently selected. en/fr/de UI strings reviewed; zh-CN/hi machine-quality drafts. 5 new functional tests (2 BLAKE2b RFC 7693 + 3 regulator preset behavior); full suite 206 passed.

**Progress (draft 6):** Phase 5 distribution scripts shipped. Three new generators, all deterministic, all audit-cross-checked:

- `scripts/build-portable.js` → `encryptalotta-portable.html` (~1.3 MB single-file build, vendored .js + favicons inlined, social-card / canonical / PWA metadata stripped). Banner timestamp derived from input mtimes so builds are byte-reproducible. Smoke-tested via Playwright loading from `file://`.
- `scripts/build-sbom.js` → `sbom.json` (CycloneDX 1.5, four library components with SHA-256/384/512 + PURL + license + VCS URL). Deterministic serial number derived from input hashes — same inputs produce byte-identical output.
- `scripts/build-release-manifest.js` → `RELEASES.md` (append-only, one section per snapshot, SHA-256 of every shipped file plus a manifest-fingerprint hash, captures git commit + tag + working-tree cleanliness). Not committed in this round — maintainers will regenerate at tagged-release time when the tree is clean.

`scripts/audit-release.js` extended to validate both artifacts: SBOM hashes are cross-checked against on-disk vendored bytes; the portable build is verified to contain zero `<script src=>` references. Both checks are soft — they only fire if the artifact is present, so contributors aren't forced to regenerate on every commit.

No runtime changes; full Playwright suite still 206 passed.

**Progress (draft 85):** §2.7 PEPPOL UBL inspector gains a `cac:PaymentMeans/cbc:PaymentDueDate` xs:date format check — paired with draft 84 on the SEPA side. EN 16931 BT-9 (Payment due date) is carried at two distinct UBL anchors: the document root `cbc:DueDate` (covered for form by draft 75) and on each `cac:PaymentMeans` block as `cbc:PaymentDueDate`. Both shapes are plain `xs:date`. Multi-method invoices (e.g. BACS + card + standing order) carry one PaymentMeans block per method, and each may declare its own PaymentDueDate when settlement timings differ across rails — none of those values are exercised by the existing root-date gate. Same canonical failure modes as draft 75 — US-style `04/01/2024`, slash-separated `2024/05/01`, datetime spilled into the date slot — all schema-valid plain text but rejected by PEPPOL access points at the xs:date schematron gate. The new `ublCheckPaymentDueDate()` walks every `<cac:PaymentMeans>` block, reads each `<cbc:PaymentDueDate>` (trimmed), runs the same `UBL_DATE_RE` regex + month/day sanity gate that drafts 75 and 81 already use, and emits one green `✓ PaymentMeans/PaymentDueDate values all match the xs:date YYYY-MM-DD form across N PaymentMeans block(s) (EN 16931 BT-9 / PEPPOL BIS Billing 3.0)` row when every value is clean or one red `✗ PaymentMeans/PaymentDueDate "{v}" does not match the xs:date YYYY-MM-DD form …` row per offending value. Skips silently when no PaymentMeans block carries a PaymentDueDate — BT-9 may live solely at the root, in which case draft 75 already covers it. Together with drafts 75 (root-level dates + InvoicePeriod bounds) and 81 (Delivery/ActualDeliveryDate) this closes out the four xs:date surfaces in EN 16931 / PEPPOL BIS Billing 3.0. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 / xs:date vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with a PaymentMeans block carrying `PaymentDueDate=2024-05-01` injected → confirmation citing the 1-block count; same fixture with `05/01/2024` (US-style MM/DD/YYYY) → mismatch row citing the canonical xs:date form). Audit OK. Full SEPA+UBL suite: 120 → 124 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 84):** §2.4 SEPA inspector gains a per-transaction `PmtId/InstrId` ISO 20022 Max35Text length check — paired with draft 85 on the UBL side, and the third in the document-/transaction-level identifier-length series alongside drafts 80 (MsgId) and 82 (EndToEndId). ISO 20022 declares `InstrId` as `Max35Text`, a 35-codepoint schema cap; draft 53 covers the orthogonal EPC SEPA character-set rule on the same field but does not validate length. InstrId is the originator-side instruction key the EPC SCT/SDD Rulebooks describe as a free-text identifier set by the initiating party for its own bookkeeping — it is commonly populated from internal batch / sequence references that may exceed 35 chars when stitched together from multiple sub-keys (e.g. `BATCH-{YYYY-Qn}-SEQ-{nnnnnn}-SUBKEY-{ref}` busts the cap with a few moderately-long sub-references). Schema-invalid for ISO 20022 (clearing systems reject the file at the xs:Max35Text gate before any rail-level check fires). The new `sepaCheckInstrIdLength()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` in pain.001 / pain.008 wrappers, measures each `PmtId/InstrId` via `[...id].length`, and emits one green `✓ InstrId values fit within ISO 20022 Max35Text across all N transaction(s) (each ≤35 characters)` row when every value fits or one red `✗ InstrId "{id}" exceeds ISO 20022 Max35Text ({n}/35 characters) — clearing systems reject the file at the schema-validation gate …` row per offending value. Together with drafts 53 (charset) this closes both schema-level constraints on the originator-side instruction identifier; with drafts 80 (MsgId) and 82 (EndToEndId) the three free-text identifier surfaces ISO 20022 caps to Max35Text now all carry both schema-level gates. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical pain.001 fixture with `InstrId=INSTR-000001` (12 chars) injected → confirmation citing the 1-transaction count; same fixture with a 41-char internal batch key injected → mismatch row citing the 41/35 over-cap). Audit OK. Full SEPA+UBL suite: 120 → 124 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 83):** §2.7 PEPPOL UBL inspector gains a `cac:Delivery/cac:DeliveryLocation/cac:Address/cac:Country/cbc:IdentificationCode` ISO 3166-1 alpha-2 check — paired with draft 82 on the SEPA side. EN 16931 BT-80 (Deliver-to country code) names this element; PEPPOL BIS Billing 3.0 narrows the value to a current ISO 3166-1 alpha-2 code. UBL 2.1 declares `cbc:IdentificationCode` as plain text with no enumeration, so the same failure modes that drove drafts 68 (party-address country, BT-40 / BT-55) and 71 (item country of origin, BT-159) apply at the delivery-location layer too — `UK` (not an ISO code — Great Britain is `GB`), `EN`, `XX`, or trailing-whitespace artefacts pass the schema but break PEPPOL routing. Distinct from drafts 68 and 71 in scope: those gates cover the supplier/customer postal-address country and the per-line item country of origin; this gate covers the deliver-to country, which is the field a buyer's customs broker and the Intrastat statistical reporting both key off when an invoice declares delivery to a different jurisdiction than the buyer's billing address. The new `ublCheckDeliveryCountry()` drills into `cac:Delivery/cac:DeliveryLocation/cac:Address/cac:Country/cbc:IdentificationCode`, trims, and validates against the shared `ISO_3166_1_ALPHA2` set (data single-sourced across drafts 68 / 69 / 70 / 71 / 83). Emits one green `✓ Delivery country code … {code} is a valid ISO 3166-1 alpha-2 code (EN 16931 BT-80 / PEPPOL BIS Billing 3.0)` row on match, or one red `✗ Delivery country code … "{code}" is not a valid ISO 3166-1 alpha-2 code …` row otherwise. Skips silently when no Delivery / DeliveryLocation / Address / Country / IdentificationCode chain is present — BT-80 is optional per PEPPOL BIS Billing 3.0 and domestic invoices without a separate delivery address legitimately omit it. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with the full Delivery / DeliveryLocation chain and `IdentificationCode=DE` injected → confirmation citing BT-80; same chain with `UK` → mismatch row citing `GB` as the canonical alternative). Audit OK. Full SEPA+UBL suite: 116 → 120 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 82):** §2.4 SEPA inspector gains a per-transaction `PmtId/EndToEndId` ISO 20022 Max35Text length check — paired with draft 83 on the UBL side, and the transaction-level extension of draft 80's `GrpHdr/MsgId` length gate. ISO 20022 declares `EndToEndId` as `Max35Text`, a 35-codepoint schema cap; draft 51 covers the orthogonal EPC SEPA character-set rule on the same field but does not validate length. The canonical real-world failure is a CRM-supplied invoice / customer reference that exceeds 35 chars — e.g. `Customer-{UUID}` whose 36-char UUID alone busts the cap, or `INV-{YYYY-MM-DD}-{customer-name}` where a long company name pushes the value past the limit. Schema-invalid for ISO 20022 (clearing systems reject the file at the xs:Max35Text gate before any rail-level check fires), but commonly missed because the originator-side application never warned about the length. The new `sepaCheckE2eIdLength()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` in pain.001 / pain.008 wrappers, measures each `PmtId/EndToEndId` value via `[...id].length` (so surrogate pairs in pasted Unicode aren't double-counted), and emits one green `✓ EndToEndId values fit within ISO 20022 Max35Text across all N transaction(s) (each ≤35 characters)` row when every value fits or one red `✗ EndToEndId "{id}" exceeds ISO 20022 Max35Text ({n}/35 characters) — clearing systems reject the file at the schema-validation gate …` row per offending value. Transactions without an EndToEndId skip silently. Together with drafts 51 (charset) and 43 (uniqueness) this closes the three schema-level constraints on the transaction-level reconciliation identifier. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical pain.001 fixture with `EndToEndId=E2E-001` (7 chars) → confirmation citing the 1-transaction count; same fixture with a 40-char CRM-style reference injected → mismatch row citing the 40/35 over-cap). Audit OK. Full SEPA+UBL suite: 116 → 120 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 81):** §2.7 PEPPOL UBL inspector gains a `cac:Delivery/cbc:ActualDeliveryDate` (EN 16931 BT-72) xs:date format check — paired with draft 80 on the SEPA side. UBL 2.1 declares the field as plain `xs:date`, which requires the canonical `YYYY-MM-DD` form; draft 75 covered the root-level date surface (IssueDate / DueDate / TaxPointDate) plus the InvoicePeriod bounds, and this gate extends the same regex (`^\d{4}-\d{2}-\d{2}$` with month 01-12 / day 01-31 sanity) to the Delivery block, which sits one level deeper at `cac:Delivery`. The canonical real-world failure modes mirror draft 75 but at the delivery-date layer: US-style `04/15/2024`, slash-separated `2024/04/15`, or a datetime spilled into the date slot (`2024-04-15T00:00:00`) — all schema-valid plain text but rejected by PEPPOL access points at the xs:date schematron gate. The new `ublCheckDeliveryDate()` reads `cac:Delivery/cbc:ActualDeliveryDate` (trimmed), validates against the existing `UBL_DATE_RE`, and emits one green `✓ Delivery/ActualDeliveryDate {v} matches the xs:date YYYY-MM-DD form (EN 16931 BT-72 / PEPPOL BIS Billing 3.0)` row on match or a red `✗ Delivery/ActualDeliveryDate "{v}" does not match the xs:date YYYY-MM-DD form …` row otherwise. Skips silently when no Delivery block (or no ActualDeliveryDate child) is present — BT-72 is optional per PEPPOL BIS Billing 3.0 and over-the-counter / digital-only supplies legitimately omit it. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 / xs:date vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `Delivery/ActualDeliveryDate=2024-04-15` injected → confirmation citing BT-72; same fixture with `04/15/2024` (US-style MM/DD/YYYY) injected → mismatch row citing the canonical xs:date form). Audit OK. Full SEPA+UBL suite: 112 → 116 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 80):** §2.4 SEPA inspector gains a `GrpHdr/MsgId` ISO 20022 Max35Text length check — paired with draft 81 on the UBL side, and complementary to draft 49's character-set check on the same field. ISO 20022 declares `MsgId` as `Max35Text`, a 35-codepoint schema cap; draft 49 covers the orthogonal EPC SEPA character-set rule on the same field but does not validate length. The canonical real-world failure is a generator that uses a full UUID (36 chars — exactly one over the cap) for the document identifier — schema-valid bytes for a CRM that issues UUIDs by default, but schema-invalid for ISO 20022 (clearing systems reject the file at the xs:Max35Text gate before any rail-level check fires). The new `sepaCheckMsgIdLength()` reads `GrpHdr/MsgId` on pain.001 / pain.008 wrappers (camt.053 is statement-side and out of scope), measures codepoint length via `[...msgId].length` so surrogate pairs in pasted Unicode aren't double-counted, and emits one green `✓ GrpHdr/MsgId {msgId} fits within ISO 20022 Max35Text ({n}/35 characters)` row when within the cap or a red `✗ GrpHdr/MsgId "{msgId}" exceeds ISO 20022 Max35Text ({n}/35 characters) — clearing systems reject the file at the schema-validation gate …` row otherwise. Together with draft 49 (charset) this closes both schema-level constraints on the document-level identifier. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical pain.001 fixture with `MsgId=MSG-2024-001` (12 chars) → confirmation citing the 12/35 ratio; same fixture with a 36-char UUID injected → mismatch row citing the 36/35 over-cap). Audit OK. Full SEPA+UBL suite: 112 → 116 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 79):** §2.7 PEPPOL UBL inspector gains a `cbc:BuyerReference` (BT-10) / `cac:OrderReference/cbc:ID` (BT-13) presence check (paired with draft 78's SEPA temporal-ordering gate). PEPPOL BIS Billing 3.0 PEPPOL-EN16931-R003 requires a PEPPOL invoice to declare either the buyer reference or the purchase-order reference — the two fields are alternates and at least one MUST be present so the buyer's accounts-payable team can match the invoice against an internal reference. UBL 2.1 marks both as optional at the schema level so an invoice missing both is schema-valid plain XML but PEPPOL access points reject at the PEPPOL-EN16931-R003 schematron gate; France's Chorus Pro public-sector portal additionally pins BuyerReference for B2G flows (the "Service Code" the central government uses to route the invoice to the right department). The new `ublCheckBuyerOrOrderRef()` reads both fields and emits one green confirmation row when at least one is present (specialised string by which field is declared — BuyerReference only, OrderReference only, or both), or one red `✗ Neither BuyerReference (BT-10) nor OrderReference/ID (BT-13) is declared — PEPPOL BIS Billing 3.0 PEPPOL-EN16931-R003 …` row when both are missing. Invoice-only — credit notes are exempt from PEPPOL-EN16931-R003. 4 new strings × 5 locales for the four surfacing modes (both / buyer-only / order-only / missing) — en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern. Two new Playwright cases (UBL fixture with BuyerReference=PO-2024-042 injected → confirmation citing BT-10; canonical fixture (neither declared) → mismatch row citing PEPPOL-EN16931-R003). Audit OK. Full SEPA+UBL suite: 108 → 112 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 78):** §2.4 SEPA inspector gains a temporal-ordering check between `GrpHdr/CreDtTm` (file creation timestamp, draft 74) and the per-PmtInf `ReqdExctnDt` / `ReqdColltnDt` (settlement date, draft 76) — paired with draft 79 on the UBL side. ISO 20022 doesn't forbid the inversion at the schema level, but clearing systems (EBA STEP2, STET, RT1) reject any pain.001 / pain.008 whose requested execution / collection date precedes the file creation timestamp because the settlement engine reads the gap as a back-dated instruction. The canonical real-world cause is a template repurposed from a prior cycle where `CreDtTm` was refreshed to today but `ReqdExctnDt` wasn't bumped forward, leaving last week's settlement date in place. Parallel in shape to draft 39's UBL DueDate ≥ IssueDate gate. The new `sepaCheckCreDtTmReqdDtOrder()` reads the date prefix of `GrpHdr/CreDtTm` (the `YYYY-MM-DD` prefix is stable across all valid ISO 20022 ISODateTime shapes — both the format and the xs:date pattern are pre-validated by drafts 74 and 76), walks every `<PmtInf>`, picks the right field by `detected.type`, unwraps the optional `<Dt>` child, parses both as Dates, and compares. Emits one green `✓ {field} is on or after GrpHdr/CreDtTm {cre} across all N PmtInf block(s) …` row across all matching blocks, or one red `✗ {field} ordering mismatch: PmtInf #i declares X which precedes GrpHdr/CreDtTm Y …` row per offending block keyed by the 1-based PmtInf index. With drafts 74 / 76 / 78 the SEPA inspector now covers both format (xs:dateTime + xs:date) and semantic ordering (CreDtTm ≤ settlement date) for every date-bearing surface in pain.001 / pain.008. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical pain.001 fixture with CreDtTm=2024-01-15 / ReqdExctnDt=2024-01-20 → confirmation citing the ordering; same fixture with `ReqdExctnDt=2024-01-10` → mismatch row citing the back-dated case and both timestamps). Audit OK. Full SEPA+UBL suite: 108 → 112 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 77):** §2.7 PEPPOL UBL inspector gains a `cbc:EndpointID` presence check on both `AccountingSupplierParty/Party` and `AccountingCustomerParty/Party` (paired with draft 76's SEPA `ReqdExctnDt` / `ReqdColltnDt` xs:date gate). EN 16931 BT-34 (Seller electronic address) / BT-49 (Buyer electronic address) and PEPPOL BIS Billing 3.0 BR-62 / BR-63 require every PEPPOL invoice to declare an electronic address for both parties via `cbc:EndpointID`; the address is what PEPPOL access points key off when routing the document through the SML / SMP discovery layer. A missing supplier or customer EndpointID means the document has no routing destination, and access points reject at the schematron gate before any content-level check fires. UBL 2.1 marks the element optional at the schema level so this is exactly the kind of presence gate this inspector is designed to surface; the canonical real-world cause is a UBL template repurposed from a non-PEPPOL flow where routing was handled out-of-band, leaving the original endpoint-free template in place when the invoice moved onto PEPPOL rails. The new `ublCheckEndpointIds()` reads `cbc:EndpointID` under each of `AccountingSupplierParty/Party` and `AccountingCustomerParty/Party`, and emits one green `✓ Both supplier and customer Party/EndpointID are declared (EN 16931 BT-34 / BT-49 / PEPPOL BIS Billing 3.0 BR-62 / BR-63)` row when both are present, or one red `✗ {party} Party/EndpointID is missing — PEPPOL BIS Billing 3.0 BR-62 / BR-63 requires …` row per missing party citing the specific rule. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with EndpointID injected on both parties → confirmation; same fixture with EndpointID injected on only the customer → mismatch row citing BR-62 for the supplier). Audit OK. Full SEPA+UBL suite: 104 → 108 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 76):** §2.4 SEPA inspector gains a strict xs:date format check on the per-PmtInf `ReqdExctnDt` (pain.001 requested execution date) / `ReqdColltnDt` (pain.008 requested collection date) — paired with draft 77 on the UBL side and a direct extension of draft 74's `GrpHdr/CreDtTm` ISODateTime gate from the header to the per-PmtInf settlement-date surface. ISO 20022 declares both as plain xs:date (`YYYY-MM-DD`) in pain.*.03-.08, and pain.001.001.09+ additionally wraps `ReqdExctnDt` in a `<Dt>` child (`<ReqdExctnDt><Dt>YYYY-MM-DD</Dt></ReqdExctnDt>`) — both shapes must hold the xs:date form. The canonical real-world failure modes mirror draft 75's UBL xs:date gate but at the settlement-date layer: US-style `04/15/2024`, slash-separated `2024/04/15`, or a datetime spilled into the date slot (`2024-04-15T00:00:00`) — all schema-valid plain text but rejected at clearing-system entry. The new `sepaCheckReqdDt()` walks every `<PmtInf>`, picks the right field (`ReqdExctnDt` for pain.001, `ReqdColltnDt` for pain.008) by `detected.type`, unwraps the optional `<Dt>` child, and validates against the same `^\d{4}-\d{2}-\d{2}$` regex from draft 75 plus month/day sanity (01-12 / 01-31). Emits one green `✓ {field} matches the xs:date YYYY-MM-DD form across all N PmtInf block(s) …` row across all matching blocks, or one red `✗ {field} mismatch: PmtInf #i declares "X" but ISO 20022 / EPC SEPA Rulebook require the canonical xs:date form …` row per offending block keyed by the 1-based PmtInf index. Together with draft 74 (CreDtTm xs:dateTime, header-level) and draft 75 (UBL xs:date, root + period), this closes out the date-format attack surface across both pain.* and UBL document families. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA / xs:date vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical pain.001 fixture with `ReqdExctnDt=2024-01-20` → confirmation citing all PmtInf blocks; same fixture with `01/20/2024` (US-style MM/DD/YYYY) → mismatch row citing the canonical xs:date form). Audit OK. Full SEPA+UBL suite: 104 → 108 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 75):** §2.7 PEPPOL UBL inspector gains a strict xs:date format check on the root-level date fields and the `InvoicePeriod` bounds (paired with draft 74's SEPA `CreDtTm` xs:dateTime gate). The existing `ublCheckDates()` (draft 39, BT-9 ≥ BT-2 temporal sanity) and `ublCheckInvoicePeriod()` (draft 47-era, EN 16931 BR-29) compare values via `Date.parse()`, which is lenient and accepts `04/01/2024`, `2024/04/01`, `2024-04-01T08:00:00`, and timestamp-with-time variants — schema-valid in some downstream parsers but rejected by PEPPOL access points at the xs:date schematron gate. The new `ublCheckDateFormats()` strictly validates every date-bearing surface — root `cbc:IssueDate` (BT-2), `cbc:DueDate` (BT-9), `cbc:TaxPointDate` (BT-7), plus the `cac:InvoicePeriod` `cbc:StartDate` / `cbc:EndDate` (BT-73 / BT-74) — against a single `^\d{4}-\d{2}-\d{2}$` regex with an additional sanity gate on month (01-12) and day (01-31) so `2024-13-01` doesn't slip through. Emits one green `✓ Date fields (IssueDate / DueDate / TaxPointDate / InvoicePeriod bounds) all match the xs:date YYYY-MM-DD form across all N field(s) (EN 16931 / PEPPOL BIS Billing 3.0)` row when every value is clean, or one red `✗ {field} "X" does not match the xs:date YYYY-MM-DD form …` row per offending value naming both the field and the canonical form. Pairs naturally with the existing temporal checks: this gate covers form (xs:date well-formedness), those cover ordering (DueDate ≥ IssueDate, EndDate ≥ StartDate) and presence — together they close out the EN 16931 / PEPPOL date-surface attack on the UBL inspector. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 / xs:date vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical UBL fixture with IssueDate=2024-04-01 / DueDate=2024-05-01 → confirmation citing all date fields; same fixture with IssueDate=04/01/2024 (US-style MM/DD/YYYY) injected → mismatch row citing the canonical xs:date form). Audit OK. Full SEPA+UBL suite: 100 → 104 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 74):** §2.4 SEPA inspector gains a strict ISO 20022 ISODateTime (xs:dateTime) format check on `GrpHdr/CreDtTm` (paired with draft 75 on the UBL side). The existing header rendering surfaces the raw `CreDtTm` value alongside the MsgId and InitgPty but does not validate its shape; ISO 20022 declares the field as ISODateTime, which requires the canonical `YYYY-MM-DDTHH:MM:SS[.fff][Z|±HH:MM]` form — T separator, hours/minutes/seconds, optional fractional seconds, optional explicit timezone designator. The canonical real-world failure modes are (a) a space separator (`2024-04-01 12:30:45` — common when the value is exported from a spreadsheet column that strips control characters), (b) a date-only value (`2024-04-01` — pasted from a different field), (c) US-style ordering (`04/01/2024 12:30:45`), and (d) bare timestamp text without separators (`20240401T123045`). All four pass downstream string consumers but break ISO 20022 schema validation at clearing-system entry — EBA STEP2, STET, and RT1 all reject the file at the xs:dateTime gate before the rail-level checks even begin. The new `sepaCheckCreDtTm()` reads `GrpHdr/CreDtTm` on pain.001 / pain.008 wrappers (camt.053 is statement-side and out of scope), tests it against the `SEPA_DATETIME_RE` regex, and emits one green `✓ GrpHdr/CreDtTm X matches ISO 20022 ISODateTime …` row on match or a red `✗ GrpHdr/CreDtTm "X" is not a valid ISO 20022 ISODateTime …` row otherwise. Header-only — per-PmtInf CreDtTm doesn't exist in pain.001 / pain.008. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / xs:dateTime vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical pain.001 fixture with `CreDtTm=2024-01-15T10:30:00` → confirmation citing the ISO 20022 form; same fixture with `2024-01-15 10:30:00` (space separator) → mismatch row citing the T separator + explicit timezone requirement). Audit OK. Full SEPA+UBL suite: 100 → 104 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 73):** §2.7 PEPPOL UBL inspector gains a `cac:FinancialInstitutionBranch/cbc:ID` BIC format check (EN 16931 BT-86 / PEPPOL BIS Billing 3.0; paired with draft 72's SEPA `CtgyPurp/Cd` gate). UBL 2.1 declares the supplier's BIC under `cac:PaymentMeans/cac:PayeeFinancialAccount/cac:FinancialInstitutionBranch/cbc:ID` (or the legacy `cac:FinancialInstitution/cbc:ID` shape) as plain Identifier text — any 8 / 11 / random-length string passes the schema, but PEPPOL BIS Billing 3.0 and ISO 9362 narrow the value to 8 or 11 alphanumeric characters with a fixed shape (4 bank, 2 country, 2 location, optional 3 branch). The canonical real-world failure modes are length errors (9 or 10 chars from a paste that lost or gained a character — `COBADEFF1` instead of `COBADEFFXXX`), case-folded values that hit the bank-code or location prefix, or a BBAN / routing number folded into the BIC slot by a template that mixes the two fields. Parallel in shape to the existing SEPA BIC cross-check (draft 27) and the UBL IBAN MOD-97 cross-check (draft 36): a new `ublCollectBics()` walks every `<cac:FinancialInstitutionBranch>` and `<cac:FinancialInstitution>` under PaymentMeans, dedupes + uppercases the nested `<cbc:ID>` value, and `ublCheckBics()` runs each through the same `bicValidate()` that powers the standalone BIC tool. Emits one green `✓ Payment-service-provider BICs (FinancialInstitutionBranch/ID) are all valid ISO 9362 across all N BIC(s) (EN 16931 BT-86 / PEPPOL BIS Billing 3.0)` row when every value is clean, or one red `✗ FinancialInstitutionBranch/ID "X" is not a valid ISO 9362 BIC …` row per offending value. Skips silently when no FinancialInstitutionBranch/ID is present — bank-coordinate-free invoices (PaymentMeansCode 48 / 54 / 55, card-payment routes) legitimately omit it. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / ISO 9362 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `COBADEFFXXX` (Commerzbank, ISO 9362-valid) injected → confirmation citing BT-86; same fixture with `COBADEFF1` (9-char, wrong length) → mismatch row citing the ISO 9362 shape). Audit OK. Full SEPA+UBL suite: 96 → 100 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 72):** §2.4 SEPA inspector gains a `PmtTpInf/CtgyPurp/Cd` validation against the ISO 20022 ExternalCategoryPurpose1Code set (paired with draft 73 on the UBL side). The optional `<CtgyPurp>/<Cd>` element under `<PmtInf>/<PmtTpInf>` classifies the high-level purpose of every transaction in the block — salary (`SALA`), supplier payment (`SUPP`), tax (`TAXS`), VAT (`VATX`), pension (`PENS`), and so on across ~34 canonical codes. ISO 20022 declares the field as `ExternalCategoryPurpose1Code` (open enumeration governed by the ISO 20022 ExternalCodeSets list, refreshed quarterly), so a typo against the closest valid code (`SALR` for `SALA`, `SUPPL` for `SUPP`) is schema-valid plain text but breaks downstream automation — clearing systems and regulator-side reporting (wage-statement matching, VAT-payment routing, social-security reconciliation) key off these codes, and a silent typo means a payment marked `SALR` won't show up in the wage-payment register the receiving country's tax authority pulls from. The new `sepaCheckCtgyPurp()` walks every `<PmtInf>` in pain.001 / pain.008 wrappers, reads each block's `PmtTpInf/CtgyPurp/Cd` (case-folded + whitespace-stripped for paste-safety), and validates against a newly inlined `EXTERNAL_CATEGORY_PURPOSE` set (34 codes, ISO 20022 ExternalCodeSets 2024-Q4). Emits one green `✓ CtgyPurp/Cd is a valid ISO 20022 ExternalCategoryPurpose1Code across all N PmtInf block(s) …` row across all matching blocks, or one red `✗ CtgyPurp/Cd mismatch: PmtInf #i declares X but ISO 20022 ExternalCategoryPurpose1Code allows only the canonical set …` row per offending block keyed by the 1-based PmtInf index. Parallel in shape to `sepaCheckSvcLvl` (draft 44) and `sepaCheckSeqTp` (draft 46): per-PmtInf gate, optional element so absent blocks skip silently. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `CtgyPurp/Cd=SALA` → confirmation citing the canonical set; same fixture with `CtgyPurp/Cd=SALR` → mismatch row citing the typo and the canonical codes). Audit OK. Full SEPA+UBL suite: 96 → 100 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 71):** §2.7 PEPPOL UBL inspector gains a `cac:Item/cac:OriginCountry/cbc:IdentificationCode` ISO 3166-1 alpha-2 check (paired with draft 70 on the SEPA side; both reuse the inlined 249-entry `ISO_3166_1_ALPHA2` set first shipped with draft 68). EN 16931 BT-159 (Item country of origin) names this element for cross-border supplies, customs clearance, and Intrastat statistical reporting; PEPPOL BIS Billing 3.0 access points reject any value that is not a current ISO 3166-1 alpha-2 code at the schematron gate. UBL 2.1 declares `cbc:IdentificationCode` as plain text with no enumeration, so the same failure modes as draft 68's party-level check apply at the line/item level — `UK` (not an ISO code — Great Britain is `GB`), `EN`, `XX`, or trailing-whitespace artefacts pass the schema but break PEPPOL routing. Distinct from draft 68 in scope: that gate covers the supplier and customer postal-address country codes (BT-40 / BT-55); this gate covers per-line item country of origin (BT-159), which is the field a buyer's customs broker reads when an invoice crosses a border. The new `ublCheckOriginCountry()` walks every `<cac:InvoiceLine>` and `<cac:CreditNoteLine>`, drills into `cac:Item/cac:OriginCountry/cbc:IdentificationCode`, trims + uppercases, and validates against the shared set. Emits one green `✓ Item country-of-origin codes (Item/OriginCountry/IdentificationCode) are all valid ISO 3166-1 alpha-2 across all N line(s) (EN 16931 BT-159 / PEPPOL BIS Billing 3.0)` row when every value is clean, or one red `✗ Item/OriginCountry/IdentificationCode "X" is not a valid ISO 3166-1 alpha-2 code …` row per offending code. Skips silently when no item declares `cac:OriginCountry` — BT-159 is optional per PEPPOL BIS Billing 3.0 and domestic-only supplies legitimately omit it. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `Item/OriginCountry/IdentificationCode=DE` injected → confirmation citing BT-159; same fixture with `UK` injected → mismatch row citing `GB` as the canonical alternative). Audit OK. Full SEPA+UBL suite: 92 → 96 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 70):** §2.4 SEPA inspector gains a `Dbtr/Cdtr CtryOfRes` ISO 3166-1 alpha-2 check (parallel to the UBL Item OriginCountry/IdentificationCode check shipped alongside as draft 71; both reuse the inlined 249-entry `ISO_3166_1_ALPHA2` set first shipped with draft 68 + draft 69). ISO 20022 declares the optional `<CtryOfRes>` element (PartyIdentification245.CtryOfRes) under Dbtr / Cdtr as type CountryCode with the pattern `[A-Z]{2}`, so any two uppercase letters pass the schema; the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks narrow the value to a valid ISO 3166-1 alpha-2 code. Same canonical failure as draft 69's `PstlAdr/Ctry` gate (`UK` for `GB`), but at a different ISO 20022 anchor — `CtryOfRes` is the party's country of residence rather than the country of the postal address, so a natural-person debtor resident in Germany but using a UK correspondence address has two distinct country-code surfaces the EPC rulebooks pin. The new `sepaCheckCtryOfRes()` walks every `<CtryOfRes>` reachable in the pain.001 / pain.008 wrapper (camt.053 is statement-side and out of scope), trims + uppercases, and validates against the shared set. Emits one green `✓ Country of residence codes (Dbtr/Cdtr CtryOfRes) are all valid ISO 3166-1 alpha-2 across all N code(s) …` row when every value is clean, or one red `✗ CtryOfRes "X" is not a valid ISO 3166-1 alpha-2 code …` row per offending value. Together with draft 69's `PstlAdr/Ctry` gate, this covers the two distinct ISO 20022 country-code surfaces an originator declares in a pain.001 / pain.008 file. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `Dbtr/CtryOfRes=DE` → confirmation; same fixture with `UK` injected → mismatch row citing the offending value and `GB` as the canonical alternative). Audit OK. Full SEPA+UBL suite: 92 → 96 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 69):** §2.4 SEPA inspector gains a `PstlAdr/Ctry` ISO 3166-1 alpha-2 check (parallel to the UBL Country/IdentificationCode check shipped alongside as draft 68). ISO 20022 declares `Ctry` as type CountryCode with the pattern `[A-Z]{2}`, so any two uppercase letters pass the schema; the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks narrow the value to a valid ISO 3166-1 alpha-2 code. The canonical real-world failure is `UK` (not an ISO code — Great Britain is `GB`) folded into a Dbtr or Cdtr postal address from a template repurposed across SEPA and non-SEPA flows; rail-level processors (EBA STEP2, STET, RT1) reject the value at the country-code gate so the inspector should surface it before the file leaves the originator. The new `sepaCheckCtry()` walks every `<Ctry>` reachable in the pain.001 / pain.008 wrapper (camt.053 is statement-side and out of scope), trims + uppercases, and validates against a newly inlined `ISO_3166_1_ALPHA2` set (249 codes, 2025 list — shared with draft 68's UBL counterpart so the data is single-sourced). Emits one green `✓ Country codes (PstlAdr/Ctry) are all valid ISO 3166-1 alpha-2 across all N code(s) …` row when every value is clean, or one red `✗ PstlAdr/Ctry "X" is not a valid ISO 3166-1 alpha-2 code …` row per offending value. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `Dbtr/PstlAdr/Ctry=DE` → confirmation; same fixture with `UK` injected → mismatch row citing the offending value and `GB` as the canonical alternative). Audit OK. Full SEPA+UBL suite: 88 → 92 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 68):** §2.7 PEPPOL UBL inspector gains a `cac:PostalAddress/cac:Country/cbc:IdentificationCode` ISO 3166-1 alpha-2 check (paired with draft 69 on the SEPA side; both share an inlined 249-entry `ISO_3166_1_ALPHA2` set). EN 16931 BT-40 (supplier country) / BT-55 (customer country) and PEPPOL BIS Billing 3.0 BR-08 / BR-55 require each party's postal address to carry a country code that is a valid ISO 3166-1 alpha-2 code; UBL 2.1 declares `cbc:IdentificationCode` as plain text with no enumeration, so `UK` (not an ISO code — Great Britain is `GB`), `EN`, `XX`, or trailing-whitespace artefacts pass the schema but are rejected by PEPPOL access points at the schematron gate. The new `ublCheckCountryCode()` walks `AccountingSupplierParty` and `AccountingCustomerParty`, drills into `Party/PostalAddress/Country/IdentificationCode`, trims + uppercases, and validates against the shared set. Emits one green `✓ Party country codes (PostalAddress/Country/IdentificationCode) are all valid ISO 3166-1 alpha-2 across all N party address(es) (EN 16931 BT-40 / BT-55 / PEPPOL BIS Billing 3.0)` row when every value is clean, or one red `✗ Country/IdentificationCode "X" is not a valid ISO 3166-1 alpha-2 code …` row per offending code. Skips silently when neither party declares a `PostalAddress/Country/IdentificationCode` — UBL 2.1 marks the wrapping elements optional, and PEPPOL BR-08 / BR-55 absence is caught by upstream schema validation. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with supplier `Country/IdentificationCode=DE` injected → confirmation citing BT-40 / BT-55; same fixture with `UK` injected → mismatch row citing `GB` as the canonical alternative). Audit OK. Full SEPA+UBL suite: 88 → 92 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 67):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check (drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 / 63 / 65) from identifiers, Ustrd, party Nm, AdrLine, and TwnNm to the postal-code field (`PstlAdr/PstCd`) — the last of the three free-text postal-address surfaces the EPC rulebooks pin to the basic SEPA character set. ISO 20022 declares PstCd as Max16Text so any Unicode codepoints up to 16 chars pass the schema, but the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks both require the basic SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) to hold across every free-text field, postal codes included. Most country postal codes are ASCII digit / letter+digit combinations (UK: `SW1A 1AA`, NL: `1234 AB`, CA: `K1A 0B1`, DE: `10115`) that pass trivially, but rare cases carry non-ASCII characters via paste — typically when a postcode is folded into the address line of a template that mixes form fields, or when a downstream system substitutes a `§` for a non-printable separator. The new `sepaCheckPstCdCharset()` walks every `<PstCd>` reachable anywhere in the pain.001 / pain.008 wrapper (camt.053 is statement-side and out of scope), trims, tests against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ Postal codes (PstlAdr/PstCd) use only EPC SEPA Rulebook allowed characters across all N code(s) …` row when every value is clean, or one red `✗ PstlAdr/PstCd "X" contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value. With drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 / 63 / 65 / 67 the EPC character-set check now covers MsgId, EndToEndId + InstrId, MndtId, PmtInfId, Ustrd, party Nm, AdrLine, TwnNm, and PstCd — nine free-text surfaces in pain.001 / pain.008, closing out every free-text field the EPC rulebook pins to the basic SEPA character set. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `Dbtr/PstlAdr/PstCd=10115` → confirmation; same fixture with `10115§` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full SEPA+UBL suite: 84 → 88 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 66):** §2.7 PEPPOL UBL inspector gains a `TaxCurrencyCode` (BT-6) check against the ISO 4217 active-currencies set — the optional element at the root of the UBL document that names the VAT-accounting currency when an invoice declares VAT in a different currency than its document currency (e.g. a SEK invoice declaring EUR-denominated VAT for a cross-border supply where VAT must be reported in the destination-state currency). UBL 2.1 declares `cbc:TaxCurrencyCode` as an open ISO-4217 enumeration so any text passes the schema, including typos (`ERU` for `EUR`) and historical codes (`ITL` Italian lira, `DEM` German mark, `FRF` French franc) — PEPPOL access points reject anything outside the current ISO 4217 active set at the schematron gate. Parallel to draft 31's `@currencyID` check but scoped to the root element only; surfaces a small but distinct gate because BT-6 is the field that governs the per-rate TaxAmountCurrency split when present, and an invalid code silently breaks the multi-currency reconciliation that BT-6 is supposed to enable. Distinct from the DocumentCurrencyCode consistency check (draft 64, which validates that every `@currencyID` matches BT-5): that gate covers intra-document agreement; this gate covers the BT-6 element's own ISO 4217 validity. The new `ublCheckTaxCurrencyCode()` reads `cbc:TaxCurrencyCode` at the root (trimmed, uppercased), looks it up in the existing inline `ISO_4217_ACTIVE` set (≈ 180 codes, same source as drafts 29 / 31), and emits one green `✓ TaxCurrencyCode X is a current ISO 4217 active code (EN 16931 BT-6 / PEPPOL BIS Billing 3.0)` row on a hit, or one red `✗ TaxCurrencyCode X is not active per ISO 4217 — EN 16931 BT-6 / PEPPOL BIS Billing 3.0 requires a current ISO 4217 code …` row on a miss. Skips silently when the element is absent — BT-6 is optional per PEPPOL BIS Billing 3.0 and most single-currency invoices don't declare it. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical UBL fixture with `TaxCurrencyCode=EUR` injected at the root → confirmation citing BT-6; same fixture with `TaxCurrencyCode=ITL` (historical Italian lira) → mismatch row citing the schematron gate). Audit OK. Full SEPA+UBL suite: 84 → 88 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 65):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check (drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 / 63) from identifiers, Ustrd, party Nm, and AdrLine to town-name fields (`PstlAdr/TwnNm`). ISO 20022 declares TwnNm as Max35Text so any Unicode codepoints up to 35 chars pass the schema, but the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks both pin the basic SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) across every free-text field, town names included. Common European town names with accented characters (`München`, `Köln`, `Düsseldorf`, `Genève`, `Zürich`, `Linköping`) are schema-valid but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the accented characters (so the counterparty bank shows a mangled town on the rail statement) or hard-reject at the rail-level character-set gate. The new `sepaCheckTwnNmCharset()` walks every `<TwnNm>` reachable anywhere in the pain.001 / pain.008 wrapper (camt.053 is statement-side and out of scope), trims, tests against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ Town names (PstlAdr/TwnNm) use only EPC SEPA Rulebook allowed characters across all N town(s) …` row when every value is clean, or one red `✗ PstlAdr/TwnNm "X" contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value. Naturally pairs with the AdrLine check from draft 63 to cover both common postal-address free-text surfaces. With drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 / 63 / 65 the EPC character-set check now covers MsgId, EndToEndId + InstrId, MndtId, PmtInfId, Ustrd, party Nm, AdrLine, and TwnNm — eight free-text surfaces in pain.001 / pain.008 that the EPC rulebook pins to the basic SEPA character set. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `Dbtr/PstlAdr/TwnNm=Berlin` → confirmation; same fixture with `München` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full SEPA+UBL suite: 80 → 84 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 64):** §2.7 PEPPOL UBL inspector gains a `DocumentCurrencyCode` (BT-5) consistency check against every `@currencyID` attribute in the document (EN 16931 BR-CO-04). UBL 2.1 declares `@currencyID` on every `*Amount` element as an open ISO-4217 enumeration, so an invoice can technically carry mixed currencies (e.g. line amounts in EUR and the tax breakdown in USD) and still pass the schema — but PEPPOL access points reject the document at the BR-CO-04 schematron gate. The canonical real-world cause is a template repurposed across multi-currency flows where the bulk replace missed one `currencyID`, or a copy-paste of a tax-breakdown block from a different invoice. Distinct from the ISO 4217 validity check (draft 31): that catches typos (`ERU` for `EUR`) and historical codes (`ITL`, `DEM`); this catches intra-document inconsistency (every code is a valid current ISO 4217 code but they don't all match BT-5). The new `ublCheckCurrencyConsistency()` reads the root `<cbc:DocumentCurrencyCode>`, walks every `@currencyID` attribute reachable in the DOM (the same collector used by `ublCheckCurrencies()`), and emits one green `✓ All monetary @currencyID attributes match the document currency X (EN 16931 BR-CO-04 / PEPPOL BIS Billing 3.0)` row on full agreement, or one red `✗ Monetary @currencyID X does not match the document currency Y — EN 16931 BR-CO-04 …` row per offending code. Skips silently when BT-5 itself is absent — the existing header rendering already surfaces that, and a real access point's schema-validation step catches the BR-04 absence upstream. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (canonical UBL fixture declaring EUR throughout → confirmation citing BR-CO-04; same fixture with the line-extension `@currencyID` flipped to USD → mismatch row citing both codes and BR-CO-04). Audit OK. Full SEPA+UBL suite: 80 → 84 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 63):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check (drafts 49 / 51 / 53 / 55 / 56 / 59 / 61) from identifiers, the Ustrd remittance field, and party-name (Nm) fields to postal-address lines (`PstlAdr/AdrLine`) — the third-most-common entry point for non-Latin characters into a SEPA file after Ustrd (draft 59) and party Nm (draft 61). ISO 20022 declares `AdrLine` as `Max70Text` so any Unicode codepoints up to 70 chars pass the schema, but the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks both pin the basic SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) across every free-text field, addresses included. A street name like `Königsallee 12` or `Côte d'Azur` folded into a `Dbtr/PstlAdr/AdrLine` or `Cdtr/PstlAdr/AdrLine` is schema-valid but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the accented characters (so the counterparty bank shows a mangled address on the rail statement) or hard-reject at the rail-level character-set gate. The new `sepaCheckAdrLineCharset()` walks every `<AdrLine>` reachable anywhere in the pain.001 / pain.008 wrapper (camt.053 is statement-side and out of scope), trims, tests against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ Postal address lines (PstlAdr/AdrLine) use only EPC SEPA Rulebook allowed characters across all N line(s) …` row when every value is clean, or one red `✗ PstlAdr/AdrLine "X" contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (the offender quoted inline). With drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 / 63 the EPC character-set check now covers MsgId (document-level), EndToEndId + InstrId (transaction-level identifiers), MndtId (pain.008 mandate reference), PmtInfId (per-block payment-information identifier), Ustrd (per-transaction unstructured remittance), party Nm (file / block / transaction level), and AdrLine (postal-address lines) — every free-text surface in pain.001 / pain.008 that the EPC rulebook pins to the basic SEPA character set. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `Dbtr/PstlAdr/AdrLine = "123 Main Street"` + `"Berlin 10115"` → confirmation citing the 2-line count; same fixture with `Königsallee 12` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full SEPA+UBL suite: 76 → 80 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 62):** §2.7 PEPPOL UBL inspector gains a `PaymentMeansCode` UNCL 4461 subset check, narrowing the existing UBL 2.1 schema (which accepts any UNCL 4461 code as plain text) to the EN 16931 / PEPPOL BIS Billing 3.0 allowed subset: `1` (Instrument not defined), `10` (In cash), `20` (Cheque), `30` (Credit transfer), `42` (Payment to bank account), `48` (Bank card), `49` (Direct debit), `57` (Standing agreement), `58` (SEPA credit transfer), `59` (SEPA direct debit), `97` (Clearing between partners). PEPPOL access points reject anything outside this subset at the schematron gate; the canonical real-world failure mode is a template repurposed across payment instruments (`70` "Bankers draft", or `2` "Automated clearing house credit" pasted into a SEPA invoice) where the arithmetic still balances against itself, the code is plain text the schema doesn't constrain, but the document is rail-broken at the access-point level. The new `ublCheckPaymentMeansCode()` walks every `<cac:PaymentMeans>` reachable from the root, reads each `<cbc:PaymentMeansCode>` (trimmed), dedupes, and emits a single ✓ row across all matching codes via `intlList()` (locale joiner applies — `30 und 58` in de, `30 et 58` in fr) or one ✗ row per offending code citing the full allowed set. Skips silently when no PaymentMeans block is present — the field is technically mandatory under BR-49 but a real access point's schema-validation step catches absence upstream; this check is about value correctness, not presence. Distinct from the UNCL 1001 document-type-code check (draft 47, validates per-document type code) and the UNCL 5305 VAT category-code check (draft 41, validates per-line category letter): those validate other UNCL code lists; this validates the payment-instrument code list. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 / UNCL 4461 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `<cbc:PaymentMeansCode>30</cbc:PaymentMeansCode>` injected → confirmation row citing UNCL 4461; same fixture with `70` → mismatch row citing the EN 16931 / PEPPOL allowed set). Audit OK. Full SEPA+UBL suite: 76 → 80 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 61):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check (drafts 49 / 51 / 53 / 55 / 56 / 59) from identifiers and the Ustrd remittance field to party-name fields — the second-most-common entry point for non-Latin characters into a SEPA file. Walks every `Nm` reachable from these anchors in pain.001 / pain.008 wrappers (camt.053 is statement-side and out of scope): `GrpHdr/InitgPty/Nm` (file-level initiating party), per-PmtInf `Dbtr/Nm` (debtor on credit transfers) and `Cdtr/Nm` (creditor on direct debits), plus per-transaction `Cdtr/Nm` (beneficiary on credit transfers) and `Dbtr/Nm` (payer on direct debits). ISO 20022 declares `Nm` as `Max70Text` — any Unicode codepoints up to 70 chars pass the schema — but the EPC SEPA Credit Transfer and SEPA Direct Debit Rulebooks both pin the basic SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) across every free-text field, names included. A `Müller`, `Görtz`, or `Bénéteau` folded into a `Dbtr/Nm` or `Cdtr/Nm` is schema-valid but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the accented characters (so the beneficiary's bank shows a mangled name on the rail statement, making the payment hard to identify) or hard-reject at the rail-level character-set gate. The new `sepaCheckPartyNmCharset()` collects every `Nm` from the five anchor positions, tests each against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ Party name (InitgPty/Cdtr/Dbtr Nm) values use only EPC SEPA Rulebook allowed characters across all N name(s) …` row when every name is clean, or one red `✗ Party name "X" contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (the offender quoted inline so the maintainer can search the file for the source record). Anchors that omit `Nm` skip silently — the existing renderers already surface absence, and the inspector doesn't add noise for partially-populated blocks. With drafts 49 / 51 / 53 / 55 / 56 / 59 / 61 the EPC character-set check now covers MsgId (document-level), EndToEndId + InstrId (transaction-level identifiers), MndtId (pain.008 mandate reference), PmtInfId (per-block payment-information identifier), Ustrd (per-transaction unstructured remittance), and InitgPty + Dbtr + Cdtr Nm (party names at file, block, and transaction levels) — every free-text field reachable in pain.001 / pain.008 that the EPC rulebook pins to the basic SEPA character set. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `InitgPty/Nm=ACME Corp` plus ASCII debtor/creditor names → confirmation row; same fixture with the initiating-party name rewritten to `Müller GmbH` → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full SEPA+UBL suite: 72 → 76 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 60):** §2.7 PEPPOL UBL inspector gains an `InvoicePeriod` start ≤ end sanity check, the fourth file-wide accounting/structural invariant on the UBL side alongside the line-sum + tax-inclusive invariants from draft 28, the PayableAmount invariant from draft 45, and the line-ID uniqueness gate from draft 48. EN 16931 BR-29 / BR-CO-19 require that when `<cac:InvoicePeriod>` is present at the document level (BT-73 StartDate, BT-74 EndDate — the billing period the invoice covers, used for subscriptions, utilities, telecoms, and any recurring supply), the StartDate must be on or before the EndDate. UBL 2.1 declares both as plain `xs:date`, so an inverted period passes the schema but is rejected by PEPPOL access points at the BR-29 schematron gate; the typical real-world cause is a template repurposed across billing cycles where the date swap was missed (so a March invoice carries Feb's End and Mar's Start, or vice-versa). The new `ublCheckInvoicePeriod()` walks the root for `cac:InvoicePeriod`, reads `cbc:StartDate` and `cbc:EndDate` (trimmed, `Date.parse`-resolved so half-completed inputs degrade silently rather than firing false negatives), and emits one green `✓ InvoicePeriod EndDate Y is on or after StartDate X (EN 16931 BR-29 / BR-CO-19)` row when the order is correct, or one red `✗ InvoicePeriod EndDate Y precedes StartDate X — EN 16931 BR-29 / BR-CO-19 requires StartDate ≤ EndDate, PEPPOL access points reject at the schematron gate` row otherwise. Skips silently when the element is absent or either bound is missing/unparseable — those degenerate cases are caught by the schema-validation step a real implementation would run upstream, and `InvoicePeriod` itself is optional per PEPPOL BIS Billing 3.0. Distinct from the IssueDate / DueDate check (the existing `ublCheckDates()`): that check covers the invoice's own issuance and payment timeline (BT-2 / BT-9); this check covers the supply period the invoice represents (BT-73 / BT-74). 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL BIS Billing 3.0 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed UBL fixture with `2024-03-01 → 2024-03-31` injected → confirmation row citing BR-29 / BR-CO-19; same fixture with the dates swapped → mismatch row citing the precedence violation). Audit OK. Full SEPA+UBL suite: 72 → 76 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 59):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check (drafts 49 / 51 / 53 / 55 / 56) from free-text identifiers to the per-transaction `<RmtInf>/<Ustrd>` unstructured remittance information field — the highest-frequency real-world rail-rejection cause in the SEPA flow. ISO 20022 declares `Ustrd` as `Max140Text`, so any Unicode string up to 140 codepoints passes the schema; the EPC SEPA Credit Transfer Rulebook and SEPA Direct Debit Rulebook both require the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) to hold across every free-text field, and `Ustrd` is the field most likely to inherit user-facing or invoice-derived text (German umlauts, French accents, em-dashes, smart quotes) the originator's accounting system never sanitised. A `Ustrd` carrying characters outside the allowed set is schema-valid but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the offending characters before forwarding (which destroys the remittance reference the beneficiary uses to reconcile the payment against their AR ledger) or hard-reject the file at the rail-level character-set gate. The new `sepaCheckUstrdCharset()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` in pain.001 / pain.008 wrappers (camt.053 is statement-side and out of scope for this gate), reads every `<Ustrd>` child under `<RmtInf>` (whitespace-trimmed; multiple `<Ustrd>` elements per `<RmtInf>` are allowed by the ISO 20022 schema and all are checked), tests each against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ RmtInf/Ustrd values use only EPC SEPA Rulebook allowed characters across all N unstructured remittance entry(ies) …` row when every value is clean, or one red `✗ RmtInf/Ustrd "X" contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (the offender quoted inline so the maintainer can search the file for the source record). Transactions without an `<Ustrd>` skip silently — `<RmtInf>` is optional in ISO 20022 (some flows rely entirely on `<EndToEndId>` for reconciliation) and the inspector doesn't add noise for the degenerate case. With drafts 49 / 51 / 53 / 55 / 56 / 59 the EPC character-set check now covers MsgId (document-level), EndToEndId + InstrId (transaction-level identifiers in both pain.001 and pain.008), MndtId (pain.008-specific mandate reference), PmtInfId (per-block payment-information identifier), and Ustrd (per-transaction unstructured remittance information) — the full free-text surface in pain.001 / pain.008. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `<Ustrd>Invoice 2024-001</Ustrd>` → confirmation citing the 1-entry count; same fixture with `Rechnung Müller 2024` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full suite: 306 → 308 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 58):** §2.7 PEPPOL UBL inspector gains a Buyer VAT identifier check, the symmetric counterpart to the Seller VAT identifier check shipped in draft 57. PEPPOL BIS Billing 3.0 invoices declare the buyer's VAT number via `AccountingCustomerParty/Party/PartyTaxScheme/CompanyID`, paired with `TaxScheme/cbc:ID="VAT"`. EN 16931 BT-48 names this as the Buyer VAT identifier and BR-CO-09 makes it mandatory for intra-EU reverse-charge supplies (the AE category from draft 41); the value MUST start with a valid ISO 3166-1 alpha-2 country code prefix followed by the member-state VAT format. UBL 2.1 declares `cbc:CompanyID` as plain identifier text so a value without the country prefix, or with the wrong remainder shape, passes the schema but is rejected by PEPPOL access points at the BT-48 schematron gate — the typical real-world cause is a template carrying the buyer's national tax registration number (e.g. a French SIREN or a German Steuernummer) where the file generator should have substituted the EU VAT identifier instead. The new `ublCheckBuyerVat()` walks `AccountingCustomerParty/Party/PartyTaxScheme`, picks the block whose nested `TaxScheme/cbc:ID = VAT`, reads `cbc:CompanyID` (whitespace-trimmed), and routes the value through the same `vatValidate()` the standalone EU VAT tool (§2.3) ships — same per-country format regex + checksum logic, no duplication, locale-correct error path. Emits one green `✓ Buyer VAT identifier X valid per ISO 3166-1 alpha-2 + EU VAT format (EN 16931 BT-48 / PEPPOL BIS Billing 3.0)` row when `vatValidate()` accepts the value (format pass or checksum pass), or one red `✗ Buyer VAT identifier X invalid — PEPPOL BIS Billing 3.0 / EN 16931 BT-48 requires a valid ISO 3166-1 alpha-2 country prefix + EU VAT format` row otherwise. Skips silently when no PartyTaxScheme with TaxScheme/ID=VAT is present — the existing customer header already surfaces what's declared, and BR-CO-09 absence (when applicable to the document's tax categories) would be caught by a real access point's schema-validation step upstream. Distinct from the Seller VAT identifier check (draft 57): same shape, mirrored anchor element (AccountingCustomerParty vs AccountingSupplierParty), different BT field (BT-48 vs BT-31). The two checks together cover both party-level VAT identifiers on the UBL side. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `FR40303265045` (the canonical FR example, passes the `(12 + 3*(SIREN%97))%97` checksum) injected into the customer's PartyTaxScheme → confirmation row citing ISO 3166-1 alpha-2 + EU VAT format; same fixture with `987654321` (no country prefix) → mismatch row citing PEPPOL BIS Billing 3.0 / BT-48). Audit OK. Full suite: 304 → 306 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 57):** §2.7 PEPPOL UBL inspector gains a Seller VAT identifier check. PEPPOL BIS Billing 3.0 invoices declare the supplier VAT number via `AccountingSupplierParty/Party/PartyTaxScheme/CompanyID`, paired with `TaxScheme/cbc:ID="VAT"`. EN 16931 BT-31 names this as the Seller VAT identifier and BR-CO-09 makes it mandatory for any invoice carrying standard-rated lines; the value MUST start with a valid ISO 3166-1 alpha-2 country code prefix followed by the member-state VAT format (e.g. `DE123456789`, `FR12345678901`). UBL 2.1 declares `cbc:CompanyID` as plain identifier text so a value without the country prefix, or with the wrong remainder shape, passes the schema but is rejected by PEPPOL access points at the BT-31 schematron gate — the typical real-world cause is a non-EU UBL template repurposed for PEPPOL where the supplier ID was carried unchanged from a flow that didn't enforce the EU format. The new `ublCheckSellerVat()` walks `AccountingSupplierParty/Party/PartyTaxScheme`, picks the block whose nested `TaxScheme/cbc:ID = VAT`, reads `cbc:CompanyID` (whitespace-trimmed), and routes the value through the same `vatValidate()` the standalone EU VAT tool (§2.3) ships — same per-country format regex + checksum logic (ISO 7064 MOD 11,10 for DE, weighted MOD 11 for the Scandinavian formats, MOD 97 for BE, etc.), no duplication, locale-correct error path. Emits one green `✓ Seller VAT identifier X valid per ISO 3166-1 alpha-2 + EU VAT format (EN 16931 BT-31 / PEPPOL BIS Billing 3.0)` row when `vatValidate()` accepts the value (format pass or checksum pass), or one red `✗ Seller VAT identifier X invalid — PEPPOL BIS Billing 3.0 / EN 16931 BT-31 requires a valid ISO 3166-1 alpha-2 country prefix + EU VAT format` row otherwise. Skips silently when no PartyTaxScheme with TaxScheme/ID=VAT is present — the existing supplier header already surfaces what's declared, and a real access point's schema-validation step would catch BR-CO-09 absence upstream. Distinct from the IBAN cross-check (draft 36, which validates Payee/Payer account numbers) and the TaxScheme/ID=VAT enforcement (draft 54, which validates the scheme designator on tax categories): those gates cover the financial-account identifier and the per-category tax scheme; this gate covers the party-level seller identifier. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / PEPPOL vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with `DE136695976` injected into PartyTaxScheme → confirmation row citing ISO 3166-1 alpha-2 + EU VAT format; same fixture with `123456789` (no country prefix) → mismatch row citing PEPPOL BIS Billing 3.0 / BT-31). Audit OK. Full suite: 304 → 306 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 56):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check from drafts 49 / 51 / 53 / 55 to the per-PmtInf `<PmtInfId>` identifier — the fifth and final free-text identifier the EPC rulebooks pin to the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space). ISO 20022 declares `PmtInfId` as `Max35Text`, so any Unicode string up to 35 codepoints passes the schema; the EPC SEPA Credit Transfer Rulebook and SEPA Direct Debit Rulebook both require the SEPA character set to hold across every free-text identifier in the file. The canonical real-world failure mode is a batch generator interpolating a company name or local reference (`Übermäßig GmbH`, `Müller & Cie`) into the block identifier — schema-valid `Max35Text`, rail-broken at the EBA STEP2 / STET / RT1 character-set gate. The new `sepaCheckPmtInfIdCharset()` walks every `<PmtInf>` in pain.001 / pain.008 wrappers (camt.053 is statement-side and out of scope for this gate), reads each block's `<PmtInfId>` (whitespace-trimmed), tests against the same `SEPA_CHARSET_RE` regex from draft 49 (`^[A-Za-z0-9/\-?:().,'+ ]+$`), and emits one green `✓ PmtInfId values use only EPC SEPA Rulebook allowed characters across all N PmtInf block(s) …` row when every value is clean, or one red `✗ PmtInfId X contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (keyed by the bad identifier so the maintainer can locate the source block). Blocks without a `PmtInfId` skip silently — the existing PmtInf header rendering surfaces what's declared, and the inspector doesn't add noise for the degenerate case. With drafts 49 / 51 / 53 / 55 / 56 the SEPA character-set check covers MsgId (document-level), EndToEndId + InstrId (transaction-level credit-transfer + direct-debit), MndtId (pain.008 mandate reference), and PmtInfId (per-block payment-information identifier) — the full free-text identifier surface in pain.001 / pain.008. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `PmtInfId=PAY-001` → confirmation citing the 1-block count; same fixture with `PAY-Übermäßig` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full suite: 304 → 306 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 55):** §2.4 SEPA inspector extends the EPC character-set check (drafts 49 / 51 / 53) to the pain.008 per-transaction mandate identifier. The EPC SEPA Direct Debit Rulebook requires the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) to hold across every free-text identifier in pain.008 — and `<DrctDbtTx>/<MndtRltdInf>/<MndtId>` is the mandate reference the creditor and debtor agreed on at mandate-signing time, used as the per-transaction reconciliation key on the SDD side. ISO 20022 declares MndtId as Max35Text so accented characters (the canonical real-world cause: a debtor's name like `Müller` folded into the mandate reference by the originator's CRM) pass the schema; clearing systems either strip the offending characters before forwarding (destroying the mandate reference) or hard-reject the file at the character-set gate. The new `sepaCheckMndtIdCharset()` walks every `<DrctDbtTxInf>` in pain.008 wrappers (pain.001 skips silently — credit transfers don't carry mandates), reads each transaction's `<DrctDbtTx>/<MndtRltdInf>/<MndtId>` (whitespace-trimmed), tests it against the same `SEPA_CHARSET_RE` regex from draft 49, and emits one green `✓ MndtId values use only EPC SDD Rulebook allowed characters across all N direct-debit transaction(s) …` row when every value is clean, or one red `✗ MndtId X contains characters outside the EPC SDD Rulebook allowed set …` row per offending value (keyed by the bad identifier so the maintainer can locate the source record). Transactions without an MndtId skip silently — MndtId is only declared on direct debits, and the per-transaction renderer from draft 22 already surfaces what's declared. With this draft the SEPA character-set checks cover MsgId (document-level), EndToEndId + InstrId (transaction-level identifiers in both pain.001 and pain.008), and MndtId (pain.008-specific mandate reference). 2 new strings × 5 locales (en/fr/de reviewed against EPC SDD / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.008 with `MndtId=MNDT-2024-001` → confirmation citing the 1-direct-debit count; same fixture with `MNDT-Müller-001` → mismatch row citing the offending value and the EPC SDD character-set requirement). Audit OK. Full suite: 302 → 304 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 54):** §2.7 PEPPOL UBL inspector gains a `TaxScheme/ID = VAT` enforcement check. UBL 2.1 allows `cac:TaxScheme/cbc:ID` to carry any UNCL 5153 tax-scheme code — GST (Australia), HST (Canada), VAT (EU), and dozens more — but PEPPOL BIS Billing 3.0 narrows the value to "VAT" only across every `TaxCategory` reachable from the root: header-side `TaxSubtotal/TaxCategory/TaxScheme` and item-level `ClassifiedTaxCategory/TaxScheme` alike. The canonical real-world failure mode is a template repurposed from a non-EU UBL flow — a New Zealand or Singapore UBL invoice template ships with `TaxScheme/ID=GST` and an engineer ports the layout to PEPPOL without updating the scheme code; the schema passes (UBL 2.1 doesn't constrain the code) but PEPPOL access points reject the document at the schematron gate. The new `ublCheckTaxScheme()` walks every `<cac:TaxScheme>` reachable from the root, reads each `<cbc:ID>` (uppercased and deduped), and emits one green `✓ TaxScheme/ID = VAT across every TaxCategory (PEPPOL BIS Billing 3.0 / UNCL 5153)` row when every code matches, or one red `✗ TaxScheme/ID X is not VAT — PEPPOL BIS Billing 3.0 narrows the UNCL 5153 tax scheme code to VAT only` row per offending code. TaxCategories without a TaxScheme child skip silently — the schema allows the absence and the existing per-rate breakdown surfaces what's declared. Distinct from the VAT category-letter check (draft 41, which validates the `TaxCategory/ID` letter against the EN 16931 BR-S/Z/E/AE/K/G/O/L/M set) and from the VAT-rate / totals invariants (drafts 28 / 37, which validate arithmetic against numeric fields): those checks operate inside the tax breakdown; this check validates the tax-scheme designator itself, which selects which rule family applies to all the values inside. 2 new strings × 5 locales (en/fr/de reviewed against PEPPOL BIS Billing 3.0 / UNCL 5153 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (UBL fixture with TaxScheme/ID=VAT → confirmation row citing PEPPOL BIS Billing 3.0 / UNCL 5153; same fixture with TaxScheme/ID=GST → mismatch row citing the PEPPOL BIS Billing 3.0 restriction). Audit OK. Full suite: 300 → 302 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 53):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check from drafts 49 / 51 to the per-transaction `<PmtId>/<InstrId>` originator-side instruction identifier — the third character-set gate, completing the trio of free-text identifiers the EPC rulebooks pin to the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space). The EPC SEPA Credit Transfer and Direct Debit Rulebooks describe `InstrId` as a free-text key set by the initiating party for its own bookkeeping; in practice it is commonly populated from internal sequence keys or batch references that may carry punctuation or whitespace outside the allowed set. A bad `InstrId` is `Max35Text`-valid but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the offending characters before forwarding (destroying the originator-side instruction key) or hard-reject the file at the character-set gate. The new `sepaCheckInstrIdCharset()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` in pain.001 / pain.008 wrappers, reads each transaction's `<PmtId>/<InstrId>` (whitespace-trimmed), tests it against the same `SEPA_CHARSET_RE` regex from draft 49 (`^[A-Za-z0-9/\-?:().,'+ ]+$`), and emits one green `✓ InstrId values use only EPC SEPA Rulebook allowed characters across all N transaction(s) …` row when every value is clean, or one red `✗ InstrId X contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (keyed by the bad identifier so the maintainer can locate the source record). Transactions without an `InstrId` skip silently — `InstrId` is optional per the ISO 20022 schema (only `EndToEndId` is mandatory inside `PmtId`), and the existing per-transaction renderer surfaces what's declared. With this draft the three character-set gates collectively cover MsgId (document-level), EndToEndId (transaction-level reconciliation key), and InstrId (transaction-level originator-side key). 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `<InstrId>INSTR-001</InstrId>` injected → confirmation citing the 1-transaction count; same fixture with `INSTR-Übergabe` → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full suite: 300 → 302 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 52):** §2.7 PEPPOL UBL inspector gains a `ProfileID` process-conformance check, the second profile-level gate on the UBL side after the `CustomizationID` gate from draft 50. PEPPOL BIS Billing 3.0 defines a single process URN — `urn:fdc:peppol.eu:2017:poacc:billing:01:1.0` ("Billing 01: Invoicing") — and conforming invoices and credit notes declare it at the document root via `<cbc:ProfileID>`. A schema-valid UBL 2.1 document carrying any other process URN is rejected by PEPPOL access points at the routing layer, because `ProfileID` selects the SML (Service Metadata Locator) / SMP (Service Metadata Publisher) service-metadata record the receiver advertises; a mismatch sends the document to a routing dead-end. Parallel in shape to draft 50: `CustomizationID` names the document profile (which schema-rule set the document claims compliance with), `ProfileID` names the transaction process (which business interaction the document participates in). Both gates must pass for a PEPPOL access point to forward the document. The new `ublCheckProfileId()` reads the root `<cbc:ProfileID>` (trimmed), compares it to the canonical PEPPOL string stored in an inline `UBL_PEPPOL_PROFILE_ID` constant, and emits one green `✓ ProfileID declares the PEPPOL BIS Billing 3.0 process (…)` row on exact match or one red `✗ ProfileID X does not match the PEPPOL BIS Billing 3.0 process (Y) — PEPPOL access points route documents by ProfileID, mismatches dead-end at the SML/SMP lookup` row otherwise, citing both the declared and expected URNs inline so the maintainer doesn't have to look up the PEPPOL spec. Missing element skips silently — the existing `ProfileID` rendering already surfaces absence, and a PEPPOL access point's schema-validation step would catch it upstream. Distinct from `CustomizationID` (which validates schema-rule conformance) and from the per-document UNCL 1001 code check (draft 47): those validate codes carried inside the document; this check validates the process URN the document declares participation in. With drafts 50 + 52 in place, both PEPPOL profile-level gates are wired. 2 new strings × 5 locales (en/fr/de reviewed against PEPPOL BIS Billing 3.0 / EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed UBL fixture with the canonical PEPPOL process URN → confirmation row; same fixture with the URN rewritten to `urn:fdc:peppol.eu:2017:poacc:billing:99:9.9` → mismatch row citing both the actual and expected values plus the SML/SMP routing failure mode). Audit OK. Full suite: 298 → 300 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 51):** §2.4 SEPA inspector extends the EPC SEPA Rulebook character-set check from draft 49 to the per-transaction `EndToEndId` reconciliation key — the second character-set gate, complementing the document-level `MsgId` gate. The EPC SEPA Credit Transfer and Direct Debit Rulebooks both require the SEPA character set (`A-Z a-z 0-9 / - ? : ( ) . , ' +` and space) to hold across every free-text identifier in the file; `MsgId` is only one entry point, and transaction-level `EndToEndId` values are commonly populated from customer references, invoice numbers, or CRM identifiers that may carry accented characters the originator never sanitised. A `EndToEndId` carrying characters outside the allowed set is `Max35Text`-valid but rail-broken — clearing systems (EBA STEP2, STET, RT1) either strip the bad characters before forwarding (destroying the reconciliation reference) or hard-reject the file at the character-set gate. The new `sepaCheckE2eIdCharset()` walks every `<CdtTrfTxInf>` (credit transfers) and `<DrctDbtTxInf>` (direct debits) in pain.001 / pain.008 wrappers, reads each transaction's `<PmtId>/<EndToEndId>` (whitespace-trimmed), tests it against the same `SEPA_CHARSET_RE` regex from draft 49 (`^[A-Za-z0-9/\-?:().,'+ ]+$`), and emits one green `✓ EndToEndId values use only EPC SEPA Rulebook allowed characters across all N transaction(s) …` row when every value is clean, or one red `✗ EndToEndId X contains characters outside the EPC SEPA Rulebook allowed set …` row per offending value (keyed by the bad identifier so the maintainer can locate the source record). Transactions without an EndToEndId skip silently — the existing per-transaction renderer already surfaces the absence. Distinct from the uniqueness gate from draft 43: uniqueness covers content (no duplicates), this check covers form (only allowed characters). The two gates together cover the EPC SEPA Rulebook's invariants for the transaction-level reconciliation identifier. 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `EndToEndId=E2E-001` → confirmation citing the 1-transaction count; same fixture with `E2E-Müller` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full suite: 298 → 300 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 50):** §2.7 PEPPOL UBL inspector gains a `CustomizationID` profile-conformance check. PEPPOL BIS Billing 3.0 invoices and credit notes MUST declare `<cbc:CustomizationID>urn:cen.eu:en16931:2017#compliant#urn:fdc:peppol.eu:2017:poacc:billing:3.0</cbc:CustomizationID>` at the document root: the CEN prefix asserts EN 16931 compliance and the PEPPOL suffix narrows the document to the BIS Billing 3.0 subset. A schema-valid UBL 2.1 invoice that declares any other URN (or a stripped EN 16931 URN without the PEPPOL suffix) is still UBL but rejected by PEPPOL access points at the gateway profile check; the typical real-world cause is a Factur-X / ZUGFeRD or generic UBL template repurposed for PEPPOL without updating the URN. The new `ublCheckCustomizationId()` reads the root `<cbc:CustomizationID>` (trimmed), compares it to the canonical PEPPOL string stored in an inline `UBL_PEPPOL_CUSTOMIZATION_ID` constant, and emits one green `✓ CustomizationID declares PEPPOL BIS Billing 3.0 conformance (…)` row on exact match or a red `✗ CustomizationID X does not match PEPPOL BIS Billing 3.0 (Y) — PEPPOL access points reject documents at the profile gate` row otherwise, citing both the declared and expected URNs inline so the maintainer doesn't have to look up the PEPPOL spec. Missing element skips silently — the existing `CustomizationID` rendering already surfaces absence, and a PEPPOL access point's schema-validation step would catch it upstream. Distinct from the UNCL 1001 document-type-code check (draft 47, which validates per-document code values against the PEPPOL allowed set) and the UNCL 5305 VAT category-code check (draft 41, which validates per-line category letters): those validate codes carried inside the document; this check validates the profile the document claims conformance to (the first profile-level gate on the UBL side). 2 new strings × 5 locales (en/fr/de reviewed against PEPPOL BIS Billing 3.0 / EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed UBL fixture with the canonical PEPPOL URN → confirmation row; same fixture with the URN reduced to `urn:cen.eu:en16931:2017` → mismatch row citing both the actual and expected values plus the PEPPOL profile-gate failure mode). Audit OK. Full suite: 296 → 298 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 49):** §2.4 SEPA inspector gains a `GrpHdr/MsgId` EPC SEPA Rulebook character-set check — the first character-set gate, complementing the existing scalar and arithmetic invariants from drafts 27 / 29 / 35 / 38 / 40 / 42 / 43 / 44 / 46. ISO 20022 declares `MsgId` as `Max35Text` (any Unicode codepoint, length ≤ 35), but the EPC SEPA Credit Transfer Rulebook §AT-43 and the parallel section in the SEPA Direct Debit Rulebook pin free-text fields and identifiers to a narrow ISO 20022 BasicLatin subset: `A-Z a-z 0-9 / - ? : ( ) . , ' +` and space. A `MsgId` carrying accented letters, em-dashes, or emoji (the typical real-world cause: a value pasted from a CRM template that wasn't sanitised) is schema-valid XML but rail-broken — EBA STEP2 / STET / RT1 either strip the offending characters before forwarding (which destroys the reconciliation reference) or hard-reject the file at the character-set gate. The new `sepaCheckMsgIdCharset()` reads `GrpHdr/MsgId` on pain.001 / pain.008 wrappers (camt.053 is statement-side and out of scope for this gate), tests against the regex `^[A-Za-z0-9/\-?:().,'+ ]+$`, and emits one green `✓ GrpHdr/MsgId X uses only EPC SEPA Rulebook allowed characters …` row on match or a red `✗ GrpHdr/MsgId X contains characters outside the EPC SEPA Rulebook allowed set …` row otherwise, citing the allowed set inline so the maintainer doesn't have to look up the rulebook. Header-only by design — `EndToEndId` uniqueness (draft 43) and `InstrId` presence (draft 19) have their own gates, and the character-set failure mode is dominated by the document-level identifier in practice (a CRM-driven MsgId is the most common entry point for non-Latin characters into the file). 2 new strings × 5 locales (en/fr/de reviewed against EPC SEPA / ISO 20022 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `MsgId=MSG-2024-001` → confirmation citing the EPC SEPA allowed character set; same fixture with `MSG-2024-Ää` injected → mismatch row citing the offending value and the EPC SEPA character-set requirement). Audit OK. Full suite: 296 → 298 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 48):** §2.7 PEPPOL UBL inspector gains a line-ID uniqueness check (EN 16931 BR-21 / PEPPOL BIS Billing 3.0) — the fourth file-wide accounting / structural invariant on the UBL side, alongside the line-sum + tax-inclusive invariants from draft 28 and the PayableAmount invariant from draft 45, and parallel in shape to the SEPA `EndToEndId` uniqueness gate from draft 43. EN 16931 BR-21 names `cbc:ID` as the InvoiceLine identifier; PEPPOL BIS Billing 3.0 access points additionally reject duplicates because downstream processors key per-line allowances, charges, and dispute references off the ID — a collision causes silent reconciliation failures even when the per-line arithmetic balances. UBL 2.1 declares `cbc:ID` as plain `Identifier` text, so duplicates are schema-valid but rail-broken; the typical real-world cause is a batch generator iterating over line items without advancing the sequence counter (exactly the same failure mode the SEPA EndToEndId gate catches one layer up). The new `ublCheckLineIdUnique()` walks every `<cac:InvoiceLine>` (or `<cac:CreditNoteLine>` for credit notes — selected via `detected.type`), extracts each line's `<cbc:ID>` (whitespace-trimmed), tallies occurrences in a `Map`, and surfaces a single green `✓ Line IDs unique across the document (N line(s)) per EN 16931 BR-21 / PEPPOL BIS Billing 3.0` row when all values are distinct, or one red `✗ Line ID duplicated within document: X appears N times — EN 16931 BR-21 / PEPPOL BIS Billing 3.0 requires unique line identifiers` row per duplicated identifier. Skips silently when fewer than two lines carry an ID — single-line invoices trivially satisfy uniqueness, and the inspector doesn't add noise for the degenerate case. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (two InvoiceLines carrying distinct `cbc:ID=1` / `cbc:ID=2` → confirmation citing BR-21 and the 2-line count; both lines reusing `cbc:ID=1` → duplicate row citing the count and the BR-21 / PEPPOL BIS Billing 3.0 requirement). Audit OK. Full suite: 294 → 296 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 47):** §2.7 PEPPOL UBL inspector gains a UNCL 1001 document-type-code check enforcing the PEPPOL BIS Billing 3.0 allowed sets. `<Invoice>` documents declare the type code via `<cbc:InvoiceTypeCode>` (EN 16931 BT-3) and PEPPOL BIS 3.0 restricts it to {380, 381, 384, 389, 875, 876, 877} — 380 (Commercial invoice) is by far the most common. `<CreditNote>` documents declare it via `<cbc:CreditNoteTypeCode>` and PEPPOL restricts it to {381, 396, 532}. Any other UNCL 1001 code is schema-valid plain integer text (the schema only requires `cbc:Code` shape) but rail-broken — PEPPOL access points reject the document at the type-code check, and the typical real-world cause is a template repurposed across UNCL document types (e.g. a debit-note code `383` carried in an invoice slot). The new `ublCheckDocumentTypeCode()` reads the type-code element selected by `detected.type` (`InvoiceTypeCode` for Invoice, `CreditNoteTypeCode` for CreditNote), trims whitespace, and emits one green `✓ Document type code N is allowed by PEPPOL BIS Billing 3.0 / EN 16931 (UNCL 1001)` row on a match or one red `✗ Document type code N is not in the PEPPOL BIS Billing 3.0 allowed set (…)` row citing the full allowed set via `intlList()` so the locale joiner applies (`380, 381, …, 875 und 876 und 877` in de, `… et 877` in fr). Missing element skips silently — the existing header rendering already surfaces what's declared, and `BR-02` (mandatory `InvoiceTypeCode`) absence is caught by the schema-validation step a real implementation would run upstream. Distinct from the VAT category-code check (draft 41, which validates `TaxCategory/ID` letters S/Z/E/…) — that check covers per-line and per-subtotal VAT categorisation; this check covers the document-level type identifier. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 / UNCL 1001 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed Invoice with `InvoiceTypeCode=380` → confirmation citing UNCL 1001 and PEPPOL BIS Billing 3.0; same fixture with `InvoiceTypeCode=999` → mismatch row citing both the offending code and the allowed-set members). Audit OK. Full suite: 292 → 294 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 46):** §2.4 SEPA inspector gains a pain.008 `SeqTp` validation — the per-PmtInf direct-debit sequence-type gate from the EPC SEPA Direct Debit Rulebook, parallel in shape to the PmtMtd / ChrgBr / SvcLvl scalar checks from drafts 40 / 42 / 44 but pain.008-only since SeqTp is meaningless for credit transfers. ISO 20022 declares `PmtTpInf/SeqTp` as `Max4Text` so any 4-character value passes the schema; the EPC SDD Rulebook restricts the value to `FRST` (first collection of a recurring mandate), `RCUR` (recurring), `OOFF` (one-off), or `FNAL` (final collection). A pain.008 declaring `RECR` (a common typo for `RCUR`), `RECU`, or any other code is schema-valid XML but rail-broken — clearing systems (EBA STEP2, STET, RT1) reject it at the rail-level sequence-type check, and the typical real-world cause is a template repurposed across recurring and one-off mandates without updating the field. The new `sepaCheckSeqTp()` walks every `<PmtInf>` in pain.008 wrappers (the function exits early for pain.001 / camt.053), locates `PmtTpInf/SeqTp` (both elements optional per the schema), reads the code (case-folded and whitespace-stripped for paste-safety), and accumulates the mismatches. On full success: one green `✓ SeqTp valid per EPC SDD Rulebook: N PmtInf block(s) declare one of FRST / RCUR / OOFF / FNAL` row across all matching blocks. On any failure: one red `✗ SeqTp invalid: PmtInf #i declares X but EPC SDD Rulebook allows only FRST / RCUR / OOFF / FNAL (ISO 20022 / EPC SDD)` row per offending block keyed by the 1-based PmtInf index so a maintainer scanning a multi-PmtInf file knows exactly which block to fix. Blocks that omit `PmtTpInf` or `SeqTp` skip silently — they already render with the rest of the PmtInf header and the maintainer can see the absence. With this draft, the four EPC-rulebook scalar gates are wired across both rulebooks: PmtMtd (TRF/DD per document type), ChrgBr (SLEV per the SCT/SDD settlement model), SvcLvl (SEPA per the rulebook's service-level designation), and SeqTp (FRST/RCUR/OOFF/FNAL per the SDD sequence-type table). 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SDD vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.008 with `PmtTpInf/SeqTp=FRST` → confirmation citing the EPC SDD allowed set; same fixture with `SeqTp=RECR` → mismatch row citing the typo'd value and the four-code allowed set). Audit OK. Full suite: 290 → 292 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 45):** §2.7 PEPPOL UBL inspector gains a `PayableAmount` consistency check (EN 16931 BR-CO-16) — the third file-wide accounting gate on the UBL side, alongside the line-sum and tax-inclusive invariants shipped in draft 28. PEPPOL BIS Billing 3.0 invoices declare `LegalMonetaryTotal/PayableAmount` as the figure the buyer is actually asked to pay; BR-CO-16 pins it to `TaxInclusiveAmount − PrepaidAmount + PayableRoundingAmount`, with `PrepaidAmount` and `PayableRoundingAmount` defaulting to 0 when absent (the most common shape — the canonical small invoice carries neither). A mismatch is schema-valid arithmetic but rail-broken: PEPPOL access points reject the invoice at the BR-CO-16 check before it ever reaches the buyer, and the typical real-world cause is a prepaid amount applied in the buyer's accounting but not subtracted from `PayableAmount`, so the invoice asks for the wrong figure. The new `ublCheckPayable()` reads the four amounts from `LegalMonetaryTotal`, treats missing optional fields as 0 cents, computes the expected value in integer cents (`Math.round(value * 100)`) to dodge IEEE-754 drift, applies the standard ±1¢ rounding tolerance per the EN 16931 published policy, and surfaces one green `✓ PayableAmount X matches TaxInclusive − Prepaid + PayableRounding (EN 16931 BR-CO-16)` row when the invariant holds, or one red `✗ PayableAmount mismatch: TaxInclusive − Prepaid + PayableRounding = A ≠ declared PayableAmount B (EN 16931 BR-CO-16)` row citing both the computed and declared values. Skips silently when either `TaxInclusiveAmount` or `PayableAmount` is missing or unparseable — the existing `LegalMonetaryTotal` rendering already surfaces the raw values upstream, and the inspector doesn't add noise for half-filled documents. Distinct from the totals-consistency check (draft 28, which verifies `TaxExcl + Σ TaxAmount = TaxInclusive`) and the VAT-rate check (draft 37, which verifies per-subtotal arithmetic): those checks cover what goes *into* the tax-inclusive total, this one covers what comes *out* of it as the buyer-facing payable figure. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed Invoice with TaxInclusive=PayableAmount=119.00 EUR → confirmation citing BR-CO-16; same fixture with PayableAmount bumped to 120.00 EUR → mismatch row citing both 119.00 and 120.00 plus BR-CO-16). Audit OK. Full suite: 288 → 290 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 44):** §2.4 SEPA inspector gains a `SvcLvl=SEPA` consistency check — the third of the three EPC-rulebook scalar gates at the PmtInf level, parallel in shape to the `PmtMtd` check shipped in draft 40 and the `ChrgBr` check shipped in draft 42. ISO 20022 declares `PmtTpInf/SvcLvl/Cd` as a free 4-character service-level code, with values like `NURG` (non-urgent), `URGP` (urgent), `PRPT` (priority), and `SEPA` (standard SCT/SDD); the EPC SEPA Credit Transfer Rulebook and SEPA Direct Debit Rulebook both pin it to `SEPA` for the standard SCT/SDD flow. A pain.001 or pain.008 declaring `NURG` or any other non-`SEPA` code is schema-valid XML but rail-broken — clearing systems (EBA STEP2, STET, RT1) reject it at the rail-level service-level gate, and the typical real-world cause is a template file repurposed across SEPA and non-SEPA wire-instruction flows without updating the field. The new `sepaCheckSvcLvl()` walks every `<PmtInf>` in pain.001 / pain.008 wrappers, locates `PmtTpInf/SvcLvl/Cd` (both elements optional per the schema), reads the code (case-folded and whitespace-stripped for paste-safety), and accumulates the mismatches. On full success: one green `✓ SvcLvl matches EPC SEPA Rulebook: N PmtInf block(s) declare SEPA (standard SCT/SDD service level)` row across all matching blocks. On any failure: one red `✗ SvcLvl mismatch: PmtInf #i declares X but EPC SEPA Rulebook requires SEPA (ISO 20022 / EPC SCT/SDD)` row per offending block, keyed by the 1-based PmtInf index so a maintainer scanning a multi-PmtInf file knows exactly which block to fix. Blocks that omit `PmtTpInf` or `SvcLvl/Cd` skip silently — they already render with the rest of the PmtInf header and the maintainer can see the absence. With this draft, the three EPC-rulebook scalar gates are complete: PmtMtd (TRF/DD per document type), ChrgBr (SLEV per the SEPA settlement model), and SvcLvl (SEPA per the rulebook's service-level designation). 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `<PmtTpInf><SvcLvl><Cd>SEPA</Cd></SvcLvl></PmtTpInf>` injected after `<PmtMtd>` → confirmation citing the SEPA expectation; same fixture with `Cd=NURG` → mismatch row citing both the declared NURG and the SEPA requirement). Audit OK. Full suite: 286 → 288 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 43):** §2.4 SEPA inspector gains an `EndToEndId` uniqueness gate — the third file-wide accounting check, complementing the `NbOfTxs` + `CtrlSum` group-header invariants from draft 35 and the per-PmtInf scalar checks shipped in drafts 40 (`PmtMtd`) and 42 (`ChrgBr`). Both the EPC SEPA Credit Transfer Rulebook (pain.001) and SEPA Direct Debit Rulebook (pain.008) require the `EndToEndId` carried in `<PmtId>/<EndToEndId>` to be unique per Originator — it is the reconciliation key the beneficiary and the originator both use to track the payment end-to-end through the clearing system (EBA STEP2, STET, RT1), and a duplicate within a batch causes either a hard reject or, worse, a silent reconciliation collision downstream. ISO 20022 lets `EndToEndId` be any `Max35Text`, so duplicates are schema-valid but rail-broken; the typical real-world cause is a batch-generation script that forgot to advance a sequence counter while iterating over a customer list. The new `sepaCheckE2eIdUnique()` walks every `<CdtTrfTxInf>` and `<DrctDbtTxInf>` across all `<PmtInf>` blocks in pain.001 / pain.008 wrappers, collects each transaction's `EndToEndId` (whitespace-stripped), tallies the occurrences in a `Map`, and surfaces a single green `✓ EndToEndId values are unique across the file (N transaction(s)) per EPC SEPA Rulebook` row when all values are distinct, or one red `✗ EndToEndId duplicated within file: X appears N times — EPC SEPA Rulebook requires uniqueness per Originator (ISO 20022 / EPC SCT/SDD)` row per duplicated identifier. Skips silently when fewer than two transactions carry an `EndToEndId` — single-transaction files trivially satisfy uniqueness, and the inspector doesn't add noise for the degenerate case. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 extended with a second `<CdtTrfTxInf>` carrying a distinct `E2E-002` → confirmation citing the two-transaction count; same fixture with the second transaction reusing `E2E-001` → duplicate row citing `E2E-001 appears 2 times` and the EPC SEPA uniqueness requirement). Audit OK. Full suite: 284 → 286 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 42):** §2.4 SEPA inspector gains a `ChrgBr` (charge bearer) consistency check — the second of the two PmtInf-level scalar gates pinned by the EPC SEPA Rulebooks, parallel in shape to the PmtMtd check shipped in draft 40. ISO 20022 enumerates `ChrgBr` as one of `DEBT / CRED / SHAR / SLEV`; the EPC SEPA Credit Transfer Rulebook and SEPA Direct Debit Rulebook both pin it to `SLEV` ("charges shared, allocated by local law") — the SEPA settlement model assumes the originator and beneficiary each bear their own bank's charges, and a `DEBT` or `CRED` declaration contradicts the model so clearing systems reject the file. The new `sepaCheckChrgBr()` walks every `<PmtInf>` in pain.001 / pain.008 wrappers, reads `<ChrgBr>` (case-folded, whitespace-stripped), and accumulates the mismatches. On full success: one green `✓ ChrgBr matches EPC SEPA Rulebook: N PmtInf block(s) declare SLEV (charges shared, allocated by local law)` row across all matching blocks. On any failure: one red `✗ ChrgBr mismatch: PmtInf #i declares X but EPC SEPA Rulebook requires SLEV` row per offending block, keyed by the 1-based PmtInf index so a maintainer scanning a multi-PmtInf file knows exactly which block to fix. `ChrgBr` is optional per the ISO 20022 schema, so blocks that omit it skip silently — they already render with the rest of the PmtInf header and the maintainer can see the absence; the inspector doesn't add noise for fields that don't have to be there. 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `ChrgBr=SLEV` injected after `<PmtMtd>` → confirmation citing the SLEV expectation; same fixture with `ChrgBr=DEBT` → mismatch row citing both the declared DEBT and the SLEV requirement). Audit OK. Full suite: 282 → 284 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 41):** §2.7 PEPPOL UBL inspector gains a VAT category-code cross-check (EN 16931 / UNCL 5305). EN 16931 declares a separate business-rule family for each allowed VAT category letter — BR-S (Standard), BR-Z (Zero), BR-E (Exempt), BR-AE (Reverse Charge), BR-K (intra-EEA), BR-G (Free export), BR-O (Outside scope), BR-L (Canary Islands), BR-M (Ceuta/Melilla) — and the rule families collectively define the allowed set `{S, Z, E, AE, K, G, O, L, M}`. Any letter outside this set is schema-valid plain text per UBL 2.1 but PEPPOL access points reject the invoice at the category-code check before it ever reaches the buyer. The new `ublCheckVatCategories()` walks every `<cac:TaxCategory>` reachable from the root (header-side `TaxTotal/TaxSubtotal/TaxCategory` and item-level `InvoiceLine/Item/ClassifiedTaxCategory` alike), reads each `<cbc:ID>` value, uppercases and dedupes, then validates against the inline `UBL_VAT_CATS` set. Confirmation row uses the existing `intlList()` helper so the locale joiner applies (`S und Z` in de, `S et Z` in fr). On failure: one warning row per offending code citing the full allowed set inline so the maintainer doesn't have to look up EN 16931. Distinct shape from the per-subtotal VAT-rate check (draft 37, which verifies `TaxableAmount × Percent === TaxAmount`) and the totals-consistency check (draft 28, which verifies `Σ TaxAmount === TaxInclusive - TaxExclusive`) — those checks are arithmetic on numeric fields and won't fire if the category letter is wrong but the math balances. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed `S` → confirmation row + UNCL 5305 reference; `XX` injected → warning row citing the allowed set). Audit OK. Full suite: 280 → 282 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 40):** §2.4 SEPA inspector gains a `PmtMtd` consistency check, parallel to the BIC / currency / NbOfTxs / IBAN-BIC cross-checks shipped in drafts 27 / 29 / 35 / 38. ISO 20022 enumerates `PmtMtd` as one of `CHK / TRF / TRA / DD` at the `<PmtInf>` level; the EPC SEPA Rulebooks tie two of those values to specific message schemas — pain.001.001.xx (customer credit-transfer initiation) MUST carry `PmtMtd=TRF` and pain.008.001.xx (customer direct-debit initiation) MUST carry `PmtMtd=DD`. A pain.001 declaring `DD`, or a pain.008 declaring `TRF`, is schema-valid XML but rail-broken — clearing systems reject it because the declared payment method contradicts the message's purpose, and the typical real-world cause is a template file that was repurposed across the two flows without updating the field. The new `sepaCheckPmtMtd()` walks every `<PmtInf>` in the document, reads its `<PmtMtd>` element (case-folded and whitespace-stripped for paste-safety), and accumulates the mismatches. On full success: one green `✓ PmtMtd matches document type: N PmtInf block(s) declare TRF as expected for pain.001` row across all matching blocks. On any failure: one red `✗ PmtMtd mismatch: PmtInf #i declares X but pain.001 requires TRF (ISO 20022 / EPC SEPA rulebook)` row per offending block — keyed by the 1-based PmtInf index so a maintainer scanning a multi-PmtInf file knows exactly which block to fix. `PmtMtd` is technically optional per the schema, so blocks that omit the element are skipped silently rather than emitting a noisy "missing" warning — they already render with the rest of the PmtInf above and the maintainer can see the absence. Pairs naturally with the schema-version detection from draft 19 (the `detected.type` value drives both the banner and the expected-PmtMtd lookup). 2 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed pain.001 with `PmtMtd=TRF` → confirmation citing the TRF expectation; same fixture with TRF rewritten to DD → mismatch row citing both the declared DD and pain.001's TRF requirement). Audit OK. Full suite: 278 → 280 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 39):** §2.7 PEPPOL UBL inspector gains a `DueDate` vs `IssueDate` temporal sanity check. EN 16931 declares `IssueDate` as BT-2 and `DueDate` as BT-9; neither EN 16931 nor the BIS Billing 3.0 schematron names a numbered business rule for the temporal ordering (only `Format` and `Code-list` rules apply to the dates themselves), but PEPPOL access-point implementations universally reject the backdated case because payment can't fall due before the invoice is even issued — it's the canonical data-entry error the cross-check exists to surface. The new `ublCheckDates()` reads both ISO 8601 dates from the root, parses them via `Date.parse()`, and surfaces an explicit row: green `✓ DueDate D is on or after IssueDate I` confirmation when both parse cleanly and `dueTs >= issueTs`, red `✗ DueDate D is earlier than IssueDate I — payment cannot fall due before the invoice is issued` row when the order is reversed. Skips silently when either date is missing or unparseable — the existing raw `Issue date` / `Due date` rows in `ublRenderHeader` already surface the underlying values, so a missing date is visible upstream without needing a duplicate warning here. Distinct from the totals consistency check (draft 28), VAT-rate check (draft 37), currency check (draft 31), and IBAN check (draft 36) — those are arithmetic / format invariants, this is the first *temporal* invariant on the UBL side, mirroring the LOTL freshness check from draft 34 in shape. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed Invoice from the shared fixture with IssueDate 2024-04-01 / DueDate 2024-05-01 → confirmation row mentioning both dates; same fixture with DueDate rewritten to 2024-03-15 → mismatch row citing both values). Audit OK. Full suite: 276 → 278 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 38):** §2.4 SEPA inspector gains an IBAN ↔ BIC country-code consistency check — a different shape from the existing scalar cross-checks (IBAN MOD-97 from draft 16, BIC format from draft 27, ISO 4217 currency from draft 29, NbOfTxs/CtrlSum from draft 35). This one is *relational*: it cares about how two values pair within the same scope. ISO 13616 puts the country in IBAN chars 0-1; ISO 9362 puts the country in BIC chars 4-5 (the BIC country sub-code, immediately after the institution code). Within a SEPA scope, the two MUST agree: `<DbtrAcct>/<IBAN>` paired with `<DbtrAgt>/<BIC|BICFI>` at PmtInf level, and `<CdtrAcct>/<IBAN>` paired with `<CdtrAgt>/<BIC|BICFI>` at transaction level (both `CdtTrfTxInf` for credit transfers and `DrctDbtTxInf` for direct debits). A German IBAN routed via a French agent is almost always a copy-paste error and is rejected at clearing (EBA STEP2, EBA Clearing, STET, RT1 — all SEPA clearing rails surface "IBAN/BIC country mismatch" as a hard reject reason). The new `sepaPairIbanBic()` helper extracts `(IBAN, BIC)` sibling pairs from a parent element by walking the standard ISO 20022 child names (`DbtrAcct + DbtrAgt`, `CdtrAcct + CdtrAgt`); `sepaCheckIbanBicCountryParity()` aggregates pairs across every PmtInf and every transaction-info element, then renders one `✓ IBAN ↔ BIC country codes agree across all account/agent pairs (N)` confirmation row across all matching pairs or one `✗ IBAN ↔ BIC country mismatch: IBAN X declares country CC1 but BIC Y declares CC2` row per offending pair. Only pairs that have both an IBAN and a BIC (or BICFI) participate — half-filled pairs skip silently. Case-folded and whitespace-stripped on extraction so user-entered SEPA samples with formatting drift still pair correctly. 2 new strings × 5 locales (en/fr/de reviewed against ISO 13616 / 9362 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed German pain.001 → confirmation; German IBAN with COBADEFFXXX swapped for BNPAFRPPXXX → mismatch row citing both the full IBAN and the French BIC). Audit OK. Full suite: 274 → 276 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 37):** §2.7 UBL inspector gains a per-`TaxSubtotal` VAT-rate consistency check enforcing EN 16931 BR-CO-17 (and the parallel category-letter rules BR-S-08 / BR-Z-08 / BR-E-08 / BR-AE-08 / BR-K-08 / BR-G-08 / BR-O-08 / BR-IC-08, which are identical in effect). PEPPOL BIS Billing 3.0 invoices break down the VAT total into one `<cac:TaxSubtotal>` per rate, each declaring `TaxableAmount`, `TaxCategory/Percent`, and `TaxAmount` — the three values that EN 16931 requires to agree. The new `ublCheckVatRates()` walks every subtotal across every `<cac:TaxTotal>`, computes `Math.round((TaxableAmount × Percent) / 100)` in integer cents (matching EN 16931's "round once per subtotal" policy — double rounding via tenths-of-cent would over-tolerate small drift), and compares against the declared `TaxAmount` with the standard ±1¢ rounding tolerance the spec allows on fractional currencies. Per-subtotal `✓ TaxSubtotal #N: TaxAmount matches TaxableAmount × X% = Y (EN 16931 BR-CO-17)` confirmation rows or `✗ TaxSubtotal #N VAT-rate mismatch: declared TaxAmount A but TaxableAmount × X% = B` warning rows. Catches the canonical test-invoice error this rule exists to prevent: typing the wrong monetary amount for a given rate (e.g. 19.50 EUR tax on a 100.00 EUR base at 19% where the correct value is 19.00). Distinct from the totals check shipped in draft 28: that check verified `Σ subtotals === header total`, this one verifies each subtotal is internally consistent. The two checks together fully cover EN 16931's tax-arithmetic invariants. Subtotals missing any of the three fields skip silently (the per-rate breakdown already surfaces the raw values). Amounts formatted via `intlNumber()` with two fraction digits so locale decimal separators apply. 2 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed 19% subtotal on a 100 EUR base → confirmation row mentioning BR-CO-17; 19.50 EUR declared on a 100 × 19% base, with the header `TaxAmount` rewritten to match so the totals check from draft 28 doesn't also fire → mismatch row citing both 19.50 and the expected 19.00). Audit OK. Full suite: 272 → 274 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 36):** §2.7 UBL inspector gains a Payee/Payer-account IBAN MOD-97 cross-check (parallel to the SEPA IBAN cross-check from draft 16). PEPPOL BIS Billing 3.0 invoices declare the supplier's bank coordinates in `<cac:PaymentMeans>/<cac:PayeeFinancialAccount>/<cbc:ID>` (or `<cac:PayerFinancialAccount>` for self-billing); the schema doesn't enforce that the value is an IBAN, so the inspector first regex-checks each ID against the ISO 13616 IBAN shape (`CC + 2 digits + 11–30 alnum`, length 15–34) and only runs the `ibanValidate()` MOD-97 walk on values that match. Confirmation row `✓ Payee/Payer financial account IBANs valid (ISO 13616 MOD-97) (N)` on full success; one warning row per failing IBAN otherwise. Non-IBAN account numbers (BBANs, US ABA routing numbers, anything outside the IBAN regex) are silently skipped — they're not the wrong identifier, just a different one. Closes a real gap where a maintainer could paste a PEPPOL invoice with a typo'd IBAN and see the inspector parse it cleanly, while a downstream payments processor would reject it at MOD-97 time. Same shape and surfacing as the SEPA IBAN cross-check, with the regex prefilter being the only meaningful difference (UBL allows non-IBAN account numbers; SEPA forbids them). 2 new strings × 5 locales (en/fr/de reviewed against ISO 13616 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (well-formed `DE89370400440532013000` injected into a `<cac:PaymentMeans>` block surfaces the confirmation row + ISO 13616 reference; final-digit flip → warning row mentioning the offending IBAN). Audit OK. Full suite: 270 → 272 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 35):** §2.4 SEPA inspector gains `NbOfTxs` + `CtrlSum` group-header invariant checks — the same shape as the UBL totals consistency check from draft 28 and the parallel BIC / currency cross-checks shipped in drafts 27 / 29. pain.001 (credit-transfer initiation) and pain.008 (direct-debit) files declare two header-level totals in `GrpHdr`: `NbOfTxs` (count) and `CtrlSum` (sum of transaction amounts). EPC SEPA rulebooks make a mismatch a hard reject reason on the bank side, so surfacing it on the inspector matches the real-world failure mode. The new `sepaCheckGrpHdrInvariants()` walks every `<CdtTrfTxInf>` + `<DrctDbtTxInf>` across all `PmtInf` blocks, then: (a) compares the count against `GrpHdr/NbOfTxs` — green `✓ NbOfTxs matches transaction count (N)` confirmation or red `✗ NbOfTxs mismatch: header declares X but file contains Y transactions` warning; (b) sums every transaction-level `<InstdAmt>` (credit-transfers) / `<Amt>` (direct-debits) in integer cents (`Math.round(value * 100)` to dodge IEEE-754 drift) with ±1¢ tolerance per the ISO 20022 implementation-guide rounding policy — green `✓ CtrlSum matches transaction total (sum)` or red `✗ CtrlSum mismatch: header declares X but transaction amounts sum to Y` warning. Both gates are independent; the CtrlSum check only fires when CtrlSum is present (it's optional per the schema). Amounts are formatted via `intlNumber()` with `minimumFractionDigits: 2` so the locale's decimal separator applies (`100,00` in de/fr, `100.00` in en). 4 new strings × 5 locales (en/fr/de reviewed against ISO 20022 / EPC SEPA vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Three new Playwright cases (happy path — both invariants confirm on SEPA_PAIN001; NbOfTxs overcounted from 1→2 surfaces the count mismatch; CtrlSum undersummed from 100.00→50.00 surfaces the amount mismatch). Audit OK. Full suite: 267 → 270 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 34):** §2.6 LOTL / TSL viewer gains a `NextUpdate` freshness check (ETSI TS 119 612 §5.3.13). The existing scheme-information block already rendered the raw `NextUpdate/dateTime` value as a key-value row; the inspector now additionally parses that timestamp, compares it against `Date.now()`, and surfaces a freshness row beneath it — green `✓ Trust list is current — next update due in N days (ETSI TS 119 612 §5.3.13)` when the horizon is still in the future, red `✗ Trust list is stale — NextUpdate was N days ago; refetch from ec.europa.eu before relying on this list (ETSI TS 119 612 §5.3.13)` when it has lapsed. The signal matters because the LOTL / TSL operator commits to a refresh cadence; accepting trust decisions from a stale XML can mean continuing to trust a CA that has since been withdrawn or downgraded at the EU level. Day delta is rendered via the existing `intlNumber()` so locale thousands separators apply on month-scale numbers. Guarded: no row is added if `NextUpdate` is missing or unparseable (the existing raw row still shows). Mirrors the JWT `exp` / `nbf` pattern of "show the raw claim, then a separate ✓ / ✗ row that gates acceptance." 2 new message strings (`lotl.warn.stale`, `lotl.ok.current`) + 1 row label (`lotl.row.freshness`) × 5 locales (en/fr/de reviewed against ETSI TS 119 612 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases — NextUpdate 30 days in the future → current row + ETSI reference; NextUpdate 90 days in the past → stale row + `ec.europa.eu` refresh pointer. Audit OK. Full suite: 265 → 267 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 33):** Two more JWT-inspector header-side rows shipped, completing the RFC 7515 §4.1 "the four headers a maintainer most often wants to eyeball" set (alg / kid / typ / crit). (a) **`typ` (token type, RFC 7515 §4.1.9 + RFC 8725 §3.11)** — informational muted badge sourced from the header. RFC 8725 §3.11 names explicit token typing as the standard defense against token-type confusion attacks: an OAuth access token tagged `at+jwt` (RFC 9068) must not be accepted as an OIDC `id_token+jwt`, an SD-JWT VC tagged `vc+sd-jwt` must not be accepted as a plain `JWT`, etc. Surfacing `typ` lets a maintainer reading a token confirm the declared media type matches the slot the token is being used in, without grepping the raw header JSON. (b) **`crit` (critical headers, RFC 7515 §4.1.11)** — danger red badge. When `crit` is present the verifier MUST understand every header parameter named in the array, or reject the token; silently accepting a token with unknown critical extensions is the textbook security failure the field exists to prevent. The inspector can't know what extensions the maintainer's verifier supports, so it surfaces the names via the existing `intlList()` helper (`exp und b64` in de, `exp et b64` in fr) along with the RFC 7515 §4.1.11 reference. Only fires when `crit` is a non-empty array per the RFC's "MUST be a non-empty array" requirement. 2 new strings × 5 locales (en/fr/de reviewed against RFC 7515 + 8725 + 9068 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). One new Playwright case asserting both rows against a hand-crafted token with `typ: "at+jwt"` and `crit: ["exp", "b64"]`. Audit OK. Full suite: 264 → 265 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 32):** Two more JWT-inspector informational rows shipped, both following the same shape as the draft 30 `sub` row. (a) **`kid` (key ID, RFC 7515 §4.1.4)** sourced from the header — surfaced as a muted badge right after the alg=none warning slot. JWKS-driven verifiers use this hint to pick the right key from a key set; surfacing it lets a maintainer cross-reference a token against a JWKS URL without parsing the raw header JSON by eye. (b) **`jti` (JWT ID, RFC 7519 §4.1.7)** sourced from the payload — surfaced as a muted badge right after the `sub` row. Issuers attach `jti` for replay-defense and audit-log correlation; reading it from a badge is faster than grepping the raw payload. Both honor string or numeric values per RFC 7519's leave-it-to-the-application typing rule (some issuers use sequence numbers, some use UUIDs). 2 new strings × 5 locales (en/fr/de reviewed against RFC 7515 + 7519 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). One new Playwright case asserting both rows render against a hand-crafted token with kid in the header and jti in the payload. Audit OK. Full suite: 263 → 264 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 31):** UBL ISO 4217 currency-code cross-check shipped (§2.7 polish). Parallel to the SEPA cross-check from draft 29, the PEPPOL UBL inspector now walks every UBL element carrying a `currencyID="..."` attribute (the UBL counterpart of ISO 20022's `Ccy="..."` — present on every `*Amount` element: `LineExtensionAmount`, `TaxAmount`, `PriceAmount`, `TaxInclusiveAmount`, `PayableAmount`, …) and verifies each unique code against the inline `ISO_4217_ACTIVE` set that already powers the SEPA cross-check (no second copy — the UBL function reaches the same constant). On full success: one confirmation row listing each unique code via `intlList()` so the locale joiner applies (`EUR und USD` in de, `EUR et USD` in fr). On failure: one warning row per offending code. Catches the canonical typo failure mode (`ERU` for `EUR`) which the totals-consistency check from draft 28 wouldn't see, because all amounts can still balance against themselves with the wrong code declared. Closes the §2.7 spec line "currency code is ISO 4217" which was described but not enforced. 2 new strings × 5 locales (en/fr/de reviewed against e-invoicing vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (EUR happy path; ERU typo flagged across every currencyID attribute). Audit OK. Full suite: 261 → 263 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 30):** Two JWT-inspector polishes shipped. (a) **`alg: "none"` warning row** (RFC 8725 §3.1 / JWT BCP). The `alg=none` algorithm-confusion attack is the canonical JWT footgun — any token whose header declares it is by definition unsigned, and accepting it as authentic has caused real-world bypasses. jwtRender now surfaces an unconditional red badge immediately after the header/payload/signature blocks whenever `header.alg.toLowerCase() === 'none'` (case-insensitive because `None` / `NONE` show up in the wild). (b) **`sub` (subject) claim row** (RFC 7519 §4.1.2). The subject identifier the JWT carries claims about now renders as its own informational badge after the existing `iat` row — a maintainer reading a token no longer has to eyeball the raw JSON to find the user it identifies. Both string and numeric `sub` values are honored (RFC 7519 leaves the type to the application). 2 new strings × 5 locales (en/fr/de reviewed against RFC 8725 + 7519 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). Two new Playwright cases (alg=none surfaces RFC 8725 mention with the JWT header row text; RFC 7519 §A.1 fixture's `sub=1234567890` surfaces in the new row). Audit OK. Full suite: 259 → 261 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 29):** SEPA ISO 4217 currency-code cross-check shipped (§2.4 polish). The inspector now walks every element with a `Ccy` attribute (`<Amt>`, `<InstdAmt>`, `<NtryAmt>`, `<Bal>/<Amt>`, `<EqvtAmt>` and any other `Ccy`-bearing element ISO 20022 may add in a future minor revision) and verifies the currency code against an inline ISO 4217:2015 (Amendment 174) active-currencies set (≈ 180 codes, current as of 2025). On full success: one confirmation row listing each unique code via the existing `intlList()` helper, so the German variant reads `EUR und USD`, French reads `EUR et USD`, etc. On failure: one warning row per offending code. Historic codes (ITL, DEM, ROL pre-2005, etc.) are intentionally excluded from the active set — pasting an unmigrated wire format surfaces the cleanup task rather than being silently accepted. Closes the §2.4 spec line "currency code is ISO 4217" which had been described but not enforced since the original SEPA inspector landed in draft 16. 2 new strings × 5 locales (en/fr/de reviewed; zh-CN/hi machine-quality drafts pending native review). Two new Playwright cases (EUR happy path; ERU typo flagged). Audit OK. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 28):** UBL totals consistency check shipped (§2.7 polish). The PEPPOL UBL inspector now verifies the two EN 16931 invariants that are independent of any optional `PrepaidAmount` / `PayableRoundingAmount` complications and surfaces an explicit ✓ / ✗ row for each:

- **Line-sum invariant.** `Σ InvoiceLine/LineExtensionAmount === LegalMonetaryTotal/LineExtensionAmount`. Catches misadded line totals (the most common e-invoice arithmetic error in the wild).
- **Tax-inclusive invariant.** `LegalMonetaryTotal/TaxExclusiveAmount + Σ TaxTotal/TaxAmount === LegalMonetaryTotal/TaxInclusiveAmount`. Catches mismatches between the header VAT total and the sum of per-scheme tax amounts.

Comparison is done in integer cents (`Math.round(value * 100)`) with a ±1¢ drift tolerance per the EN 16931 rounding policy, so a single rounding-induced 0.01 EUR delta does not produce a false positive but a 0.10 EUR misadd does. Only `TaxTotal/TaxAmount` (the per-scheme grand totals) are summed for invariant #2 — `TaxSubtotal/TaxAmount` rows are skipped via a parent localName check so per-rate sub-rows don't get double-counted.

5 new strings × 5 locales (en/fr/de reviewed against EN 16931 vocabulary; zh-CN/hi machine-quality drafts pending native review). Three new Playwright cases (well-formed Invoice passes both invariants, declared LineExtensionAmount mismatch flagged, declared TaxInclusive mismatch flagged). Audit OK. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 27):** SEPA BIC format cross-check shipped (§2.4 polish). The inspector already cross-checked every `<IBAN>` element against MOD-97 via the standalone IBAN tool's validator; the same pattern now applies to every `<BIC>` and `<BICFI>` element (`<BIC>` is the pain.001.001.03 form, `<BICFI>` the 2009+ schema form — both are walked). On full success: one confirmation row `BIC format valid (ISO 9362) (N)`; on failure: one warning row per bad BIC. Catches the canonical typo failure mode (BIC pasted into the wrong column, character flips, lowercase) that the IBAN MOD-97 alone wouldn't surface. 4 new strings × 5 locales (en/fr/de reviewed against ISO 9362 vocabulary; zh-CN/hi machine-quality drafts pending native review). Two new Playwright cases (valid-BIC happy path, malformed-bank-code rejection). Audit OK. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 26):** Main-JWT `iss` / `aud` verifier cross-checks shipped. The collapsible verifier-checks panel under the JWT input is now split into two labeled groups — "JWT (RFC 7519)" with `iss` + `aud` inputs, and "SD-JWT KB-JWT" with the existing KB-JWT `aud` + `nonce` inputs. After the time-claim badges, jwtRender consults the new inputs and renders independent ✓ / ✗ rows: green when `iss` exactly matches the expected issuer (RFC 7519 §4.1.1); green when `aud` contains the expected value (§4.1.3 — string or array form honored); red with the received value on mismatch; red with an explicit "no iss/aud claim in JWT" row when the claim is absent (so a verifier never silently passes a check). 13 new strings × 5 locales (en/fr/de reviewed against RFC 7519 vocabulary; zh-CN/hi machine-quality drafts pending native review). Three new Playwright cases (iss match, iss mismatch surfaces received value, aud array-form match). Audit OK. Full suite expected ≈ 250 → 253 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 25):** Two cross-cutting polishes shipped.

- **JWT time-claim row coverage.** Decoder now renders `exp`, `nbf`, and `iat` as independent badges (red / green / neutral) instead of only `exp`. RFC 7519 §4.1.5 makes `nbf` a separate acceptance gate — a token can be `exp`-valid but `nbf`-future and the inspector now surfaces that on its own line. `iat` is informational only (issuers populate it for audit/replay-window math; it does not gate acceptance). 3 new strings (`jwt.nbf.notYet` / `jwt.nbf.active` / `jwt.iat.issued`) × 5 locales (en/fr/de reviewed against RFC 7519 vocabulary; zh-CN/hi machine-quality drafts pending native review per the project pattern). 2 new Playwright cases (nbf future → red row; nbf past → active green row). The existing RFC 7519 §A.1 fixture test was extended to additionally assert the iat ISO row, so the path is exercised against a canonical wire-format token.
- **Phase 1 §1.5 #4 close-out — plural-key parity audit.** `scripts/audit-release.js` walks every key in `STRINGS.en` whose final segment matches one of the six CLDR plural rule names (`.zero|.one|.two|.few|.many|.other`), groups them by base key, and fails if any group is missing `.other` (the universal Intl.PluralRules fallback — without it, `intlPlural()` would fall through to the bare base key and surface `undefined` to the user). Non-en locales are intentionally not checked: German plurals collapse to `.other`, Chinese has no plural distinction at all — requiring `.one` in those locales would be wrong. The plural-key convention is now documented inline at the top of `scripts/export-strings-csv.js` so reviewers and contributors discover it without having to read the spec.

Full suite: 247 → 250 passed. Audit OK. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 24):** Phase 7 §2.8 KB-JWT verifier-side `aud` / `nonce` cross-checks shipped. The JWT inspector exposes an optional collapsible verifier panel with two inputs (expected audience, expected nonce); when filled they are compared against the KB-JWT payload claims and rendered as explicit ✓ / ✗ rows, with the received value surfaced on mismatch and an explicit "missing claim" row when the KB-JWT omits the field. RFC 7519 array-form `aud` is honored (match if the expected value is in the array). Inputs are not persisted — per-decode replay-defense values, not preferences. 13 new UI strings × 5 locales (en/fr/de reviewed; zh-CN/hi machine-quality drafts). Three new Playwright cases (aud match, aud mismatch with received value, nonce mismatch with received value). Full suite: 244 → 247 passed. Audit OK. No new vendored deps, no CSP change, tool count unchanged. Closes the draft 23 `aud` / `nonce` deferral; the only remaining §2.8 follow-up is the ISO mdoc CBOR decoder.

**Progress (draft 23):** Phase 7 §2.8 KB-JWT verification + sd_hash binding shipped. When the SD-JWT input carries a trailing KB-JWT and the issuer-signed payload declares a `cnf.jwk`, the inspector imports the JWK via Web Crypto (ES256 / ES384 / EdDSA / RS-family supported) and verifies the KB-JWT signature inline. The `sd_hash` claim is recomputed against `base64url(SHA-256(<jwt>~<disc>~…~))` per SD-JWT draft §4.3 so a swapped KB-JWT or substituted disclosures are flagged with an explicit ✗ row. Seven new UI strings × 5 locales (en/fr/de reviewed; zh-CN/hi machine-quality drafts pending native review). Two new Playwright cases using on-the-fly node-generated P-256 keypairs (happy-path verify; tampered-signature rejection). Full suite: 242 → 244 passed. Audit OK. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 22):** Phase 7 polish slice — §2.4 SEPA gains per-PmtInf `ReqdExctnDt` (requested execution date; both schema shapes handled) and per-transaction `MndtId` (mandate ID, pain.008 direct debits via `DrctDbtTx/MndtRltdInf/MndtId`). §2.7 UBL gains a "Copy as CSV" button on the line items table — fixed-column-order export (`#,Name,Quantity,UnitCode,Price,PriceCurrency,LineAmount,LineCurrency`) with RFC 4180 quoting. Five new UI strings × 5 locales (2 SEPA + 3 UBL; en/fr/de reviewed against the banking/e-invoicing glossary; zh-CN/hi machine-quality drafts pending native review per project pattern). Three new Playwright cases. Full suite: 239 → 242 passed. Audit OK. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 21):** SD-JWT cleartext-payload reconstruction view shipped + `bytesToBase64()` consolidation. The verifier-side disclosure-resolution algorithm from SD-JWT draft §7 now renders as a `Disclosed payload (reconstructed)` block right after the disclosure table — every matched `_sd` digest is replaced by its disclosed claim, array `{"...": digest}` placeholders are spliced back in, and `_sd`/`_sd_alg` are stripped. Digests without a matching disclosure are dropped (the holder withheld them; the verifier wouldn't see them either). One new UI string (`jwt.sdjwt.reconstructed`) × 5 locales. One new Playwright case. The duplicate `bytesToBase64()` definition (the second one hoisted over the first and dropped the `urlSafe` flag) was deleted; sdJwtRender lost its inline base64url workaround. Full suite: 238 → 239 passed. No new vendored deps, no CSP change, tool count unchanged.

**Progress (draft 20):** Phase 7 §2.8 first slice shipped — SD-JWT decoder folded into the existing JWT inspector (no new top-level tool, tool count stays at 45). When the input contains a `~`, the inspector splits off the leading JWT (decoded as today), then for each disclosure base64url-decodes it to its `[salt, name, value]` or `[salt, value]` triple and computes the SHA-256 digest over the wire-bytes; that digest is cross-checked against every `_sd` array reachable from the payload (top-level and nested), so each disclosure renders with a `✓ referenced in payload _sd` or `✗ unreferenced` flag. The `_sd_alg` payload claim is honored (defaults to `sha-256` per the SD-JWT draft §5.1). A trailing segment that parses as `<b64url>.<b64url>.<b64url>` is treated as the key-binding JWT and surfaced as a separate decoded JSON block; KB-JWT signature verification, holder-key resolution from `cnf`, and the cleartext-payload reconstruction view are deferred. 10 new UI strings (`jwt.sdjwt.*`) × 5 locales (en/fr/de reviewed against the SD-JWT draft's vocabulary; zh-CN/hi machine-quality drafts pending native review). Three new Playwright cases (digest-matched disclosures, orphaned disclosure flagged, KB-JWT surfaced). Full suite: 235 → 238 passed. ISO mdoc CBOR decoder + the rest of §2.8 deferred to a follow-up. One incidental bug noted but worked around inline: the file ships two `bytesToBase64()` definitions and the second hoists over the first, dropping the `urlSafe` flag — sdJwtRender inlines its own base64url path; a cleaner consolidation is a separate cleanup.

**Progress (draft 19):** SEPA inspector polish — schema-version detection (`pain.001.001.03 (customer credit-transfer initiation)` in the document banner instead of just `pain.001`) and per-transaction `<InstrId>` surfaced alongside `<EndToEndId>`. One new UI string (`sepa.tx.instrId`) × 5 locales. Two new Playwright cases. Full suite 233 → 235 passed. No tool-count change. The two items called out as "not in this slice" in the draft 16 notes are now done; the only remaining §2.4 deferral is XAdES-signed SEPA files, which is properly §2.5 territory.

**Progress (draft 18):** Phase 7 §2.7 first slice shipped — PEPPOL UBL e-invoice inspector. Namespace-blind `DOMParser` walk by `localName` (same pattern as the SEPA and LOTL inspectors) handles both OASIS UBL 2.1 root document types: `<Invoice>` (PEPPOL BIS Billing 3.0 / EN 16931) and `<CreditNote>` (which reuses the same line-item path but pulls `CreditNoteLine` / `CreditedQuantity` instead of `InvoiceLine` / `InvoicedQuantity`). Surfaced fields: `CustomizationID`, `ProfileID`, invoice number, issue and due dates, document currency, supplier / customer name (`PartyName/Name` preferred, falls back to `PartyLegalEntity/RegistrationName`), the four `LegalMonetaryTotal` amounts, per-line item (quantity with `unitCode`, unit price, item name, line amount), and per-rate `TaxSubtotal` breakdown. All currency-tagged amounts render with their `currencyID`. No vendored deps — reused the `sepaFirst` / `sepaTextOf` / `sepaAllLocal` helpers as generic namespace-blind XML walkers. ~160 lines of parser + render JS, ~32 new UI strings × 5 locales (en/fr/de reviewed against standard e-invoicing terminology; zh-CN/hi machine-quality drafts pending native review per the project pattern), 5 SEO prose keys en-only via the existing Phase 7 carve-out. Tool count 44 → 45 — JSON-LD `featureList` + `featureList_count`, meta / og / twitter descriptions, and README all updated. Three new Playwright cases (Invoice happy path with line + tax breakdown, CreditNote happy path, non-UBL XML rejected). Full suite: 233 passed. Factur-X / ZUGFeRD hybrid PDF + UN/CEFACT CII deferred until the §2.5 PDF attachment extractor lands. One incidental fix landed in the same change: the shared `sepaFirst()` / `sepaCollectIbans()` walkers were upgraded from `getElementsByTagName(name)` to `getElementsByTagNameNS('*', name)` so they match by local name across every namespace — the unprefixed form was missing PEPPOL UBL's `cbc:` / `cac:` elements. The SEPA and LOTL inspectors are unaffected (their docs use default namespaces, where both forms return the same nodes).

**Progress (draft 17):** Phase 7 §2.6 eIDAS LOTL / TSL viewer shipped. Same namespace-blind `DOMParser` pattern as the SEPA inspector (§2.4): walk by `localName`, prefer `xml:lang="en"` for multi-language `<Name>` blocks. Two modes — LOTL renders the per-country `<OtherTSLPointer>` entries with their `<TSLLocation>` URLs; TSL renders per-`<TrustServiceProvider>` services with type / status / starting time and acknowledges embedded `<X509Certificate>` blocks without parsing them inline (defers to the TLS Certificate Parser). The §0.2 / §2.6 "no fetching" rule is honored — a footer note on every parse points the user to `ec.europa.eu` for the official LOTL file. No vendored deps. ~190 lines of parser + render JS, ~25 new UI strings × 5 locales (en/fr/de reviewed; zh-CN/hi machine-quality drafts), 5 SEO prose keys en-only. Tool count 43 → 44 — JSON-LD featureList + count, meta / og / twitter descriptions, README updated. Three new Playwright cases (LOTL mode happy path, TSL mode happy path, non-trust-list XML rejected). Full suite: 230 passed.

**Progress (draft 16):** Phase 7 §2.4 SEPA ISO 20022 XML inspector shipped — first larger Phase-7 tool. Namespace-blind `DOMParser` walk by `localName` handles pain.001 (credit-transfer initiation, all minor revisions), camt.053 (bank-to-customer statement), and pain.008 (direct-debit, inherits the pain.001 renderer). Every `<IBAN>` element is cross-checked against the existing `ibanValidate()` MOD-97 implementation and bad checksums surface inline as warning rows. UI mirrors the IBAN / BIC / VAT layout (textarea + Parse button). No vendored deps. ~200 lines of parser + render JS, ~12 new UI strings × 5 locales (en/fr/de reviewed, zh-CN/hi machine-quality drafts per project pattern), 5 SEO prose keys en-only via the existing Phase 7 carve-out. Tool count 42 → 43 — JSON-LD `featureList` + `featureList_count`, meta/og/twitter descriptions, and README all updated. Four new Playwright cases (pain.001 happy path, camt.053 happy path, MOD-97 failure flagged, non-ISO-20022 XML rejected). Full suite: 227 passed.

**Progress (draft 15):** Phase 1 §1.6 command palette shipped end-to-end. Modal dialog (~14 lines of CSS, 13 lines of HTML) with `role="dialog"` / `aria-modal` / `aria-labelledby` + `role="listbox"` results, triggered by `⌘K` / `Ctrl+K`, closes on `Esc` / overlay-click. Filter haystack = tool-id + localized label + English synonyms, locale-folded via `toLocaleLowerCase(lang)`; multi-term queries AND'd. `TOOL_SYNONYMS` ships English-only (42 entries) per the §1.6 directive to defer locale-specific synonyms until §1.3 native review — polyglot fallback ensures English queries always match in any locale. Three new UI strings (`palette.title`, `palette.placeholder`, `palette.empty`) translated for all five locales; STRINGS now 1101 keys × 5 locales. Seven new Playwright cases (Ctrl+K open/Esc close, "swift" → BIC, "epoch" → timestamp, multi-term "ed signature" → Ed25519, ArrowDown+Enter, empty state, fr + en-query polyglot invariant). Full suite: 223 passed.

**Progress (draft 14):** Phase 1 §1.5 third slice — first `Intl.ListFormat` call-site. The BIC validator's multi-tag row now reads `intlList(r.tags)` instead of `r.tags.join(', ')`, so a German-locale BIC with location ending in `0` and branch `XXX` renders as `Test-BIC (Ort endet auf 0) und Hauptniederlassung (XXX)`. Other `.join(', ')` sites (TLS SANs, CIDR / cron internal fields, X.509 DN parts) intentionally left alone because they are technical token lists, not prose. One new Playwright case asserts the German `und` connector. Full suite: 216 passed. With this slice, all four `Intl.*` helpers (`NumberFormat`, `RelativeTimeFormat`, `PluralRules`, `ListFormat`) have at least one production call-site; §1.5 is closed for Phase 1.

**Progress (draft 13):** Phase 1 §1.4 step 1 (logical-property CSS refactor). All 17 physical-direction declarations in `index.html` migrated to their logical equivalents (`margin-inline-start`, `padding-inline-start`, `inset-inline-end`, `border-inline-start`, `text-align: start`) — split across the stylesheet (8 sites) and inline button styles (9 sites). One new Playwright case asserts the refactor by flipping `documentElement.dir = 'rtl'` and reading `getComputedStyle(...).insetInlineEnd` on `.tool-card::after`. Full suite: 215 passed. Steps 2–6 of §1.4 (runtime dir flip in setLang, icon mirroring, LTR-embed for code blocks, word-break for CJK) deferred until the first RTL locale ships.

**Progress (draft 12):** Phase 1 §1.5 second slice. `intlPlural()` + `intlList()` helpers added; the regex tool's match-count row migrated to plural-aware keys (`regex.match.count.one` + `regex.match.count.other` across all five locales). The legacy `regex.match.count: '{n} match(es)'` key was retired in the same commit — the parenthesized fallback was the textbook example of why `Intl.PluralRules` exists. STRINGS now 1098 keys × 5 locales. Two new Playwright cases (en singular/plural + fr singular/plural). Full suite: 214 passed. `intlList` is scaffolded but not yet wired to a call-site; first candidate is multi-recipient encryption or Shamir share enumeration, deferred until the surrounding strings get a §1.3 review.

**Progress (draft 11):** Phase 1 §1.5 first slice. `intlNumber()` + `intlRelativeFromNow()` helpers added next to `t()` / `tFormat()`; four call-sites converted (PBKDF2 timing, CIDR `Total`/`Usable`, PGP key `Created`/`Expires`, Unix-timestamp `Relative`). Legacy `timestamp.rel.*` template strings kept as fallback for browsers that pre-date `Intl.RelativeTimeFormat`. Three new Playwright cases (fr PBKDF2, de CIDR, fr relative phrasing). Full suite: 212 passed. No new vendored deps; no CSP change; no new top-level strings. `PluralRules` / `ListFormat` deferred.

**Progress (draft 9):** PWA precache service worker shipped (§4.5). `sw.js` precaches the static shell (locale variants, vendored libs, icons, manifest, privacy page) on install and serves cache-first thereafter. Registration is HTTPS-gated, scope `/`, silent on failure. The document CSP keeps `connect-src 'none'`; a narrow path-scoped `_headers` override grants the SW response `connect-src 'self'` (needed for the install-time precache fetches and nothing else). No new vendored dependencies, no innerHTML, no behavior change for non-PWA users. Audit (`scripts/audit-release.js`) still passes — CSP regex matches the default document rule, which is unchanged.

**Progress (draft 8):** GDPR-by-design surface + regional SEO aliases shipped.

- `/privacy.html` (§5.1) — self-contained ~200-line static page, light/dark via `prefers-color-scheme`, no JS, no external assets. Footer link added to the SPA with `footer.privacy` / `footer.privacyAria` translated for en/fr/de/zh-CN/hi. Localized privacy variants deferred (translation-quality bar).
- Regional English hreflang aliases (§5.3) — `en-GB`, `en-IE`, `en-AU`, `en-NZ`, `en-ZA`, `en-IN` added to `index.html` and to every `<url>` block in the sitemap via `scripts/build-sitemap.js`. Locale variants inherit the aliases via `build-i18n-variants.js`.

**Progress (draft 7):** Regulator-preset annual freshness audit + framing docs shipped.

- Each entry in `REGULATOR_PRESETS` now carries an `asOf: 'YYYY-MM-DD'` stamp; the PGP key generator surfaces it inline in the preset's help footnote ("parameters verified 2026-05-13"). New string `preset.note.verifiedPrefix` translated for en/fr/de/zh-CN/hi.
- `scripts/audit-release.js` gains an 11th check that warns when any preset is more than 12 months old. Warn rather than fail — staleness is visible but does not block a release.
- `SECURITY.md` (disclosure policy, response SLAs, in/out-of-scope threat classes, build-verification chain) and `THREAT-MODEL.md` (what protects, what does not, recommendations by user type) shipped at the repo root, fulfilling §5.2.

### Phase 5 — Distribution and sovereignty

1. Codeberg mirror (§4.1).
2. ✅ Reproducible release manifest (§4.2) — generator shipped draft 6; signing deferred.
3. ✅ SBOM (§4.3) — shipped draft 6.
4. ✅ Single-file portable build (§4.6) — shipped draft 6.
5. ✅ PWA precache (§4.5) — shipped draft 9.
6. Tor onion mirror (§4.4).
7. Submissions to public-good registries (§4.7).

### Phase 6 — Regulator presets

1. ✅ Implement the dropdown + JSON config (§3) — shipped draft 5 (PGP key generator) and draft 10 (PBKDF2 tool).
2. ✅ First sources: BSI TR-02102, ANSSI RGS, NIST SP 800-57 / SP 800-132, Privacy Guides — all four regulators surfaced in both tools.
3. Argon2id presets pending §2.9 (the Argon2id tool itself is deferred to Phase 7).

### Phase 7 — Larger tool builds

1. 🟡 SEPA XML inspector (§2.4) — pain.001 + camt.053 shipped draft 16; pain.008 inherits the pain.001 renderer; XAdES-signed files deferred to §2.5.
2. eIDAS signature inspector — PAdES/CAdES/XAdES (§2.5).
3. ✅ EU LOTL viewer (§2.6) — shipped draft 17.
4. 🟡 Factur-X / ZUGFeRD / PEPPOL inspector (§2.7) — PEPPOL UBL Invoice / CreditNote shipped draft 18; Factur-X / ZUGFeRD hybrid PDF + UN/CEFACT CII deferred until the §2.5 PDF attachment extractor lands.
5. 🟡 EUDI Wallet credential decoder (§2.8) — SD-JWT decoder shipped draft 20 as an extension of the JWT inspector; ISO mdoc CBOR decoder deferred.
6. Argon2id (§2.9).
7. age (§2.11).

### Phase 8 — Tier 2 and 3 locales

1. Russian, Turkish, Dutch, Japanese, Korean, Indonesian, Vietnamese (Tier 2).
2. Swedish, Czech, Hebrew, Persian, Urdu, Bengali, Tamil, Swahili (Tier 3).
3. Each goes through the same 7-pass review.

---

## 7. Tensions and non-goals

These are not deferred — they are **rejected** for this project.

1. **No PII validators.** Italian Codice Fiscale, Polish PESEL, Spanish DNI/NIE, German Steuer-ID, French INSEE/NIR, Dutch BSN, UK NINO, US SSN — all rejected. They are personally identifying numbers and a validator is borderline a data-handling tool. Commercial identifiers (IBAN, VAT, BIC, GS1, ISIN, CUSIP) are fine.
2. **No EU compliance theater.** Cookie banner generators, DPA templates, "GDPR audit checklists", "DPIA wizards", privacy-policy generators — all rejected. They are a different product and would dilute the focused, technical character of the site. Refer users to dedicated compliance products.
3. **No country auto-detection.** Locale comes from `navigator.language` and `localStorage`. The site does not infer country from IP, timezone, or any other signal. The same site is served to everyone.
4. **No "translation breadth over quality."** Five well-translated locales beat fifteen badly-translated ones. New locales ship only after Pass 3 (native review) of §1.3. The README's current note ("fr/de/zh/hi are machine-quality awaiting review") gets honored: those four go through native review *before* any new Tier 1 locale ships.
5. **No fetching trust lists or revocation data.** The eIDAS and LOTL tools (§2.5, §2.6) decode what you paste; they do not phone home. This is a feature, not a limitation.
6. **No WASM unless we have to.** Pure JS first. WASM only if a real user complains and there's no other way to deliver acceptable performance (the Argon2 trade-off in §2.9 is the canonical case).
7. **No build step beyond what already exists.** Vendored scripts only. No npm install, no bundler, no transpiler. If a new tool requires a real build system, it's not the right fit for this site.
8. **No telemetry of any kind.** Including "anonymous usage metrics" or "error reporting." If something breaks, users file an issue.
9. **No accounts, no sync, no cloud anything.** The single most-requested "feature" for tools like this. Permanent no.
10. **No mobile-app wrapper.** PWA install (§4.5) is enough. No Cordova, no Capacitor, no Tauri. The browser is the platform.

---

## 8. Acceptance criteria per workstream

A workstream is "done" when:

- **i18n infrastructure (Phase 1):** RTL renders correctly with `dir="rtl"` set on the existing site; plural strings use `Intl.PluralRules`; command palette accepts at least one French synonym and finds the right tool.
- **A new locale (Phases 2, 3, 8):** all 900 rows in `strings.csv` have `<locale>_status = reviewed`; all 7 passes (§1.3) completed and signed off; locale present in `hreflang`, language picker, `build-i18n-variants.js`; `/<locale>/index.html` exists and crawls; one external reviewer (native speaker, technically literate) has clicked through every tool in the locale and not flagged issues.
- **A new tool (Phases 4, 7):** runs offline (verified by disabling network in DevTools); CSP unchanged or extension justified; constant-time comparisons where applicable; output round-trips; existing test harness covers golden path; entry in `STRINGS` and in the command palette.
- **A regulator preset (Phase 6):** linked to the regulator's actual document (not a summary); dated in the help text; parameters in a JSON config (no hardcoded logic); audit-release script flags it for re-review one year out.
- **Distribution (Phase 5):** mirror pushes succeed automatically on tag; SHA-256s reproducible from a clean clone; onion address resolves; PWA install prompt fires on Chrome and Firefox.

---

## 9. Open questions

These need answers before the relevant phase starts:

- Who are the native reviewers? Building a small list of reviewers per Tier 1 locale (one per locale minimum) is a prerequisite to Phase 2. Reach out via privacy-tools mailing lists, Tor community, Codeberg communities.
- Is there appetite for a `MAINTAINERS.md` listing per-locale reviewers (with their consent) as a transparency measure?
- Should the single-file portable build (§4.6) be linked from the site itself or only from the repo? Linking from the site adds clarity but also adds a download that could lag the live site.
- For the EUDI Wallet decoder (§2.8): wait for the ARF to stabilize (likely mid-2026), or ship against the current spec and accept churn?
- For the Argon2 trade-off (§2.9): is the WASM CSP change worth the perf? Possibly skip Argon2 entirely until Web Crypto adds it natively (under discussion in W3C).

---

## 10. Maintenance

This spec is living. When a phase completes, update the rollout section to reflect what shipped. When a regulator preset's source document is superseded, update §3. When a translation glossary entry in §1.7 is corrected by a native reviewer, update the table and note the reviewer's pseudonym (with consent) and date.

The spec is the source of truth for *intent*; the code is the source of truth for *behavior*. Where they disagree, fix one or the other — never let the gap widen.
